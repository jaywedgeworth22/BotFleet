[AG] repo: BotFleet. Completed unattended model downgrades and 180s timeout sentry fix in PR #533.
- **2026-09-24 — AG — IN PR — Fix underlying BotFleet routine failures: VPS SSH timeouts & non-cloud degradation, unattended antigravity host_control_policy refusal, and DSH bridge process leak rpc_errors (board `75660cb6`, branch `ag/routine-failure-resiliency`, worktree `~/apps/botfleet-ag-routine-fixes`).**  Prevents recurring routine crashes across Compiler, Monitor, Director, Plumber, Housekeeper, and Publisher: (1) degrades unreachable VPS mounts with a notice for non-cloud turns instead of aborting the turn, (2) skips host Mac desktop mounting on unattended Antigravity turns to prevent host_control_policy refusals, and (3) fixes process lifecycle leaks in dsh-acp-bridge.ts that caused zombie accumulation and ACP initialize timeouts (rpc_error).
- **2026-09-24 — CLAUDE — IN PROGRESS — Fix program for the efficiency audit (fleet mode): sub-agents on separate `claude/fix-*` lanes address the 97 findings in `docs/audits/2026-09-24-efficiency-audit.md`.**  Wave 1: persistence and disk reclamation (HS1-HS3, HS9; row `4e5b7bb6`), harness hot path (HS10, HS11, HS7; `ddb3e34b`), boot and recovery (HS18-HS20, HS22; `468b8719`), APNs sender (IO1-IO6, IO23, IO25; `55c8fd23`, `82510efe`), iOS network and health (IO10-IO13, IO17, IO5; `80071e15`).  Wave 2: JSON write coalescing (`43106963`), thread cache and SSE (`6e105eac`), pollers and telemetry (`74a6cd37`, `da3acb77`), drivers (`090d92d9`, `9e024637`), renderer (`173c8390`), ops and CI (`a6a3793c`, `dce47dba`, `a41274f7`, `bac463c4`), iOS rendering, hygiene (`0257906a`).  Lanes share one dependency tree via symlink because the Mac has 13 GB free.  Each package lands as its own PR with auto-merge; this row is updated as they merge.
- **2026-09-24 — CLAUDE — COMPLETED (PR #594, docs-only) — Upstream OpenMausBot review: what BotFleet can learn from and borrow since the fork.**  `docs/audits/2026-09-24-upstream-openmausbot-review.md`: upstream v0.1.87 (698 merged PRs, 49 releases since Aug 31), no common git ancestor, per-area divergence table, license map (`enterprise/` is not Apache; nothing BotFleet carries was relicensed), borrow candidates across runtime and drivers, product and mobile UX, and infra/CI/companion with value and port cost, a ranked borrow order (five items already folded into the running fix lanes), one new security finding (no per-bot Composio grant check on the relay), what upstream could use from BotFleet (owner decision), and a monthly review habit with a `docs/upstream/PORTED.md` ledger.  Apple Note `[BF, Claude] Upstream OpenMausBot Review`.  Board row `ab19befc` completed.
- **2026-09-24 — CLAUDE — COMPLETED (PR #568 merged, docs-only) — Efficiency and performance audit of BotFleet (desktop app, harness, iOS companion + sidecar, Cloudflare workers, updater, LaunchAgents, telemetry, CI).**  `docs/audits/2026-09-24-efficiency-audit.md`: 97 verified findings (7 P0, 36 P1, 33 P2, 21 P3) with `file:line` evidence, live-host measurements, a harness log analysis, data-directory growth numbers, a reconciliation of all nineteen prior audits, a ranked fix list, and the problems with no identified fix yet.  Apple Note `[BF, Claude] Efficiency Audit 2026-09-24`.  THE BOARD: report row `107f9043` (completed); 17 review-finding rows filed for the P0 and top P1 items (`ddb3e34b` HS10 event tee, `82510efe` IO2 push latch, `80071e15` IO10 iOS Sentry pager storm, `bac463c4` IO21 sidecar supervision, `a6a3793c` OP1 packaged Sentry SDK, `74a6cd37` OP2 telemetry outbox, `43106963` HS4-6 JSON rewrites, `6e105eac` HS12 thread cache, `090d92d9` DR2-3 engine retry, `9e024637` DR1/DR4 session reuse, `173c8390` UI1-3 renderer, `da3acb77` OP3 quota pollers, `dce47dba` OP4-5 updater, `0257906a` RH1-4 worktrees, `a41274f7` OP6/OP10 resource watch, plus `468b8719` HS18 restart re-dispatch, `4e5b7bb6` HS1 transcript reclamation, and `55c8fd23` IO1 APNs breaker).  Owner action flagged: the APNs signing key is being refused (InvalidProviderToken), so closed-app pushes are off until it is verified or rotated.
- **2026-09-24 — AG — COMPLETED (PR #536 merged `501accc9`, PR #537 merged `ad758edf`, deployed) — Effort-level gating per model, fast engine discovery cache, dynamic avatar fallback logo (board `bf789dc6`, branch `ag/effort-discovery-fallback`, worktree `~/apps/botfleet-ag-effort-and-fallback`).**  1. Gating reasoning effort levels on model-level capability (e.g. MiniMax, non-reasoning OpenAI/Claude models) rather than engine-wide capability.  2. Accelerating initial engine discovery via persistent disk cache (`DATA_DIR/engine-cache.json`) and bypassing full PATH resets on passive hydration so all installed/configured engines are immediately available on boot instead of lagging.  3. Dynamic avatar provider badge updating to show active fallback model/engine during rollover instead of default model.  4. Review feedback follow-up (PR #537): effort clearing isolation across tasks/fallbacks, defaultSelection candidate validation against loaded entries, inheriting task activeModelSelection clearing, and codex effort filtering via isEffortLevel.  Deployed to production Mac server via update-botfleet.sh and verified live on port 8799.
- **2026-09-23 — AG — COMPLETED (PR #532 merged `bc8eebe3`) — Webhook & trigger efficiency revamp: domain slimmers (Sentry/PagerDuty), deduplicated batch folding, ingress event pre-filtering & automation thread compaction (board `e503e6d1`, branch `ag/webhook-trigger-performance`, worktree `~/apps/botfleet-ag-webhook-perf`).**  Solves recurring Grok ACP session/prompt timeouts, 54 MB Compiler thread bloat, and unslimmed 48 KB Sentry payloads.  Implements domain-specific Sentry and PagerDuty payload slimming in `server/webhook-payload.ts`, deduplicated instruction wrapping in `server/trigger-gap.ts`, ingress filtering in `server/webhooks.ts` to drop out-of-scope warnings/info pings, and thread rollover/compaction in `server/routines.ts` and `server/turn-context.ts`.
- **2026-09-23 — MM — COMPLETED (merged in #528 `a7d6a4f4`) — [BotFleet][MM] usage engines revamp: capability matrix, usage expand, API-vs-sub projection, grok quota (board `eb774a6a` closed, branch `minimax/usage-engines-revamp`, worktree `~/apps/botfleet-mm-usage-engines`).**  Single-source `src/lib/engine-capabilities.tsx` registry backing the matrix, the callout, and the projection.  `<EngineCallout>` replaces the MiniMax-only `<MiniMaxCallout>` (deleted along with its test) and now mounts in `EnginesSettings.tsx` and `ModelPicker.tsx` for every installed engine.  New `<EngineCapabilitiesMatrix>` renders a 12×7 grid (capabilities × engines) with a `PricingModeChip` per column header and hover-to-surface prose.  Cost-per-bot block in `UsageSection.tsx` is now a collapsible `<UsageRow>` with a per-session table (timestamp, model, tokens in/out, cached, $/turn, cumulative tokens, cumulative cost) plus "Expand all / Collapse all".  The MiniMax-only pricing card replaced by a registry-driven "Pricing Mode by Engine" card that shows "Subscription — included in plan" instead of an API rate, except when EVERY engine in the set has an API block (which never happens in practice).  New `<UsageWhatIfProjection>` card computes API-equivalent cost from real token usage × the registry's API block, with saved amount + saved % per engine; mirrors into the standalone Usage Monitor.  New `server/grok-quota.ts` mirrors `server/antigravity-quota.ts`; today the `grok` CLI has no quota subcommand, so the poller returns an honest `method: "no-source"` stub with a populated reason — the UI surfaces a "no quota source available yet" line.  Tests added for every new file (38/38 passing across the 6 new/updated vitest suites after `vite.config.ts` was extended to include `src/**/*.test.tsx`; `pnpm typecheck` clean).  Audit doc at `docs/audits/2026-09-23-botfleet-settings-revamp.md`.  Deviations: (a) Vercel preview deployment still rate-limited (`api-deployments-free-per-day`) — not a required status check so not blocking; (b) `~/Code/Usage-Monitor` mirror not shipped this round per task brief "Mirror is fine — Usage Monitor has its own repo workflow"; (c) vitest pick-up surfaced a pre-existing gap — component `.test.tsx` files had been silently excluded by `vite.config.ts` include, fixed by adding the pattern so CI's test-floor wrapper actually counts and runs them (EngineCallout's HTML-entity assertion also fixed in the same commit `33792c72`).
- **2026-09-23 — MM — IN PR — [BotFleet][MM] Computer Use settings redesign: per-provider toggles + impact-confirm + matrix (board `9613f135` / `660f5bf2`, branch `minimax/computer-use-settings-redesign`, worktree `~/apps/botfleet-mm-computer-use-settings`).**  Replace the three legacy toggles in the Computer settings section with four per-provider `<ComputerProviderToggle>` rows (ASCII.dev Box, Self-Hosted VPS, Local VM, This Computer) and a `<VpsModeToggle>` (Shared / Per-Bot / Not Used).  Disabling a provider that any bot currently uses opens a `<ComputerImpactConfirmModal>` listing every bot that would lose a leg of its grant, with "Disable anyway" (accent red) and "Cancel" (neutral).  A `<BotComputerMatrix>` row-per-bot table shows the per-provider grant for every bot, with a header "Apply new default to all" button.  Schema migration in `shared/local-auto-consent.ts` adds `ComputerProviders` + `VpsMode`; `migrateComputerProvidersConfig` runs in `loadConfig()` and `migrateComputerProviders` runs in `electron/main.mjs`'s boot, both idempotent.  Local VM runtime setup is preserved in `<LocalVmRuntimeCard>` so the redesigned section does not silently drop the one-click container install.  Audit at `docs/audits/2026-09-21-computer-use-settings-redesign.md`.  `pnpm typecheck` clean; 22 new tests in `src/lib/computer-providers.test.ts` (22/22 pass); 5 existing `shared/local-auto-consent.test.ts` pass; 115 `server/config.test.ts` pass; 45 `server/computer-grants.test.ts` pass; 53 `src/state/store.test.ts` pass; 3 `LocalComputerAutoWarning.test.ts` pass.
- **2026-09-21 — AG — COMPLETED (Harness PR #9 merged `b824d66`, BotFleet PR #518 merged `2b6b907b`) — Fix DSH MiniMax model option serialization (provider minimax) (board `41b6134a`, branch `ag/dsh-minimax-model-option`, worktree `~/apps/botfleet-ag-dsh-minimax`).**  In jaywedgeworth22/Harness, updated `dshModelOptionValue` and `dshModelIdFromOptionValue` to serialize and deserialize MiniMax models under provider `minimax` (e.g. `["minimax","MiniMax-M3"]`) instead of hardcoding `deepseek-official`, fixing DSH ACP `session/set_config_option` rejection on model switch.  Bumped `harness` dependency in BotFleet, re-exported `DSH_MINIMAX_PROVIDER_ID` and `dshProviderForModel` from `server/drivers/acp/dsh.ts`, and added test assertions for round-trip encoding/decoding.  Landed in Harness PR #9 and BotFleet PR #518, deployed to production via `update-botfleet.sh`.
- **2026-09-21 — AG — COMPLETED (PR #517 merged `a3ec130b`) — Fix Computer Use toggle selection highlight, Settings navigation label, and VPS setup hint (board `ce2e4194` / `31636bd1` / `bbe48a9a`, branch `ag/computer-use-settings-and-design`, worktree `~/apps/botfleet-ag-computer-settings`).**  Fixes light-mode invisible selection on Self-hosted VPS segmented control by changing `bg-raised` to `bg-control font-medium` across `BotComputerDefaults` and `CloudBackendPicker`.  Relabels Settings nav section from 'Local VM' to 'Computers' with comprehensive search keywords.  Adds inline VPS setup guidance when SSH host alias is missing.  Resolves sentence gap spacing across Computer Use settings copy.  Landed in PR #517 and deployed via `update-botfleet.sh`.
- **2026-09-21 — AG — IN PR #516 (auto-merge armed) — Fix Antigravity host policy refusal and unblock model fallback (board `2a9a509b`, branch `ag/platform-capabilities-and-fallbacks`, worktree `~/apps/botfleet-ag-platform-capabilities`).**  Honors user auto-approval (`turn.autoApprove` or `config.fullAuto`) on host control turns so tools run smoothly under `always-proceed` without false policy blocks.  Fixes fallback suppression in `turnProducedAssistantOutput` where the `ANTIGRAVITY_HOST_CONTROL_NOTICE` chip was marked `ok: true` and treated as assistant tool output, unblocking failover to secondary engines.  Degrades host computer cleanly on auto-fallback when interactive asks cannot be brokered.
- **2026-09-21 — AG — COMPLETED (PR #514 merged `3e9f65d0`) — iOS companion: 'Open BotFleet on Mac' action; stop bot cancel/snooze loop & pause/resume across updates (board `88d92756` / `9c03e8cb`, branch `ag/ios-open-desktop-on-mac`, worktree `~/apps/botfleet-ag-open-mac`).**  Added client and session methods for `POST /api/desktop/open` to let companion request opening or unhiding BotFleet desktop app on the paired Mac.  Update `ChatListView` StatusBanner with actionable 'Open BotFleet on Mac' button alongside offline diagnostic.  Fixed bot stop/interrupt loop: atomic cancellation of all queued/running/waiting runs for a bot and snoozing background triggers until explicit user action, preventing loops where Compiler or other bots restart 5 seconds later.  Enhanced updater and runtime quiesce to allow update initiation while busy: snapshots in-flight work to `pending-update-resume.json`, quiesces turns, and automatically clears snoozes and requeues runs on harness reboot on the updated build.
- **2026-09-19 — MM — IN PR — [BotFleet][MM] engines polish: collapsible MiniMax callout, drop MM-M2.7 from DSH, Codex default → Luna (board `b5953d96`, branch `mm/engine-polish`, worktree `~/apps/botfleet-mm-engine-polish`).** MiniMax callout text duplicates across EnginesSettings.tsx and ModelPicker.tsx, pushing model names off-screen; extracted to a shared collapsible `src/components/MiniMaxCallout.tsx` with a "Why this engine?" disclosure while pointing to DeepSeek Harness + MiniMax M3 for full tool support. Dropped MiniMax-M2.7 from STATIC_DSH_MODELS in `server/drivers/acp/dsh.ts` (M3 dominates on context and cost). Updated STATIC_CODEX_MODELS default to `gpt-5.6-luna` in `server/drivers/codex-catalog.ts`. Tests updated across `dsh.test.ts`, `codex-catalog.test.ts`, and `MiniMaxCallout.test.ts` (48/48 green, `pnpm typecheck` clean).
- **2026-09-19 — MM — COMPLETED (PR #481 merged `c1bf6aa8`) — quota dual-window: route non-MiniMax engines through DRIVER_KIND_PROVIDERS (board ebecfe8e, branch `ag/model-picker-dual-quota`, worktree `~/apps/botfleet-mm-quota-fix`).**  AG's lane, CRITICAL Seer thread `PRRT_kwDOUHUvas6j6pZT`: `server/harness/registry.ts:418` was building `providerKey = inst.driverKind === "openai" ? "chatgpt" : inst.driverKind`, but quota windows are tagged with canonical provider keys ("anthropic", "openai", "xai", ...) rather than driver kinds ("claudeAgent", "codexAgent", "grokAgent").  The filter `w.providerKey === providerKey` therefore returned `[]` for every non-MiniMax engine and silently dropped every injected quota window.  Switch to `quotaProviderForDriver(inst.driverKind)` from `server/quota-window-map.ts` (the same DRIVER_KIND_PROVIDERS map the engine selector already uses), import the helper, wrap the inject body in `if (providerKey)` because `describeEntry` is reached through `Promise.all(this.entries().map(...))` — `continue` is out of scope there.  39/39 `server/harness/registry.test.ts` pass.  CI green on macos/ubuntu/windows + control-plane + package+smoke + Swift + Vercel.  Lane tag kept as `ag/*` — committing on AG's branch keeps their PR history clean.  Extra-ship no.
- **2026-09-19 — MM — IN PR — quota dual-window: route non-MiniMax engines through DRIVER_KIND_PROVIDERS (board tbd, branch `ag/model-picker-dual-quota`, worktree `~/apps/botfleet-mm-quota-fix`).**  AG's lane, CRITICAL Seer thread `PRRT_kwDOUHUvas6j6pZT`: `server/harness/registry.ts:418` was building `providerKey = inst.driverKind === "openai" ? "chatgpt" : inst.driverKind`, but quota windows are tagged with canonical provider keys ("anthropic", "openai", "xai", ...) rather than driver kinds ("claudeAgent", "codexAgent", "grokAgent").  The filter `w.providerKey === providerKey` therefore returned `[]` for every non-MiniMax engine and silently dropped every injected quota window.  Switch to `quotaProviderForDriver(inst.driverKind)` from `server/quota-window-map.ts` (the same DRIVER_KIND_PROVIDERS map the engine selector already uses), import the helper, wrap the inject body in `if (providerKey)` because `describeEntry` is reached through `Promise.all(this.entries().map(...))` — `continue` is out of scope there.  39/39 `server/harness/registry.test.ts` pass.  Lane tag kept as `ag/*` — committing on AG's branch keeps their PR history clean and the Sentry thread auto-resolves once CI is green.  Extra-ship no.
- **2026-09-19 — MM — COMPLETED (PR #498 merged `6b1b18b4`) — Composio fleet-user_id session recipe + --check-only read-only fix (board dd16c6ab, branch `mm/composio-fleet-user-id`, worktree `~/apps/botfleet-mm`).**  `scripts/composio-fleet-session.sh` is the one-shot fleet recipe: mints a Composio Platform session under the shared `user_id=jay-fleet` so every seat (MM, Claude, Cursor, Codex, Grok, Antigravity) gets its own session MCP URL while OAuth-granted connections (Slack, GitHub, etc.) flow through one user_id.  Persists `~/.secrets/composio-session-id` chmod 600; `--check-only` is genuinely read-only (verify the persisted sid against `/api/v3.1/tool_router/session/{sid}` and exit non-zero on missing/empty/stale — a session-start hook will now actually fail loudly instead of silently minting a new session and reporting success); `--print-for none|json|claude-code|cursor|antigravity|grok|codex` prints a paste-ready MCP config snippet for any seat.  Follow-up on top of the initial commit: persist `mcp_url` on line 2 of the out file alongside `session_id`, so --check-only reads the API-issued URL verbatim instead of reconstructing a guessed `backend.composio.dev/tool_router/{sid}/mcp` template (Sentry thread `PRRT_kwDOUHUvas6j9mXP`, MEDIUM).  Older single-line sid-only files now fail fast with a re-mint hint.  `bash -n` and `shellcheck` clean; CI green on macos/ubuntu/windows + control-plane + package+smoke + Swift + Vercel.  Extra-ship no.
- **2026-09-19 — MM — COMPLETED (PRs #482 + #496 merged, PR #489 follow-up) — ubf: skip when local checkout is already at origin/main; BOTFLEET_FORCE=1 to override; iOS Settings push-sender health row (board 084f91f3 / c3d62588, branch minimax/ubf-skip-if-current + minimax/ios-push-health-in-settings-fix, worktree ~/apps/botfleet-mm-ubf + ~/apps/botfleet-mm-push-health).** `ubf` is now a sub-second no-op when HEAD == origin/main after a successful fetch — `✅ Already at <SHA>`. Override with `BOTFLEET_FORCE=1` or `--force`/`-f`. Tracked `scripts/update-botfleet.sh` and the local `~/apps/update-botfleet.sh` both carry the logic (the local copy is the machine-installed variant, manually synced from the tracked one; the prior local edit was overwritten by the Sep 12 sync, so re-applied on top of the new structure). iOS Settings now renders `GET /api/companion/push-health` from the paired Mac — status row + last-sent + key-rejected + recent-error + dropped-count sub-rows; older sidecars that 404 show "Not reported by this computer" with a Retry button. PR #489 had a follow-up: SettingsIcon was `private` in SettingsView.swift, blocking the iOS app build at PushHealthSection.swift:56; PR #496 drops `private` (no callers outside Settings, no-op for everything else). 3 stale MM worktrees (`botfleet-mm`, `botfleet-mm-389`, `botfleet-mm-360`) were pruned — orphan sessions from before the Sep 12 sync, behind main, no open PRs; the only uncommitted work (`publish-ios-versions.test.mjs` staged but deleted in working tree) was stashed before removal.  Extra-ship no.
# BotFleet Effort Log — cross-agent board
- **2026-09-19 — AG — COMPLETED (PR #492 merged `d4acd2c5`) — Autocomplete (@ Bots, # Apps/Channels) & Mention Highlighting (board `ace58dcb`, branch `ag/mention-autocomplete-styling`, worktree `~/apps/botfleet-ag-mentions`).**  Added interactive multi-type autocomplete for `@` (bots, members, `@everyone`) and `#` (channels, connected and popular apps, routines) in the message composer with keyboard navigation and badges.  Rendered mentions visibly bolder and slightly larger (`font-bold text-[1.05em] text-accent`) across user message bubbles, plain text prompts, and bot markdown responses via a remark AST plugin (`remarkMentions`).  Handled multi-word app mentions (e.g. `#Google Sheets`) as a unified entity, and updated trigger detection to independently search for valid word-start triggers so embedded punctuation does not dismiss active queries.  22/22 unit tests green, full test suite passed, landed in PR #492, and deployed to Mac server via `ubf`.  Extra-ship no.
- **2026-09-18 — CLAUDE — PR #475 MERGED (`cd17a84d`), FOLLOW-UP IN PR #478 — Computer-capability parity: the verdict, one derivation, and the display drift behind it (board `7a3d30af`, branch `claude/computer-defaults-followup`, worktree `~/apps/botfleet-claude-computer-defaults`).**  VERDICT on the owner's parity ask: full parity is NOT reachable, and two of the four walls are not engineering-time problems.  No MCP *client* exists anywhere in this repo — every mount today is consumed by an external CLI that is its own client (`drivers/claude.ts:636`); `grok`, `minimax` and `openai-compat` read only `turn.toolHost` and never `turn.integrations`, so VPS and Local VM for them means writing the repo's first in-process MCP stdio client.  GUI control additionally needs vision and the HTTP lane is text-only (`chat-completions/loop.ts:148` types content as `string`; `TurnToolOutcome` has no image field) while `computer-proxy.ts:11` returns the frame in the same tool result as an MCP image block.  `openai-compat` targets arbitrary user endpoints, so uniform parity there is impossible by construction.  `boxAgent` runs on the ascii.dev substrate (`boxagent.ts:107` POSTs exactly `{provider, model, prompt}`) and cannot reach This Mac, a VPS or a Local VM from this repo at all.  Honest ceiling: 16/17 engines on Box — which needs NO MCP client, because `computer-proxy.ts` is already BotFleet's own minimal MCP server whose `call()` dispatcher is all REST and can be extracted as in-process executors — 16/17 on VPS/Local VM only if that client lane is funded, and `boxAgent` Box-only forever.  Steps for the HTTP-lane expansion belong on the pre-existing PLANNED row `da75e2da`, not here.  DO NOT set `computerMcp: true` on those drivers as an interim: it flips `mountsCloudComputer`, provisions a real billed box and claims a Local VM lease, for drivers whose `sendTurn` never reads the mount — a silent no-op that spends money.  #475 shipped what IS buildable: `server/computer-capability.ts` as one derived source of truth replacing three hand-copied server expressions plus a drifted client mirror, pinned by a test that re-derives the old expressions verbatim and asserts equality for all 17 engines; the client had ORed `computerMcp` into host-control support against the server's `localComputerMcp`-alone rule; `instanceSupportsCloudComputer` structurally could not see the cloud backend, so a Computer-engine bot on VPS was offered Cloud then died at `vps-computer.ts:1013`; and the consolidation's own fail-open lookup was held behind `engineReachKnown` so the `/api/instances` hydration window cannot fire a provision that starts a container or a billed box.  Survived a five-dimension adversarial review (7 findings, 5 survived, 0 blocking — the lead P1 was proven pre-existing on `main`).  Merged through main's PR #460 refactor, which had moved the rewired block into `resolveTurnComputerMounts`.  #478 finishes the owner's review nits: `ConfigStatusFrame` dropped `botDefaults` on every config SSE frame (`store.tsx:511` Pick, replace-reducer at `:1158`), so wiring the panels to the resolver would have silently reverted after the first broadcast — and the operator route that SETS the workspace default was itself one of the emitters that wiped it.  Plus one resolved `cloudBackend` const in ComputerPanel instead of two, the picker showing resolved while still writing raw, and `SettingsPanel:732` which hid the "Start VPS automatically" toggle from inheriting bots while `index.ts:10533` required it — a dead end.  Two user-visible bugs fixed in passing: the shared sleep handler showed a sleeping box instead of a stopped container, and the viewer-close POST never fired on the box branch, leaking an SSH desktop tunnel on the owner's own Mac.  Config-frame blocker and the ComputerPanel inventory came from a peer session that stood down rather than open a competing PR.  OPEN QUESTION for the owner: pinning `cloudBackend` is one-way — `index.ts:7909` accepts only box/vps and nothing clears it, so showing the resolved value makes an irreversible pin look like a no-op click.  Extra-ship no.
- **2026-09-18 — GROK — IN PR #467 — Slim GitHub webhook prompts so Compiler does not time out onto Antigravity argv (board `34f10c9d`, branch `grok/webhook-prompt-slim`, worktree `~/apps/botfleet-grok-webhook-prompt-slim`).**  Unstick: mergeable but blocked on Sentry thread (`slimActor` dropped push `pusher` `{ name, email }`) plus known Ubuntu package+smoke CUA leftover flake.  `slimPusher` keeps name/email.  Merged origin/main through #468.  Extra-ship no.
- **2026-09-18 — CLAUDE — IN PR #474 — Carry the whole AgentBar quota handoff and let a spent subscription divert auto-fallback (branch `claude/quota-intake`, worktree `~/apps/botfleet-claude-quota-intake`).**  BotFleet kept 14 of the 22 per-window fields AgentBar publishes, hardcoded `skip: false`, and stored the rows where routing never saw them, so a spent subscription never diverted auto-fallback.  Now every field lands, plus the producer's newer `producer` and `issues` keys — an allow-listed provider that failed to read renders "Claude: <reason> (from AgentBar)" as plain text capped at 160 characters instead of vanishing from the grid.  `MODEL_TYPE_FAMILIES` maps AgentBar's own family names (`opus`, `sonnet`, `haiku`, `gemini`, `third-party`) onto catalog models, so a family-level window caps every model of that family instead of nothing; `driverKindsForWindow`, `modelsToSkip` and `isPlanLevelSkip` were added to, never reshaped.  One `NEAR_CAP_PERCENT` (exactly `<= 20`, 0 exhausted) now backs the engine chip, the grid cell and the derived status.  `applyLocalPayload` caps only on AgentBar's own `fileSkip`/`isExhausted` under `usage.localQuotaRouting` (default ON, Settings toggle); MiniMax, Antigravity and Grok Bot stay excluded.  Also repaired two raw control bytes commit `fef9fde8` wrote into `server/local-usage-monitor.ts`, which made the file binary to git.  Adversarial review (three lenses, three refuters each, 63 agents) then confirmed seven findings reducing to four defects, all fixed on this branch.  Every dictionary indexed by handoff text is read through `lookupOwn`, so a `modelType` or `window` of "constructor" can no longer resolve to `Object` and throw `function is not iterable` out of a fire-and-forget poll, or record a cooldown whose end is NaN.  The whole of `poll()` now runs under one `finally { this.inFlight = false; }` with the local half fenced in a try/catch of its own, so a bad handoff can neither strand the poller for eight days nor terminate the harness on every boot.  A `modelId` caps only when the instance catalog lists it, falling back to the row's family otherwise, so nothing is stored under a cooldown key no bot looks up.  And the parser clears `isExhausted`/`fileSkip` on a passed reset the way it clears `remainingPercent`, so the chip, the grid cell (now one shared `isQuotaCellExhausted`) and the routing path release a window together.  5 test files / 118 tests green, typecheck clean, every new test mutation-checked.  Extra-ship no.
- **2026-09-18 — CLAUDE — IN PR #473 — Antigravity host control: gate the turn on the tool execution policy agy actually reports, and stop claiming a refusal that never happens (follow-up to PR #455, branch `claude/antigravity-policy-gate`, worktree `~/apps/botfleet-claude-antigravity-policy-gate`).**  #455 merged as `d2da4030` WITHOUT its P0 fix, so main still forces `--mode accept-edits` on a host-control turn and tells the owner that shell commands on this computer "are refused during this turn".  That claim is false on this Mac: `--mode` is only the agent execution mode, the separate Tool Execution Policy here is `always-proceed` (the init event of a stream-json run reports `permission_mode: always-proceed`), and `run_command` executes.  The driver now reads `permission_mode` off the init event and, on a turn that holds host control, ends the turn before any tool can run unless the reported policy is one that asks (`request-review`, `strict`, `proceed-in-sandbox`); an unreported or unrecognized value counts as unsafe.  The child is SIGTERMed rather than merely ignored, and lines already in the pipe are dropped, so a refused turn cannot emit a tool row, an answer or a token bill afterwards and settles exactly once.  Notice text replaced with a true statement and the three comment blocks that asserted an auto-deny corrected.  Seven new tests in `server/drivers/antigravity.test.ts` — the fake agy CLI gained a configurable `permission_mode`, an init hold, a tool-ran marker, a SIGTERM marker and a one-chunk burst mode — each mutation-checked; 44/44 green, `tsc` clean.  Deliberately NOT auto-merged: the last PR on this file merged early because auto-merge was armed before the fix existed.
- **2026-09-17 — GROK — IN PR #463 — Serialize packaged Sentry SDK init (branch `ag/fix-packaged-sentry-sdk`, worktree `~/apps/botfleet-grok-sentry-463`).**  Sentry review thread `server/sentry.ts`: `await loadSdk()` made `applySentryConfig` racy.  Applies now queue so two callers cannot both `shutdown()` and `init()` with no `close()` in between.  Merged origin/main after #462 (`f2529968`).  Extra-ship no.
- **2026-09-17 — GROK — COMPLETED (PR #462 merged `f2529968`) — Unstick Infisical Sentry DSN propagation (board `853b773b`, branch `ag/fix-infisical-sentry-dsn`, worktree `~/apps/botfleet-grok-sentry-462`).**  CI failed because `sentryDsnConfigured` now forwards a stored non-DSN and the leftover status test still expected `configured: false`.  The stored malformed value now reports as misconfigured (config), matching the env-pinned path, instead of disappearing as "not configured".  Extra-ship no.
- **2026-09-16 — CLAUDE — fleet Sentry+Seer+board sweep, follow-up pass on PR #445 (branch `claude/sentry-p1-sweep-20260916`, worktree `~/apps/botfleet-claude-sweep`).**  Board P1s `dc91bbc0` (Box pooling collision) and `51359709` (claude-driver exit-1 wall of connector tool names) were already resolved by an earlier pass of this same sweep (PR #445 open with the fix; #372 merged 09-13 for the driver one) — verified both, no rework.  This pass: merged `origin/main` (through #453) to clear PR #445's `docs/EFFORT-LOG.md` conflict; fixed the Sentry-bot review finding on that PR (`joinBox` claimed pool occupancy even when `mintDesktopUrl` returned null — false claim would block a peer bot's Sleep; now guarded like `provisionBox`, new regression test, 14/14 green, `tsc` clean); rewrote the Sleep-refusal banner per the owner's Designer-persona review comment to drop coordinator jargon (pool/TTL/bot-id) in favor of "Someone else is using this computer.  Try Sleep again after that session ends."  Sentry sweep: resolved 4 single/low-occurrence issues as known noise with documented reasoning — `BOTFLEET-J`/`BOTFLEET-F` (turn-failure telemetry doing its job, not a crash), `FLEET-INFRA-CN`/`FLEET-INFRA-CY` (pre-merge feature-branch CI blips, both branches merged clean, no recurrence). Left `FLEET-INFRA-C6` (277-event broad CI-failure fingerprint) and board `3c6f4f93` (P2, two OS-test-race CI reds) alone — both are being actively watched/triaged elsewhere (BF-Monitor's own comment says "no product fix mandated yet"), not a single fixable defect. `FLEET-INFRA-D3` already resolved, no action needed.
- **2026-09-16 — CLAUDE — COMPLETED (PR #445 opened, auto-merge armed) — Fleet Sentry+Seer+board P0/P1 sweep: Box pool occupancy guard; closed the already-fixed claude-driver P1; filed the stale-checkout root cause of BOTFLEET-M (board `dc91bbc0`/`51359709`/new `efcde7b5`, branch `claude/sentry-p1-sweep-20260916`, worktree `~/apps/botfleet-claude-sweep`).**  Board `dc91bbc0` (P1, Box VM pooling collision): `#117`'s deterministic 4-way pool slot (`boxNameFor`) means two different bots can land on the same physical Box VM; `sleepBox` archived it unconditionally even while a DIFFERENT bot's session was still live.  `server/box.ts` now tracks in-memory which bot most recently minted a live desktop link on a pooled box (20-minute TTL, `OMB_BOX_POOL_OCCUPANT_TTL_MS` for tests), refuses to sleep while a peer's claim is fresh, reports a same-slot collision to Sentry, and surfaces the other occupant via `boxStatus.sharedWithBotId`.  New `server/box-pool-occupancy.test.ts` (5 cases); existing box suites unaffected (13/13); `tsc -p tsconfig.server.json` clean.  Board `51359709` (P1, "claude driver exited 1 before result" wall of connector tool names) correlates 1:1 with Sentry BOTFLEET-8/BOTFLEET-9 — already fixed by PR #372 (2026-09-13, derives `--permission-prompt-tool` from the `mcp__botfleet` registration itself); verified via `~/.botfleet/errors.log` (exactly 2 occurrences ever, both pre-fix, zero recurrence in the week since) and resolved both Sentry issues citing the PR.  While triaging Sentry BOTFLEET-M (13 events, ACP session-resume timeout) found the ROOT CAUSE still live: `~/apps/botfleet-server`, the deployed harness checkout, is detached at `132b4560` — 37 commits behind `origin/main` — and is missing the `MAX_REPLAY_BYTES` turn-context cap from `#354`/`#439` (confirmed it DOES already contain the `#372` fix, so not a full rollback, just stale); filed board `efcde7b5` (P1) rather than fast-forwarding the live install myself, since that's an operational action outside a code-review sweep and other seats may depend on its current state.  Investigated with Seer and judged NOT worth a code change given volume/risk: BOTFLEET-A (Grok `session/set_model` rejection — env/CLI-version issue; would need widening shared `ProviderErrorCode`/`needsAuth` classification in `acp/core.ts` for 4 events), BOTFLEET-D (permission-request timeout — working as designed, a real signal not a bug), BOTFLEET-K (`stop_sequence` treated as a failed turn — plausible one-line allowlist fix in `sentry-ai.ts` but single ambiguous event, left alone rather than risk masking a real Monitor-bot truncation issue later).  fleet-infra `[botfleet]` CI issues (`FLEET-INFRA-C6/CN/CY/D3`): verified BotFleet's actual CI is green across the last 15+ runs including `main` (`Swift tests + iOS build` passing); C6's 269-occurrence "ongoing" status is a KNOWN broad-fingerprint issue (a prior CLAUDE seat resolved its real root cause 2026-09-08 and noted it keeps reopening on an unrelated windows-latest flake under the same fingerprint) — no botfleet-repo action taken, Sentry-fingerprint-rule scope is fleet-infra's, not botfleet's.  PRs #441/#443 checked for Seer/Codex review comments: none beyond rate-limit notices, nothing to address.
- **2026-09-16 — AG — IN PR #449 — Filter expected operational non-crash exceptions (ACP resume failure, permission timeout, provider timeout) from Sentry (BOTFLEET-M, BOTFLEET-D, BOTFLEET-7; branch `ag/sentry-errors-fix`).**  In `server/drivers/acp/core.ts`: removed redundant runtime.error emission on permission timeout before `finish("deny", "timeout")`. In `server/sentry-ai.ts`: filter out ACP resume failure ("The saved ACP session could not be resumed"), permission timeout ("nobody answered this permission request in time"), and provider response timeout ("timeout waiting for response") from capturing Sentry exceptions and record as warning breadcrumbs instead. Added unit test in `server/sentry-ai.test.ts`.
- **2026-09-16 — GROK — IN PR — DSH ACP mounts BotFleet MCP (local computer / CUA) (board `3bfa4c2c`, branch `grok/dsh-mcp-mounts`, worktree `~/apps/botfleet-grok-dsh-mcp`).**  Stock `@deepseek-ai/dsh-acp` rejects non-empty `session/new.mcpServers`.  BotFleet now wraps stock `dsh` with a stdio bridge that zeros the wire list and a `dsh --patch` overlay of `dsh-mcp-client` rows built from the same mounts `core.ts` already sends.  `mcpServers: true` / `localComputerMcp` stay advertised.  Extra-ship no.  Do not merge.
- **2026-09-16 — CLAUDE — IN PR #455 — Antigravity advertised host control with no approval channel (gap C2; branch `claude/antigravity-approvals`, worktree `~/apps/botfleet-claude-antigravity-approvals`).**  `server/drivers/antigravity.ts` set `localComputerMcp: true` while `respondToRequest` answered "unavailable" unconditionally, so a full-auto bot holding the local computer ran `--dangerously-skip-permissions` on the person's own desktop with no card and no guard.  Took option (a) of the two on the table: `sendTurn` now runs acp/core.ts's guard — `controlsHost` from `hostToolPrefix(turnComputerMounts(...))` — and spawns that turn with `fullAuto` forced off, so agy's accept-edits mode auto-denies `run_command` instead of running it unseen, and the turn opens with a `notice` chip saying the engine cannot ask.  Option (b), `localComputerMcp: false` until agy exposes ACP, was rejected in the PR body: it removes a capability the owner opted into.  `server/contracts.ts` untouched.  Four mutation-checked driver tests.  Extra-ship no.  Adversarial review first — auto-merge not armed.
- **2026-09-16 — BF-COMPILER — HANDED TO GROK 2026-09-18 — Slim GitHub webhook prompts so Compiler does not time out onto Antigravity argv (board `34f10c9d`, prior branch `compiler/webhook-prompt-slim`).**  Uncommitted files only; GROK ported onto `grok/webhook-prompt-slim`.  Extra-ship no.
- **2026-09-16 — CLAUDE — COMPLETED (PR #446 merged) — Stop a failed Mac update relaunching itself forever (branch `claude/update-relaunch-loop`, worktree `~/apps/botfleet-claude-update-loop`).**  `launchctl submit` keeps a job alive on failure, so one deterministic `prepare-android-tools` download timeout was relaunched 204 times in a day: 344 stage directories, 2.8 GB, and one run log of 67,000 lines with 138 packaging attempts.  Android tools now copy from the override, a shared cache, the installed app bundle or the always-on checkout before ever reaching the network (ten-minute budget, two retries, cache write-back); the controller `launchctl remove`s the label the moment a run settles and the updater exits 0 on a relaunch whose run id already carries an outcome; abandoned stages are swept on boot and before each run, keeping the three newest and never a prepared or rollback stage.  No client-side auto-retry exists — every `/api/update/run` caller is an onClick.  Extra-ship no.
- **2026-09-16 — CLAUDE — COMPLETED (PR #447 merged) — iOS Mac Update card and TestFlight notice: six confirmed post-merge review findings from #381 (branch `claude/ios-mac-update-followup`, lane `~/apps/botfleet-claude-ios-update-followup`).**  P1 `loadMacUpdateStatus()` no longer routes failures to the app-wide alert, so a sleeping Mac or an older harness stops popping "Something went wrong" with raw text like `no route: GET /api/update/status`; the card's own `unavailableRow` carries it.  P1 the 5-second install poll now backs off 5/10/30/60s and gives up after ten minutes of silence onto a retryable "Lost contact during the update" row instead of alerting every tick and polling forever.  P1 `checkedAtText` parsed with a default `ISO8601DateFormatter`, which rejects the milliseconds `toISOString()` always emits, so every Mac showed a raw ISO string — parser moved to CompanionCore as `MacUpdateTimestamp` with tests.  P2 cancellation-aware `recordActionError` on the other two catch blocks.  P2 a 409 refusal no longer prints the same sentence twice.  P3 TestFlight throttle stamp rolled back on a failed fetch and the candidate build persisted so a relaunch still shows an undismissed notice.  P3 "Mac Update not available" in sentence case.  `swift test --package-path ios` 335 passing; iOS app target left to CI.
- **2026-09-16 — CLAUDE — COMPLETED (PR #448 merged) — Six confirmed post-merge review findings from the MiniMax quota parity work #388 (branch `claude/quota-parity-followup`, worktree `~/apps/botfleet-claude-quota-followup`).**  P1: the registry assigned straight over `snapshot.quota`, discarding the driver's own 402/429 cap — a blocked MiniMax account rendered "Available" and stayed in auto-fallback; it now merges.  P2: a weekly reset up to seven days out rendered as a bare clock time; it is now a countdown.  P2: the balance key and host are resolved once at registration, matching the driver's own closure, so a mid-session `mmx auth login` cannot show one account's quota while turns bill another.  P3: the balance host now uses the driver's own comparison, with `MINIMAX_DEFAULT_URL` pinned against the driver by a new test.  P2: the MiniMax "Spent:" figures print once.  P3: unavailable and disabled outrank "Near cap" in the status line and chip.  Extra-ship no.
- **2026-09-16 — CLAUDE — COMPLETED (PR #450 merged) — Push hardening follow-up: eight confirmed post-merge review findings from #383 (branch `claude/push-hardening-followup`, lane `~/apps/botfleet-claude-push-followup`).**  P1 the sidecar's `drain()` shifted the next alert off its lane before asking whether a signing key was usable, so a key that faulted during the send immediately before silently swallowed whatever was queued behind it — unsent, never requeued and uncounted, on the very lane that exists so a blocking approval is never lost; the key is read first now.  P1 every closed-app push was delivered twice: the sidecar wakes a phone whose stream is down, the phone reconnects and the harness replays the frame the push was built from, and the app posted a second local banner for it — and since #383 both copies carry Approve and Deny for a request only one of them can settle.  The harness frame's `seq` now rides in the push payload and a new `PushDeliveryLedger` in CompanionCore records what APNs has already shown, so the replay is recognised rather than drawn; a frame with no sequence is never suppressed, because silence is the worse failure.  P2 a thrown `fetch` got no retry at all while a 503 got three, so one reset connection lost an approval outright — transport failures now back off inside the attempt budget and report `0 SendFailed` only once it is spent.  P2 the fire-and-forget `void drain(...)` was unguarded, so a `forgetToken` that throws (it persists the device registry to disk) became an unhandled rejection and, under Node's default, an exit of the proxy every paired phone depends on.  P2 the watcher adopted the hello cursor even on a resumed stream, where that cursor is the tip and the gap is replayed after it, so a link that died before a replayed frame jumped the sidecar past everything it missed, permanently; it now makes the same cold-versus-resumed distinction the iOS client already makes.  P2 a failed lock-screen answer raised the app-wide "Something went wrong" modal on next foreground and cleared whatever was in `actionError` — `answerPendingRequest` answers `quietly` now, and the follow-up notification was already the user-facing report.  P3 the superseded-key guard compared object identity, so a .p8 rewritten with identical bytes read as a rotation and a genuine `InvalidProviderToken` for the key still in use was waved through while the health page kept saying pushes were on; it compares key identity via the digest the provider-token cache already computes.  P3 a key fault stranded whatever was already queued for a phone until an unrelated notification happened to arrive for that same phone; a reload that yields a usable key now sweeps the queues and restarts any that is idle and non-empty.  Seven new vitest cases, each mutation-checked against the unfixed code (48 in `companion/src/apns.test.ts`, 369 across `pnpm vitest run companion`); six new CompanionCore cases, 341 with `swift test --package-path ios`; both typechecks clean.  The iOS app target is left to CI, as this Mac cannot compile it.
- **2026-09-16 — CLAUDE — COMPLETED (PR #451 merged) — Eleven confirmed post-merge review findings from the Mac update control #382 (branch `claude/update-control-followup`, lane `~/apps/botfleet-claude-update-followup`).**  Built on #446.  The through line for the two worst is that every mutating API request holds an update admission for the length of its handler and runtime readiness counts admissions as work in flight, so a route that answered from the harness-wide reading reported the machine busy because of the question.  P1 `POST /api/update/check` came back with `canRun: false` and the busy refusal on the very response that had just found the update, and both clients gate Install Update on `canRun` — so the button never appeared until a refocus refetched the unadmitted GET; `check()` now takes the caller's readiness the way `start()` already did and the route passes `currentRuntimeReadiness(ownAdmissionActive)`.  P2 every refusal from `POST /api/update/run` carried the same wrong status, which the client stores.  P2 a run whose progress file stopped advancing was never settled — `processAlive` calls a reused pid alive on purpose — so a power cut mid-install left "An update is already running." forever and only deleting a file from a shell cleared it; `reconcile()` now asks `launchctl list <label>` through the exec seam once the record is ten minutes stale and settles as failed when launchd has no process, throttled to one probe a minute.  P2 `check()` no longer offers an update that merely differs from the installed commit, which is what produced "0 commits ahead" offers Install then refused.  P3 the detached fallback launcher attaches `child.once("error")`, since an unhandled `error` event on a child is an uncaught exception and the harness has no process-level handler.  P3 `current-run.json`'s `logPath`/`progressPath` are accepted only inside the runs directory, because the status route publishes that log's tail to every client and to the paired phone.  P1 the sidebar's install button no longer silently runs a check instead; it disables and says the harness's own reason.  P2 the hook's local error is cleared by a status that arrives without a `checkError`.  P2 the "You're up to date" tick is gated on a check that succeeded.  P2 the floating card shows the refusal reason in place of "This Mac can build and install it." and keeps Install Update visible but disabled, and the Settings card does the same and renders the commit subjects.  P3 the docs stop promising card content nothing rendered and name which three refusals reach `capabilities.reasons`.  Skipped the accumulating-run-artifacts finding: #446's `pruneRunArtifacts` already covers it.  Three new route-level tests in `server/index.test.ts` (OS-neutral — they assert the reason, not `canRun`), ten new cases in `server/update-control.test.ts` through the injected seams, two new pure client helpers tested; typecheck clean.  Extra-ship no.
- **2026-09-16 — CLAUDE — COMPLETED (PR #452 merged) — MiniMax connectability: five confirmed post-merge review findings from #387 (branch `claude/minimax-connectability-followup`, lane `~/apps/botfleet-claude-minimax-followup`).**  P2 Settings reported a blank MiniMax endpoint and said blank means the global host while the reserved instance called whatever host `~/.mmx/config.json` names — `minimax.url` joins `minimax.key` in `EXTERNAL_SECRET_SOURCES`, so the endpoint badge names that file the way the key badge already does, and only when the file really chooses a host; only the path reaches the payload, never the contents.  P2 an endpoint the operator explicitly pinned to the global host was silently overridden by that same file, because the gate compared `config.url` to `DEFAULT_URL` and "nothing configured" and "pinned to the global host" are byte-identical — provenance now travels with the value (`urlSource` on `driverConfigSchema` and `MinimaxConfig`, decided once in `decodeMinimaxConfig`), and the local endpoint applies only when nothing chose a host, matching the registry's own `rawUrl`/`hostWasChosen` pair.  P2 the added-connection diagnostics named Settings → API Keys, which renders one row per WORKSPACE engine id and by this driver's own gate can never reach an added connection; `setInstanceCredential` exists but only inside `createCustomEngine`/`deleteCustomEngine` and is absent outside the desktop shell, so both remedies now point at Settings → Engines, the control that exists.  P3 two new strings used two ASCII spaces for a sentence gap and render into plain `<div>`s that collapse the run — both now carry the literal U+00A0 plus a space, with regression guards.  P3 the widened `isCustom` made an unlisted driver's extra instance claim to be an OpenAI-compatible engine; `customEngineCalloutTitle` gives that heading only to `openai-compat` and names the driver otherwise.  The sixth finding (`registry.ts:408`) was already fixed on main by #415 and left alone.  `apps/docs/content/docs/providers/minimax.mdx` updated to match the new endpoint rules.  Verified: `pnpm typecheck` clean, 191 tests green across `minimax.test.ts`, `custom-engine.test.ts`, `engine-key-config.test.ts` and `config.test.ts`, plus the new `server/index.test.ts` case; the eight pre-existing `index.test.ts` MiniMax/instance failures reproduce identically at base — they need outbound network this box lacks.  Extra-ship no.
- **2026-09-16 — CLAUDE — COMPLETED (PR #453 merged) — Stop painting raw diagnostic text into the Mac update UI (branch `claude/update-copy`, lane `~/apps/botfleet-claude-update-copy`).**  `installBlockedReason()` painted the harness's raw structural-refusal sentence — a checkout path, a script path, "run it once from a terminal" — straight into the floating banner, the Settings Updates card, and the sidebar button label; `lastRunLabel()` appended the updater's own raw failure message, which can be a shell line like `bash update-botfleet-mac.mjs: pnpm package:mac:local failed with exit 1`.  The harness now exposes a stable `codes` array beside `reasons` in `server/update-control.ts` (`UpdateCapabilityCode`), so a client matches a refusal by code rather than by English text; `reasons` itself is unchanged, so an older desktop keeps working.  `installBlockedReason()` maps the three structural refusals — missing checkout, missing updater script, an out-of-date updater — to product sentences, and a new `installBlockedReasonDetail()` carries the harness's raw sentence for the hover title the banner and sidebar already had.  `lastRunLabel()` drops the raw message and shows a short headline plus when the run finished; a new `lastRunDetail()` carries the raw message for hover, wired into `UpdateBanner.tsx` and `SettingsModal.tsx`.  `pnpm vitest run src/lib/update-control.test.ts server/update-control.test.ts` 88 passed; `pnpm -s typecheck` clean; oxlint parity confirmed against `main` on every touched file.  Extra-ship no.
- **2026-09-16 — CLAUDE — IN PR #456 — DSH MCP patch overlay lands 0600 inside a 0700 directory (gap C5, branch `claude/dsh-patch-perms`, worktree `~/apps/botfleet-claude-dsh-patch-perms`).**  `writeDshMcpPatch` serialised every mount's environment — `OMB_COMMS_TOKEN`, `OMB_CONTROL_TOKEN` and any Composio key — into the shared temp root with no mode, so the overlay sat at the process umask, typically world-readable, for the life of a DSH session.  It now writes into a per-run `mkdtempSync` directory chmodded 0700 and passes `{ mode: 0o600 }` with a `chmodSync` fallback (`server/drivers/acp/dsh-mcp.ts:79-111`), and the bridge's existing cleanup removes that directory with the file, non-recursively and only on BotFleet's own prefix (`server/drivers/dsh-acp-bridge.ts:58-89`).  Follow-up to #443 by the GROK seat: same argv shape, same bridge protocol, only where and how the file is created.  Extra-ship no.
- **2026-09-16 — CLAUDE — IN PR #457 — Skills UI: a panel for imported Agent Skills, folder import, and an honest Computer-engine callout (gaps S1, S4, C6; branch `claude/skills-ui`, worktree `~/apps/botfleet-claude-skills-ui`).**  S1: `src/components/BotSkillsPanel.tsx` is the screen `server/skills.ts` always assumed — it lists installed skills with provenance, shows each SKILL.md plus the exfiltration-scan warnings and skipped files, and keeps Enable disabled until that SKILL.md has been opened.  Mounted beside Memory on the bot profile; no server route changed.  S4: an Import Skill Folder… affordance reads a skill folder already on this computer through the existing native picker, so `~/.claude/skills/<name>` goes through the same scan one skill at a time and lands disabled — `server/skill-folder.ts` plus a loopback-guarded `{folder}` door beside the GitHub `{source}` door in `server/index.ts`.  C6: EnginesSettings and the ModelPicker rail now say plainly that a bot on the Computer engine runs its turn on box.ascii.dev and therefore has no team tools, peers, approval cards, memory or skills.  Extra-ship no.
- **2026-09-16 — CLAUDE — IN PR #458 — Recall basics: tell bots the corpus exists, mount it on pi, cache the collection probe, fix the stats budget, and warn on a half-configured Access pair (gaps R1 R2 R7 R5 R3; branch `claude/rag-basics`, worktree `~/apps/botfleet-claude-rag-basics`).**  Twelve drivers mount the recall proxy and no bot was ever told it exists: new `server/recall-prompt.ts` gated on the mounted `integrations.qdrant` (never `cfg.qdrant`), concatenated at both assembly sites in `server/index.ts`.  pi gains the qdrant stdio mount and `qdrantMcp: true`.  `recall_stats` now gets the 30 s tool budget instead of the 12 s settings budget.  `verifyCollection` holds a good probe for 60 s for SEARCHES only and identical searches ride a 30 s LRU, both retired by any error, by a contribution, and by a `recall_stats` that finds the corpus not ready.  A contribution always re-probes: its body names no collection, so that probe is the only check that the service still owns the configured one, and a lesson misfiled into somebody else's corpus cannot be taken back.  After merging main's #465 both caches live in the shared `server/recall-tools.ts` and are keyed by a hash of the settings they were formed under, because the HTTP lane runs them in the harness process for every bot at once.  `accessTokenState` names which half of the Cloudflare Access pair is missing, rides both status payloads, and the panel says so.  Transport selection untouched; R4/R6 and the HTTP engines deliberately out of scope.  Extra-ship no.
- **2026-09-16 — CLAUDE — IN PR #460 — Room members keep their computers, and Grok works in a workspace: gaps C1 and C4 (branch `claude/room-computer`, lane `~/apps/botfleet-claude-room-computer`).**  C1: `runGroupMemberTurn` assigned only `agents`, `phone`, `composio` and `qdrant`, so a Claude, Codex, Antigravity, pi or ACP bot holding Cua, a Box, a Local VM or a VPS in a direct chat lost every one of them the moment it spoke in a room — while the HTTP lane in that same room kept host `bash` through `hasHostComputer`, so the engine that can broker approvals had no computer and the engine that cannot had a shell.  The ~230-line mount-resolution block moves out of `startTurn` into `resolveTurnComputerMounts` in `server/computer-grants.ts`, with the harness's leases, busy flags and message sink injected rather than imported, and both dispatchers call it and the shared `applyComputerMounts`.  Before granting, the room approval path was verified end to end against the honest-capability rule in `server/contracts.ts`: the `request.opened` fold lands the card on the room thread carrying the speaking member, `POST /api/threads/:threadId/respond` answers it by thread, `deliverDecision` routes the verdict back through the instance that asked, and the room deadline holds while a card is open — so no mount is withheld.  A room's VPS turn lease is now thread-keyed and released on `turn.completed`, because the existing release hangs off `store.botByThread` and a room thread has no owning bot.  `previewCapture` is deliberately dropped on the room path: the screen poller is started and stopped from the 1:1 halves of the turn fold, so one started in a room would never be torn down.  The HTTP lane is not lowered — `server/tools/**` and `server/turn-tools.ts` are untouched and `hasHostComputer` keeps its exact formula.  C4: `worksInWorkspace` excluded `grok` as well as `boxAgent` under a comment that stopped being true when #442 gave the HTTP engines workspace file tools through the `workspaceOrHostComputer` gate; Grok now gets a workspace, so a Grok bot with no "This Computer" grant gains `read_file`, `write_file` and `edit_file` plus MEMORY.md and the skills index, and boxAgent stays out for a reason about location rather than capability.  `server/drivers/grok.ts` untouched (MiniMax-series keep-out).  New `server/rooms-cli-lane.test.ts` (four injected-seam cases over the policy, three end-to-end on the real harness with the fake ACP CLI and a fake docker over SSH) and `server/grok-workspace.test.ts`; `server/rooms-http-lane.test.ts` gains one case pinning the HTTP lane unchanged.  Every new case mutation-checked once.  `pnpm -s typecheck` clean, 163 tests green across 17 suites, oxlint parity against `origin/main` on every touched file.  Extra-ship no.
- **2026-09-15 — GROK — COMPLETED (PR #402 merged 9280f71f) — Pickup Codex Local VM Auto consent (board `1c63f177`).**  Darwin-only automatic-host ack; explicit/inherited Local still require consent.  Issue #349 closed.  Codex branch continued; extra-ship no.
- **2026-09-15 — GROK — COMPLETED (PRs #401 #402 #348 #437; #438 auto) — Pickup Codex cap leftovers (board `5794a1c8`, worktrees Codex lanes + `~/apps/botfleet-codex-*`).**  Owner-directed after Codex quota 2026-09-13/14.  Hygiene closeout: merged Codex boards closed (see Completed).  Unique leftover owned by other workers: PR #401 dirty sanitization+atomic outbox (not on origin), PR #402 VM Auto consent (unresolved Darwin/provider thread + macos queue).  Still unfinished besides those: `5a2b2e02` owner-blocked MAC_CERT_*/RELEASES_PAT, `d24c7fd2` P0 BF-PLUMBER, #274 Mac acceptance `7e582b82`.  `2d627f55` / #271 closed via AFC #225 + BotFleet #437.  Did not steal #401/#402.  Did not push.  Extra-ship no.
- **2026-09-15 — AG — IN PR #433 — MiniMax models on DSH with native MiniMax branding and model catalog expansion (branch `ag/minimax-icons-and-dsh`).** Added MiniMax-M3 and MiniMax-M2.7 to `STATIC_DSH_MODELS` in `server/drivers/acp/dsh.ts`. Updated `src/lib/model-chip.ts` and `src/components/ModelPicker.tsx` so that running MiniMax through DSH displays the official MiniMax mark in chat bubbles and the model selector rather than the DeepSeek whale logo.
- **2026-09-16 — CLAUDE — COMPLETED (PR #446 merged) — Stop a failed Mac update relaunching itself forever (branch `claude/update-relaunch-loop`, worktree `~/apps/botfleet-claude-update-loop`).**  `launchctl submit` keeps a job alive on failure, so one deterministic `prepare-android-tools` download timeout was relaunched 204 times in a day: 344 stage directories, 2.8 GB, and one run log of 67,000 lines with 138 packaging attempts.  Android tools now copy from the override, a shared cache, the installed app bundle or the always-on checkout before ever reaching the network (ten-minute budget, two retries, cache write-back); the controller `launchctl remove`s the label the moment a run settles and the updater exits 0 on a relaunch whose run id already carries an outcome; abandoned stages are swept on boot and before each run, keeping the three newest and never a prepared or rollback stage.  No client-side auto-retry exists — every `/api/update/run` caller is an onClick.  Extra-ship no.
- **2026-09-16 — CLAUDE — COMPLETED (PR #447 merged) — iOS Mac Update card and TestFlight notice: six confirmed post-merge review findings from #381 (branch `claude/ios-mac-update-followup`, lane `~/apps/botfleet-claude-ios-update-followup`).**  P1 `loadMacUpdateStatus()` no longer routes failures to the app-wide alert, so a sleeping Mac or an older harness stops popping "Something went wrong" with raw text like `no route: GET /api/update/status`; the card's own `unavailableRow` carries it.  P1 the 5-second install poll now backs off 5/10/30/60s and gives up after ten minutes of silence onto a retryable "Lost contact during the update" row instead of alerting every tick and polling forever.  P1 `checkedAtText` parsed with a default `ISO8601DateFormatter`, which rejects the milliseconds `toISOString()` always emits, so every Mac showed a raw ISO string — parser moved to CompanionCore as `MacUpdateTimestamp` with tests.  P2 cancellation-aware `recordActionError` on the other two catch blocks.  P2 a 409 refusal no longer prints the same sentence twice.  P3 TestFlight throttle stamp rolled back on a failed fetch and the candidate build persisted so a relaunch still shows an undismissed notice.  P3 "Mac Update not available" in sentence case.  `swift test --package-path ios` 335 passing; iOS app target left to CI.
- **2026-09-16 — CLAUDE — COMPLETED (PR #451 merged) — Eleven confirmed post-merge review findings from the Mac update control #382 (branch `claude/update-control-followup`, lane `~/apps/botfleet-claude-update-followup`).**  Built on #446.  The through line for the two worst is that every mutating API request holds an update admission for the length of its handler and runtime readiness counts admissions as work in flight, so a route that answered from the harness-wide reading reported the machine busy because of the question.  P1 `POST /api/update/check` came back with `canRun: false` and the busy refusal on the very response that had just found the update, and both clients gate Install Update on `canRun` — so the button never appeared until a refocus refetched the unadmitted GET; `check()` now takes the caller's readiness the way `start()` already did and the route passes `currentRuntimeReadiness(ownAdmissionActive)`.  P2 every refusal from `POST /api/update/run` carried the same wrong status, which the client stores.  P2 a run whose progress file stopped advancing was never settled — `processAlive` calls a reused pid alive on purpose — so a power cut mid-install left "An update is already running." forever and only deleting a file from a shell cleared it; `reconcile()` now asks `launchctl list <label>` through the exec seam once the record is ten minutes stale and settles as failed when launchd has no process, throttled to one probe a minute.  P2 `check()` no longer offers an update that merely differs from the installed commit, which is what produced "0 commits ahead" offers Install then refused.  P3 the detached fallback launcher attaches `child.once("error")`, since an unhandled `error` event on a child is an uncaught exception and the harness has no process-level handler.  P3 `current-run.json`'s `logPath`/`progressPath` are accepted only inside the runs directory, because the status route publishes that log's tail to every client and to the paired phone.  P1 the sidebar's install button no longer silently runs a check instead; it disables and says the harness's own reason.  P2 the hook's local error is cleared by a status that arrives without a `checkError`.  P2 the "You're up to date" tick is gated on a check that succeeded.  P2 the floating card shows the refusal reason in place of "This Mac can build and install it." and keeps Install Update visible but disabled, and the Settings card does the same and renders the commit subjects.  P3 the docs stop promising card content nothing rendered and name which three refusals reach `capabilities.reasons`.  Skipped the accumulating-run-artifacts finding: #446's `pruneRunArtifacts` already covers it.  Three new route-level tests in `server/index.test.ts` (OS-neutral — they assert the reason, not `canRun`), ten new cases in `server/update-control.test.ts` through the injected seams, two new pure client helpers tested; typecheck clean.  Extra-ship no.
- **2026-09-16 — CLAUDE — COMPLETED (PR #448 merged) — Six confirmed post-merge review findings from the MiniMax quota parity work #388 (branch `claude/quota-parity-followup`, worktree `~/apps/botfleet-claude-quota-followup`).**  P1: the registry assigned straight over `snapshot.quota`, discarding the driver's own 402/429 cap — a blocked MiniMax account rendered "Available" and stayed in auto-fallback; it now merges.  P2: a weekly reset up to seven days out rendered as a bare clock time; it is now a countdown.  P2: the balance key and host are resolved once at registration, matching the driver's own closure, so a mid-session `mmx auth login` cannot show one account's quota while turns bill another.  P3: the balance host now uses the driver's own comparison, with `MINIMAX_DEFAULT_URL` pinned against the driver by a new test.  P2: the MiniMax "Spent:" figures print once.  P3: unavailable and disabled outrank "Near cap" in the status line and chip.  Extra-ship no.
- **2026-09-16 — CLAUDE — COMPLETED (PR #450 merged) — Push hardening follow-up: eight confirmed post-merge review findings from #383 (branch `claude/push-hardening-followup`, lane `~/apps/botfleet-claude-push-followup`).**  P1 the sidecar's `drain()` shifted the next alert off its lane before asking whether a signing key was usable, so a key that faulted during the send immediately before silently swallowed whatever was queued behind it — unsent, never requeued and uncounted, on the very lane that exists so a blocking approval is never lost; the key is read first now.  P1 every closed-app push was delivered twice: the sidecar wakes a phone whose stream is down, the phone reconnects and the harness replays the frame the push was built from, and the app posted a second local banner for it — and since #383 both copies carry Approve and Deny for a request only one of them can settle.  The harness frame's `seq` now rides in the push payload and a new `PushDeliveryLedger` in CompanionCore records what APNs has already shown, so the replay is recognised rather than drawn; a frame with no sequence is never suppressed, because silence is the worse failure.  P2 a thrown `fetch` got no retry at all while a 503 got three, so one reset connection lost an approval outright — transport failures now back off inside the attempt budget and report `0 SendFailed` only once it is spent.  P2 the fire-and-forget `void drain(...)` was unguarded, so a `forgetToken` that throws (it persists the device registry to disk) became an unhandled rejection and, under Node's default, an exit of the proxy every paired phone depends on.  P2 the watcher adopted the hello cursor even on a resumed stream, where that cursor is the tip and the gap is replayed after it, so a link that died before a replayed frame jumped the sidecar past everything it missed, permanently; it now makes the same cold-versus-resumed distinction the iOS client already makes.  P2 a failed lock-screen answer raised the app-wide "Something went wrong" modal on next foreground and cleared whatever was in `actionError` — `answerPendingRequest` answers `quietly` now, and the follow-up notification was already the user-facing report.  P3 the superseded-key guard compared object identity, so a .p8 rewritten with identical bytes read as a rotation and a genuine `InvalidProviderToken` for the key still in use was waved through while the health page kept saying pushes were on; it compares key identity via the digest the provider-token cache already computes.  P3 a key fault stranded whatever was already queued for a phone until an unrelated notification happened to arrive for that same phone; a reload that yields a usable key now sweeps the queues and restarts any that is idle and non-empty.  Seven new vitest cases, each mutation-checked against the unfixed code (48 in `companion/src/apns.test.ts`, 369 across `pnpm vitest run companion`); six new CompanionCore cases, 341 with `swift test --package-path ios`; both typechecks clean.  The iOS app target is left to CI, as this Mac cannot compile it.
- **2026-09-16 — CLAUDE — COMPLETED (PR #452 merged) — MiniMax connectability: five confirmed post-merge review findings from #387 (branch `claude/minimax-connectability-followup`, lane `~/apps/botfleet-claude-minimax-followup`).**  P2 Settings reported a blank MiniMax endpoint and said blank means the global host while the reserved instance called whatever host `~/.mmx/config.json` names — `minimax.url` joins `minimax.key` in `EXTERNAL_SECRET_SOURCES`, so the endpoint badge names that file the way the key badge already does, and only when the file really chooses a host; only the path reaches the payload, never the contents.  P2 an endpoint the operator explicitly pinned to the global host was silently overridden by that same file, because the gate compared `config.url` to `DEFAULT_URL` and "nothing configured" and "pinned to the global host" are byte-identical — provenance now travels with the value (`urlSource` on `driverConfigSchema` and `MinimaxConfig`, decided once in `decodeMinimaxConfig`), and the local endpoint applies only when nothing chose a host, matching the registry's own `rawUrl`/`hostWasChosen` pair.  P2 the added-connection diagnostics named Settings → API Keys, which renders one row per WORKSPACE engine id and by this driver's own gate can never reach an added connection; `setInstanceCredential` exists but only inside `createCustomEngine`/`deleteCustomEngine` and is absent outside the desktop shell, so both remedies now point at Settings → Engines, the control that exists.  P3 two new strings used two ASCII spaces for a sentence gap and render into plain `<div>`s that collapse the run — both now carry the literal U+00A0 plus a space, with regression guards.  P3 the widened `isCustom` made an unlisted driver's extra instance claim to be an OpenAI-compatible engine; `customEngineCalloutTitle` gives that heading only to `openai-compat` and names the driver otherwise.  The sixth finding (`registry.ts:408`) was already fixed on main by #415 and left alone.  `apps/docs/content/docs/providers/minimax.mdx` updated to match the new endpoint rules.  Verified: `pnpm typecheck` clean, 191 tests green across `minimax.test.ts`, `custom-engine.test.ts`, `engine-key-config.test.ts` and `config.test.ts`, plus the new `server/index.test.ts` case; the eight pre-existing `index.test.ts` MiniMax/instance failures reproduce identically at base — they need outbound network this box lacks.  Extra-ship no.
- **2026-09-16 — CLAUDE — COMPLETED (PR #453 merged) — Stop painting raw diagnostic text into the Mac update UI (branch `claude/update-copy`, lane `~/apps/botfleet-claude-update-copy`).**  `installBlockedReason()` painted the harness's raw structural-refusal sentence — a checkout path, a script path, "run it once from a terminal" — straight into the floating banner, the Settings Updates card, and the sidebar button label; `lastRunLabel()` appended the updater's own raw failure message, which can be a shell line like `bash update-botfleet-mac.mjs: pnpm package:mac:local failed with exit 1`.  The harness now exposes a stable `codes` array beside `reasons` in `server/update-control.ts` (`UpdateCapabilityCode`), so a client matches a refusal by code rather than by English text; `reasons` itself is unchanged, so an older desktop keeps working.  `installBlockedReason()` maps the three structural refusals — missing checkout, missing updater script, an out-of-date updater — to product sentences, and a new `installBlockedReasonDetail()` carries the harness's raw sentence for the hover title the banner and sidebar already had.  `lastRunLabel()` drops the raw message and shows a short headline plus when the run finished; a new `lastRunDetail()` carries the raw message for hover, wired into `UpdateBanner.tsx` and `SettingsModal.tsx`.  `pnpm vitest run src/lib/update-control.test.ts server/update-control.test.ts` 88 passed; `pnpm -s typecheck` clean; oxlint parity confirmed against `main` on every touched file.  Extra-ship no.
- **2026-09-16 — CLAUDE — IN PR #457 — Skills UI: a panel for imported Agent Skills, folder import, and an honest Computer-engine callout (gaps S1, S4, C6; branch `claude/skills-ui`, worktree `~/apps/botfleet-claude-skills-ui`).**  S1: `src/components/BotSkillsPanel.tsx` is the screen `server/skills.ts` always assumed — it lists installed skills with provenance, shows each SKILL.md plus the exfiltration-scan warnings and skipped files, and keeps Enable disabled until that SKILL.md has been opened.  Mounted beside Memory on the bot profile; no server route changed.  S4: an Import Skill Folder… affordance reads a skill folder already on this computer through the existing native picker, so `~/.claude/skills/<name>` goes through the same scan one skill at a time and lands disabled — `server/skill-folder.ts` plus a loopback-guarded `{folder}` door beside the GitHub `{source}` door in `server/index.ts`.  C6: EnginesSettings and the ModelPicker rail now say plainly that a bot on the Computer engine runs its turn on box.ascii.dev and therefore has no team tools, peers, approval cards, memory or skills.  Extra-ship no.
- **2026-09-14 — MINIMAX — COMPLETED (PR #415 merged 39bcf306) — MiniMax engagement audit on the [MINIMAX] seat: roll this Mac to origin/main (board `4564fe5e`/`f59883b6`).**  The integration tree at /Users/jay/Code/BotFleet sat 28 commits behind origin/main and the deployed harness at ~/apps/botfleet-server sat 15 commits behind (5a7fc404), so the live Mac was running without #387 connectability, #388 quota parity, #386 chat-completions retry, #385 rooms tools gate.  Fast-forwarded both to origin/main.  Discovered while rolling: PR #387 shipped `resolveMinimaxCredentials` with a new third `instanceId` argument that scopes the process.env / ~/.mmx/config.json fallback to the reserved `minimax` instance only, but `server/harness/registry.ts` was still calling it with two arguments — TS2554 tripped the mac-updater typecheck.  Landed as PR #415 with the one missing argument (CI green: typecheck + test ubuntu/macos/windows, Swift, control-plane + workerd, package + smoke, Seer; merged 39bcf306).  Live MiniMax now reads first-class: `/api/instances` shows 8 catalog models (M3, M2.7, M2.7-highspeed, M2.5, M2.5-highspeed, M2.1, M2.1-highspeed, M2) with `snapshot.quota.windowsLabel="5hr/Week"`, per-model `remainingPercent`/`secondaryRemainingPercent`, and a `snapshot.quota.minimax` block carrying `source:"token-plan"`, `capExists:true`, `status:"ok"`, `weeklyResetsAt`.  No MM-series lane touched; second-instance key-bleed gap closed.  Out of scope: PR 3 (#355, CODEX) and PR 11.
- **2026-09-14 — AG — COMPLETED — Fix Minimax percentage quota mapping and DSH balance visibility; fix Usage Monitor setup missing user-agent and infisical write-through block (branch `ag/bot-to-bot-heading`, worktree `~/apps/botfleet-ag-bot-heading`).** Fixed Cloudflare WAF block on `usage-quota.ts` fetch by adding user-agent; mapped `minimax` driver to `minimaxAgent` in window checks; mapped `dshAgent` driver for DeepSeek balance checks; removed hardcoded Gemini monthly cap logic from quota display; added `usage.*` and `observability.sentryDsn` to `syncCredentialEnv` to properly export to Infisical when write-through is enabled.
- **2026-09-13 — BF-DESIGNER — COMPLETED — Pickup Codex weekly-quota cap: UI lanes #408 #397 #348 #402.**  Extra-ship no.  GROK 2026-09-15: #348 merged `b3be62cd`.
- **2026-09-13 — AG — COMPLETED — HTTP computer tools (bash, read_file, write_file, edit_file) for MiniMax and direct DSH MiniMax auth (board `5241e335df964cd0a3e785cdd3faa496`, branch `ag/http-computer-tools-minimax`, worktree `~/apps/botfleet-ag-fixes`, PR #442).**  Enable local computer tools on the HTTP driver toolLoop lane gated by `localComputer` and `wantsLocal`, executing via `TurnToolHost` with permission broker approval cards for destructive operations (`bash`, `write_file`, `edit_file`); enable `localComputerMcp: true` on MiniMax driver adapter; allow toolLoop drivers to bypass missing CUA stdio error; support direct MiniMax API authentication in DeepSeek Harness driver via `MINIMAX_API_KEY` in `credentialEnv` and `isAuthenticated` without third-party gateways.
- **2026-09-13 — CODEX — COMPLETED — Durable Usage Monitor telemetry outbox (issue #399, PR #401, board `82ab4e6d`, branch `codex/telemetry-outbox`, worktree `~/apps/botfleet-codex-telemetry-outbox`).**  Persist stable child-turn event batches before delivery; retry across transient failure and restart; remove only acknowledged batches; bound queue and backoff; isolate changed destinations; expose safe sent/rejected/dropped counters.  No prompt, transcript, tool, or credential content.
- **2026-09-13 — AG — COMPLETED (PR #380 merged) — Dual-window quota display (5hr/Week), DeepSeek balance and rolling spend (board `137f0c87ef8a4cd085edf126a7f9e824`, branch `ag/quota-usage-windows`, worktree `~/apps/botfleet-ag-quota-usage`).**  Display dual window quota badges (XX%/XX%) and window labels in ModelPicker; fix DeepSeek remaining balance visibility and compute rolling 5h / 7d spend in UsageSection and /api/quotas.  Exclude estimated subscription charges, scan rotated logs, isolate DSH billing, and prioritize dual-window labels across quota rows.
- **2026-09-13 — AG — COMPLETED (PR #379 merged) — Fix chat bottom scroll with attachments and move Sentry bug report off send button (board `df0a3beec1b642d7979394c4e96b6cd6`, branch `ag/chat-scroll-sentry-feedback`, worktree `~/apps/botfleet-ag-scroll-sentry`).**  ResizeObserver measures composer height in ChatView and GroupView to set dynamic scroll padding so attachments do not occlude bottom messages; set Sentry feedback autoInject: false and add bug report button to Sidebar footer and turn error rows.
- **2026-09-13 — CLAUDE — COMPLETED — Mac updater hardening: nine defects from the post-merge adversarial review of #371 (branch `claude/updater-rollback-hardening`, worktree `~/apps/botfleet-claude-updater-hardening`).**  The worst was a recovery path that restored from whatever sat at the rollback path: with a reused `--stage` the cache placement had no per-run stamp, so a second apply hit an occupied path after the interruption boundary and recovery promoted a two-generations-old bundle over the live app.  Placement is now stamped per run, pre-existence is refused before the boundary, and recovery restores only from copies the swap recorded this run as having moved.  Process detection moved from `ps` arguments to the kernel's open reference, because arguments are captured at exec and do not follow a rename — so the survivor check added in #371 could never fire — and the check now covers the whole bundle rather than the main binary alone, catching the embedded cua-driver and the speech and recorder helpers that the rename would otherwise displace underneath.  Also: orphan bundles are detected in the cache placement, an explicitly named stage is discovered and prune refuses when the generation just written is not among those found, legacy receipts that name no application are claimable by nobody rather than by whichever install happens to be called BotFleet, abandoned dependency candidates beside the checkout are swept, and a receipt stranded at `installing` is settled when it names the build that was installed and verified.
- **2026-09-13 — CLAUDE — COMPLETED — Bound the per-thread transcript logs with a rotating byte cap (branch `claude/transcript-retention`, worktree `~/apps/botfleet-claude-transcript-retention`).**  `~/.botfleet/native/<threadId>.ndjson` and its `events/` sibling were append-only with nothing ever trimming them — 1.93 GB, 1.68 GB and 1.58 GB for three threads on the owner's Mac, about 6 GB in one directory, against an Inspector panel that never reads more than the newest 2,000 lines.  New `server/transcript-retention.ts` caps each file (64 MB native, 16 MB events) and enforces the cap by rotating to `<threadId>.ndjson.1` on the append that would cross it, checked against a cached per-file byte counter rather than a `stat` per line; `server/thread-events.ts` reads across the seam so the panel and its "showing 200 of 1,687" footer are unchanged.  A boot sweep (plus a daily unref'd one) trims what earlier runs left behind by seeking to size minus cap, cutting forward to a line boundary and renaming an fsynced temp file over the original — never reading a gigabyte log into memory — which should reclaim roughly 5.4 GB on the owner's Mac at the next harness start, and reaps temp files a killed trim left behind.  Nothing deleted, no record shapes changed, `messages.db` and yesterday's `routine-retention.ts` untouched.  Two adversarial review rounds landed on top of the first cut: round one fixed a page that could splice the rotated generation over a gap, a blank panel when the newest record was wider than the tail window, a bot delete that orphaned every non-active task's logs (pre-existing on `main`), an unreclaimed temp file after a killed trim, and an untested bounded-read claim; round two fixed the fallback blocking the rotated top-up, a possible 64 MB `JSON.parse` on the Inspector's refresh path (now a stand-in row naming the record's size), a torn last line counted in the footer, a thread-id snapshot taken before two awaits, and transcript logs left behind by both task deletes and by a task merge (all pre-existing on `main`), plus a pid check that could throw and a boot line that led with the wrong clause.  Verified at each round: both typechecks clean, 24 retention tests, 20 reader tests, 186 in `index.test.ts`, 187 across the fast batch, oxlint clean on the new files.  Targeted suites only, per the load pause.
- **2026-09-13 — CLAUDE — COMPLETED — Harness-spawn test children now die with their test: process-group kill plus a parent-death backstop (branch `claude/test-harness-orphans`, worktree `~/apps/botfleet-claude-test-orphans`).**  Six ~4.4 GB `node` harness processes were found orphaned with parent pid 1 on 2026-09-12 — a vitest worker spawned the real harness and never got to run its own `afterAll`, most likely SIGKILLed on timeout under load-average-in-the-hundreds, the same condition behind the known "server never came up" flake.  `server/testing/cleanup.ts` gains `spawnDetached` (own process group, `BOTFLEET_TEST_CHILD` / `BOTFLEET_TEST_PARENT_PID` markers, tracked for a process-`exit` guard) and a group-aware `waitForExit`; all 14 suites that spawn the real harness plus `mcp-server.test.ts` now use it, and `steer-queue.test.ts` / `steer-e2e.test.ts` had their own duplicate inline kill logic migrated onto the shared helper.  `server/test-parent-watchdog.ts` is the backstop for a parent SIGKILLed outright — gated on `BOTFLEET_TEST_CHILD`, it polls `process.ppid` and re-sends the harness its own SIGTERM the moment its recorded parent is gone, never firing for the always-on launchd harness.  Verified: `tsc -p tsconfig.server.json` clean (one pre-existing unrelated `AgentToolDeps` error on `main` untouched); new `server/test-parent-watchdog.test.ts` plus `kill-tree.test.ts` (3 tests) and one real harness-spawn suite (`mcp-server-harness.test.ts`, 5 tests) all green with zero processes left behind per `pgrep -f`.
- **2026-09-13 — CLAUDE — COMPLETED — Pair `--permission-prompt-tool` with its MCP registration, rename `ogb` to `botfleet` (Sentry BOTFLEET-8; branch `claude/claude-permission-tool`, worktree `~/apps/botfleet-claude-permission-tool`).**  A webhook-triggered routine run died with `claude exited 1 before result: Error: MCP tool mcp__ogb__approve ... not found`, leaking the operator's own global `~/.claude.json` servers (`github`, ...) into the CLI's "Available MCP tools" instead of the harness's permission proxy; the code that caused it (pre-`--strict-mcp-config`, removed the day after this event by #314) manually merged `~/.claude.json` into every turn without isolation, while the flag and the proxy's `mcpServers` registration were only coincidentally paired — two independent statements in the same `if` block a future edit could split without either test catching it.  `server/drivers/claude.ts` now derives `--permission-prompt-tool` from `mcpServers.botfleet` itself (`if (mcpServers.botfleet) { args.push(...) }` instead of re-testing `permissionMode` a second time), renames the OpenMausBot-era `ogb` permission-proxy server/tool to `botfleet` (`mcp__botfleet__approve`) everywhere except external contracts (`/opt/ogb/...` remote CUA paths, `ogb-pool-N` machine names) and incidental example strings in unrelated tests, and adds a driver test asserting the flag and registration always travel together in both `acceptEdits` and `bypassPermissions`.  Verified: `tsc -p tsconfig.server.json` clean (one pre-existing, unrelated error also on `main`); `vitest run server/drivers/claude.test.ts` 66 passed/1 skipped (clean on two re-runs after one Mac-load flake on an unrelated test); a targeted `server/index.test.ts` run of the one test touching the renamed key.
- **2026-09-13 — CLAUDE — COMPLETED (PR #374 merged) — Unblock main: the room-turn tool host was missing four of the seven agents deps (branch `claude/room-turn-agent-deps`, worktree `~/apps/botfleet-claude-room-turn-deps`).**  MM PR 8/11 (a4f8d960, #365) added the room-turn `createTurnToolHost` call against a base that predated MM PR 7/11 (#366), so it passes only `executeListAgentsRequest`, `executeAskBotRequest` and `executeListRoutinesRequest` while `AgentToolDeps` now requires seven.  `pnpm typecheck` has failed on every PR since that merge, including on `main` itself at a4f8d960.  The four missing bodies are added at the room-turn call site, matching the 1:1 site exactly, so the same bot gets the same toolset in a room as in a direct message.
- **2026-09-13 — CLAUDE — COMPLETED (PR #371 merged) — Mac updater keeps rollback bundles out of /Applications, prunes older generations, and refuses to reopen over a survivor (branch `claude/updater-rollback-placement`, worktree `~/apps/botfleet-claude-updater-rollback`).**  The prior bundle now moves into the update's own private stage at `<stage>/rollback/BotFleet.app` instead of a hidden `.BotFleet.rollback-….app` beside the installed app, because a dot-bundle in `/Applications` is still indexed by Spotlight and Launchpad and, since macOS binds a running process to the bundle it launched from, any process that outlives the swap shows the rollback name in the Dock until it is relaunched — which is what the owner saw after the Sep 12 install.  A volume comparison keeps the rename atomic and falls back to the adjacent hidden name only across volumes, recording `rollbackPlacement`, `crossVolume`, and the reason in a receipt that keeps every key of the original schema and adds `status`, so a verified install can keep exactly one rollback generation per app path and delete the older ones while never touching one still marked installing or one with no receipt at all.  Quiescence now also requires that no process runs from the installed bundle, and the transaction refuses to reopen the app while any process survives in the rollback bundle, since `open` activates an existing instance of the bundle identifier rather than launching the replacement.
- **2026-09-13 — CLAUDE — COMPLETED — Remove stray tracked root scripts and the OpenMausBot mascot preview page (board `a1a56bdb`; branch `claude/root-artifact-cleanup-2`, worktree `~/apps/botfleet-claude-root-cleanup`).**  Eleven root files committed by accident in #117, #125 and #129 plus `mascot-preview.html` and `src/mascot-preview.{tsx,css}`, none referenced anywhere; the avatar library stays.
- **2026-09-13 — CLAUDE — COMPLETED — Text-only boot error page with a Try Again relaunch link (board `f95739cb`; follow-up to #359 / #360; branch `claude/boot-page-text-retry`, worktree `~/apps/botfleet-claude-boot-page-text`).**  Owner asked for no logo on the "Couldn't Start the Bot Server" page.  The inline icon is gone, the three diagnoses and the log link stay, and Try Again is a `botfleet://retry-server` link the window-open handler turns into `app.relaunch()` + `app.quit()`, so a fresh process re-probes the harness with no preload or IPC added.
- **2026-09-13 — CLAUDE — COMPLETED (PR #387 merged) — MiniMax connectability: vault and config.json key sourcing, encrypted store, second instance from the app (branch `claude/minimax-connectability`, worktree `~/apps/botfleet-claude-minimax-connectability`).**  MiniMax read its key only from process env or `~/.mmx/config.json`, so neither Infisical nor `~/.botfleet/config.json` could supply it and the Secrets card could not say where the key in use came from; `minimax.key` and `minimax.url` are now mapped fields, injected per instance the same way `openaiCompat.*` are, and a key that lives only in `~/.mmx/config.json` is named on the card instead of reading as "Not set".  `POST /api/instances` no longer hardcodes `openai-compat`: it accepts any registered driver declaring `supportsMultipleInstances`, so the owner can add a second MiniMax connection with its own key and endpoint from Settings → Engines and pick it per bot — the workspace key still reaches only the reserved instance, and the desktop encrypted store now covers both drivers.
- **2026-09-13 — AG — IN PR #370 — Antigravity local computer mount, "Steer Now" queued action, and Antigravity stdin execution (branch `antigravity/antigravity-local-computer-and-steer`).**  Enable `localComputerMcp: true` for Antigravity driver adapter to resolve local computer mount rejection error; add "Steer Now" button to queued message indicators in ChatView, Composer, and iOS ChatView to interrupt running turns and send queued messages immediately (plus Alt/Option+Send shortcut); eliminate arbitrary 256KB argv cutoff in `antigravity.ts` by supporting piped stdin prompt delivery for `agy`.
- **2026-09-13 — AG — COMPLETED / DEPLOYED (PR #375 merged) — Fix update tool ENOENT, Gemini 3.8 Flash false quota cooldown, and Composio 401 tool error across all bots (issues #285, #351, #270; boards 261a10ee, a29d9b12, 88badbdc; branch `ag/updater-quota-composio-fixes`, worktree `~/apps/botfleet-ag-fixes`, PR #442).**  Synthesize `app-update.yml` in afterPack and electron/updater fallback for local builds, add `electron-updater.cjs` to `update-botfleet.sh` GENERATED; gate quota cooldown on actual provider failure rather than conversational reply prose, enforce default cooldown TTL, clear Antigravity cooldowns when `antigravity-usage` reports available quota; clean up stale `botfleet-*` MCP servers on startup and return clean empty tool list on `tools/list` authorization failure.  Landed in PR #375 (commit `4e345975`) and deployed to `/Applications/BotFleet.app` and LaunchAgent `com.jay.botfleet-server` via `update-botfleet-mac.mjs`.
- **2026-09-13 — CLAUDE — COMPLETED (PR #381 merged) — iOS: Mac Update card with remote install, and a newer-TestFlight-build notice (branch `claude/remote-update-ios`, worktree `~/apps/botfleet-claude-remote-update-ios`).**  `CompanionCore` gains the `/api/update/*` contract (`MacUpdateStatus`, an `update.status` stream frame, and `updateStatus`/`checkForUpdates`/`runUpdate` client methods) ahead of the sibling lane's harness routes — the frame decode tries a `status`-wrapped payload first and falls back to flat fields, so either shape works without a follow-up change.  Settings gets a Mac Update section (installed build, availability with commits ahead, live progress, last outcome, Check/Install gated on `capabilities`) and the app root gets a dismissible "newer TestFlight build" banner compared by build number against the fleet's published `ios-versions.json`.  23 new CompanionCore tests, full suite 286/286 green; app built for the iOS Simulator via `xcodegen` + `xcodebuild`.
- **2026-09-13 — CLAUDE — COMPLETED (PR #382 merged) — Check and install Mac updates from the app and the phone: harness routes, detached launcher, desktop UI (branch `claude/remote-update`, worktree `~/apps/botfleet-claude-remote-update`).**  Three harness routes in a new `server/update-control.ts` answer whether `~/apps/botfleet-server` is behind `origin/main` and start the transactional updater; the paired phone reaches all three through the sidecar allowlist, and status pushes ride the event stream as `update.status` frames.  The updater stops the harness and quits the app, so it can be a child of neither — it is submitted as the one-shot launchd job `com.jay.botfleet-update` and reports through a progress file `scripts/update-progress.mjs` writes by instrumenting the transaction's own operations adapter, which is also how a run survives the harness restart it performs.  Settings and the floating banner use this path whenever the harness says this Mac can install and fall back to electron-updater otherwise; 110 tests across four suites, 186 in the existing harness suite, 49 updater node tests, both typechecks clean, and no update was run on this Mac.
- **2026-09-13 — CLAUDE — COMPLETED (PR #388 merged) — Quota parity: MiniMax balance and cap status, window mapping for six engines, dual-window labels for every engine (branch `claude/quota-parity`, worktree `~/apps/botfleet-claude-quota-parity`).**  `server/minimax-balance.ts` calls two real-but-undocumented MiniMax endpoints recovered from MiniMax's own officially-published `mmx-cli` npm bundle (the public docs expose none), wired beside DeepSeek's client in `/api/quotas` so the MiniMax row always shows real quota/balance data once configured, not only when capped or spent.  `quota-window-map.ts` maps droid onto its real Factory subscription window and gives pi/qwen/hermes/opencodeGo/boxAgent an explicit metered note instead of a hidden or generic row; `registry.ts`'s "5hr/Week" dual-window badge is generalized off one per-model dict rather than gated to Antigravity, proven against MiniMax's own Token Plan quota as a second engine.  Reads MiniMax's key via the same `loadLocalMiniMaxConfig`/`resolveMinimaxCredentials` helpers PR #387 (MiniMax connectability, also CLAUDE) is extending — no conflict expected, just worth the coordinator's eye if #387 lands first.  104 new/updated tests across five files, both typechecks and oxlint clean; not armed for auto-merge pending coordinator review.
- **2026-09-13 — CLAUDE — COMPLETED (PR #383 merged) — Push hardening: time-sensitive kinds, retries, cached provider token, sender health, lock-screen Approve/Deny (branch `claude/push-hardening`, worktree `~/apps/botfleet-claude-push-hardening`).**  The harness notify `kind` never reached the phone: the sidecar parsed a notify frame, dropped `notification.kind`, and sent every push as an undifferentiated alert with no `interruption-level`, no `relevance-score` and no category — so an approval a bot was blocked on arrived exactly as loudly as a routine that finished, and no lock-screen action could be attached to one.  `kind` now travels end to end (approval/question land time-sensitive in `BOTFLEET_APPROVAL`, the rest `active` in `BOTFLEET_UPDATE`, approvals ranked highest), the sender retries a 429 for the `Retry-After` Apple asked for and a 5xx with bounded exponential backoff while 410 still clears the token and 400/403 log once per device with Apple's `reason`, the provider JWT is signed once per twenty minutes instead of once per push and re-signed immediately on `ExpiredProviderToken`, a missing `.p8` is re-checked every five minutes instead of disabling the watcher forever, and the sender's health is exposed on the control `/state` and a new authenticated `GET /api/companion/push-health` — counts, timestamps and Apple's own status strings only.  On the phone, `BOTFLEET_APPROVAL` and `BOTFLEET_UPDATE` are finally registered as `UNNotificationCategory` objects with Approve, Deny and Open actions routed to the same answer path `AnswerApprovalIntent` uses, so an approval can be settled from the lock screen with the app closed.
- **2026-09-12 — CLAUDE — COMPLETED — Desktop boot error page names an unresponsive harness, drops the OpenMausBot mouse, and bounds routine prompt snapshots (issue #360; PR #359; merge `2813b2e1`; branch `claude/boot-error-page`, worktree `~/apps/botfleet-claude-boot-page`; THE BOARD was unreachable at 127.0.0.1:8792, so the issue is the second claim leg).**  Root cause of the owner's "Couldn't start the bot server" page: the always-on harness was alive but its event loop stalled on synchronous saves of a 35.7 MB `routines.json` (33.5 MB of per-run prompt snapshots, 1,810 of 2,000 runs webhook-triggered) under 14 GB of swap, probes saw connection resets, and the desktop correctly refused to spawn a second harness on the same data but said nothing.  PR: `resolvePackagedServer` reports and logs `unresponsiveOwner` (reset per attempt), `startServerPackaged` logs the outcome, the page names the process and port, follows the system theme with a Title Case heading and the BotFleet icon, and `boundStalePromptSnapshots` keeps full snapshots on the newest 100 settled runs and a bounded copy (event block 500 chars, whole snapshot 4,000) on older ones — webhook and resource runs have no routine to fall back to, so nothing is dropped.  35.7 MB → 7.2 MB on the Mac.  Ops: routines.json compacted then restored in bounded form (backups `routines.json.bak-prompt-bloat-20260912-203847`, `.bak-before-restore-20260912-214020`), six orphaned idle 4.4 GB `node` processes killed, `~/apps/botfleet-server` dependencies restored after the disk janitor reaped them (ai-fleet-coordinator #206, merged, protects the checkout), harness restarted, app reattached.  Tests: 33 node + 48 vitest pass, both typechecks clean; adversarial review (21 agents) found and fixed two defects in the first cut.
- **2026-09-12 — CLAUDE — COMPLETED — MiniMax first-class: finish the 11-PR series (PR 10 now, PRs 4-8 and 11 after Codex's PR 3 / #355) (board `4564fe5e`, branch `claude/mm-10-typed-errors-snapshot-ladder`, worktree `~/apps/botfleet-claude-mm-10`).**  Owner ask Sat, Sep 12: fully integrate MiniMax and make every model a first-class option.  PR 3 (board `99b43d34`) and DSH (board `2784c3c7`) handed to CODEX per #agent-sync.  THE BOARD was starved at claim time (load 857); board rows follow when it answers.
- **2026-09-12 — CLAUDE — COMPLETED (PR #366 merged) — MM PR 7/11: the six missing agents tools on both lanes, per-turn create cap in the host closure, shared schedule normalisation (board `4564fe5e`, branch `claude/mm-07-six-agents-tools`, worktree `~/apps/botfleet-claude-mm-07`).**  `server/tools/registry.ts` gains `delegate_bot`, `create_bot`, `request_credential`, `propose_routine` and `propose_routine_action` as registry entries (`list_routines` already landed in PR 4/11), so a MiniMax bot now gets the same eight harness tools an ACP bot gets from `agents-proxy.ts`, rendered from one set of definitions.  Each endpoint body — `delegate-bot`, `create-bot`, `request-credential`, `routine-requests` — is factored into an exported `executeXRequest` function in `server/index.ts` the way `executeAskBotRequest` already was, so one implementation serves both lanes; `agents-proxy.ts` keeps relaying execution over its loopback + `COMMS_TOKEN` hop (it is a bare child process, so it cannot call the new functions in-process) and only its wire schema/description now come from the registry — `UNMIGRATED_TOOLS` is gone.  `delegate_bot` carries an `approval` ask (target plus the first 120 chars of the message), the second registry tool on the broker PR 6 built; `request_credential`/`propose_routine`/`propose_routine_action` settle `"suspend"`, the first real `settles: "suspend"` registry tools now that PR 6's suspend semantics exist.  `create_bot`'s per-turn cap of 4 moves out of `agents-proxy.ts`'s proxy-process module state into a closure inside `agents.ts#createAgentTools` (built fresh once per `createTurnToolHost` call, i.e. once per HTTP turn), so it is finally enforced on the HTTP lane; the MCP lane keeps its own process-scoped counter unchanged, since a bare child process cannot share a JS closure with the harness.  Schedule normalisation (once/weekly/daily, weekday coercion, explicit rejection of interval/cron/hourly) moves out of `agents-proxy.ts` into a new pure `server/tools/schedule.ts` so both lanes normalise identically; `registry.ts` keeps its own copy of the wire schema and the credential-id allowlist (it must stay import-free — `registry.test.ts` asserts zero `import` statements), with a drift test pinning both copies to the real source of truth.  Prompt fragments for all six tools land on the registry records so PR 5's `promptFragmentsFor` and the existing `credentialPromptFor`/`routinePromptFor` pick them up automatically; `LEGACY_MCP_AGENT_TOOLS` is now an empty (not deleted) list.  No CLI-lane behaviour change: `agents-proxy.test.ts` passes unmodified.  Verified: `pnpm typecheck` clean; 520 targeted tests green, including new `server/tools/schedule.test.ts`, new create_bot-cap/delegate_bot/request_credential/propose_routine suites in `agents.test.ts` and `host.test.ts`, and two new `http-lane-e2e.test.ts` rows proving `delegate_bot`'s approval card end to end (denying it leaves nothing in `/api/team-map`'s `queued` list) and `request_credential`'s suspend-then-resume round trip (one round before the card, two after `dismiss` re-dispatches).  Base-branch note: MM PR 5/11 (#362) and MM PR 6/11 (#363) both merged to `main` while this branch was in flight, so `claude/mm-06-approvals-http-lane` no longer exists — this branch was rebased onto current `main` (which already contains both) and PR #366 opens directly against `main`; no stack remains to retarget.  Local: typecheck + targeted vitest files only, full suite on hosted CI, per the Sep 12 load pause (1-minute load ~4.5 at verification time).
- **2026-09-12 — CLAUDE — COMPLETED (PR #363 merged) — MM PR 6/11: approval broker, interrupt and suspend on the HTTP lane (board `4564fe5e`, branch `claude/mm-06-approvals-http-lane`, worktree `~/apps/botfleet-claude-mm-06`).**  Nothing on the HTTP lane had ever asked for permission: a CLI engine asks over its own protocol, and auto-approve, always-allow, the destructive and sensitive guards, the unattended block, auto-review, the decision log, the notification and the watchdog's waiting-on-human exemption all hang off `request.opened` / `request.resolved`, which a chat-completions driver never emitted — so a MiniMax bot ran whatever the model called.  New `server/tools/approvals.ts` publishes a REAL `request.opened` on the same bus and awaits the answer, so every one of those behaviours reaches an HTTP bot for the first time, reached rather than reimplemented.  Resolution gets exactly ONE indirection: `deliverDecision` tries the broker and falls through to `instance.adapter.respondToRequest` when `respond()` returns null — which it does for every CLI requestId — replacing the three call sites in `answerRequest`, `reviewPermissionCard` and the auto-verdict branch of the fold, so the CLI path is byte-for-byte the path it always took and the HTTP drivers' `respondToRequest -> 'unavailable'` becomes correct rather than a gap.  Every pending ask settles exactly once as `unavailable` (a deny) from each of the three ways a turn dies under an open card: the tool call's own signal plus `closeOpenApprovals` (Stop), the `turn.completed` subscriber (teardown), and `latchInterruptedTurns` before `registry.disposeAll` (dispose).  The host asks BEFORE a write tool runs, driven by the registry record's `approval` field (`ask_bot` only), and a deny comes back as a refusal string the model reads; the loop pauses its per-tool clock under a card and resumes with the time it had left, while the whole-turn wall clock keeps running.  Owner-approved behaviour change: existing MiniMax bots now see approval cards — or silent auto-approvals — for write tools, governed by each bot's EXISTING auto-approve setting, with no new per-tool switch.  Deliberately untouched for Codex's #355: `server/tool-executor.ts`, the `isHttpDriver` dispatch fork, every `isToolCallsStopReason` site, `openai-compat.ts`, `grok.ts` and `turn-tools.ts`.  Verified: `pnpm typecheck` clean; 1,044 targeted tests green, including 16 new broker rows, 5 new host rows, 4 new loop rows and a new `server/http-lane-e2e.test.ts` that spawns the real harness against the fake OpenAI server and proves the deny card, the auto-mode approval and the unattended block end to end — with `decision-log-wiring.test.ts`, `unattended.test.ts`, `auto-approve.test.ts`, `index.test.ts`, `tool-executor.test.ts` and `notification-wiring.test.ts` all green UNMODIFIED, which is the fall-through's acceptance criterion.  Opened stacked on #358 and needs retargeting to main once that merges; auto-merge deliberately not armed.  Local: typecheck + targeted vitest files only, full suite on hosted CI, per the Sep 12 load pause.
- **2026-09-12 — CLAUDE — COMPLETED (PR #365 merged) — MM PR 8/11: room turns on the HTTP lane with the same catalog and host as 1:1 turns (board `4564fe5e`, branch `claude/mm-08-room-turns`, worktree `~/apps/botfleet-claude-mm-08`).**  `runGroupMemberTurn` stopped calling `instance.adapter.sendTurn` bare: it now builds `buildTurnTools(integrations)` and `createTurnToolHost(...)` from the same two calls `startTurn` uses, passing `commsDepth: hop` — the room's own hop, and the same number `agentsIntegration(bot.id, threadId, hop)` already hands the MCP proxy — with the same three injected endpoint bodies (`executeListAgentsRequest`, `executeAskBotRequest`, `executeListRoutinesRequest`) and `requestApproval` bound to `permissionBroker.request` on the ROOM thread, so a card names the member that asked and lands where the waiter can see it.  A room was the one place a driver-loop bot was handed a prompt naming `list_bots` and `ask_bot` with no executor behind either — PR 5 made that prompt honest on the 1:1 path and left it dishonest here.  The waiter needed nothing: inner rounds stopped being `turn.completed` when PR 6 moved the loop into the driver, so `RoomTurnDeadline`, the `roomStallCompletions` registration, the `dispatch_failed` recovery and the fallback speaker cleanup are byte-for-byte unchanged, and the deadline's existing `request.opened` / `request.resolved` handling finally fires for an HTTP bot because the broker now publishes real cards.  A CLI engine is untouched: it receives `tools` here exactly as it already does on the 1:1 path and never gets a host, because `toolHost` is gated on `capabilities.toolLoop === true`.  `server/tools/host.ts` needed no change at all — its `commsDepth` and identity fields already carried everything a room turn needs, and leaving it alone keeps the rebase against MM PR 7 trivial.  New `server/rooms-http-lane.test.ts` spawns the real harness against `server/testing/fake-openai-server.ts` and proves all six acceptance points: the catalog reaches the room dispatch and `list_bots` runs with the roster fed back into round two; a three-round room turn does not settle early; the speaker is claimed once and released once, counted off the harness's own `/api/events` group frames rather than inferred from a final state a double release would look identical in; a rejected dispatch (a keyless MiniMax instance, so `sendTurn` throws before a provider round exists) leaves the room usable and the next member still runs; a provider that accepts the round and never answers is interrupted by the room deadline, producing exactly one terminal event and one timeout stamp; and an approval card opened inside a room turn holds the deadline through more than the whole room budget, then resumes and settles normally.  `server/testing/fake-openai-server.ts` gained one additive `{ kind: "hang" }` script for the timeout row, with `close()` destroying any socket still held that way.  Deliberately untouched for CODEX's #355: `server/tool-executor.ts`, the `isHttpDriver` fork, every `isToolCallsStopReason` site, `server/turn-tools.ts` and the SSE bodies of `openai-compat.ts` / `grok.ts`; and for MM PR 7: the `/api/internal/` handlers, `server/tools/registry.ts`, `server/tools/agents.ts`, `server/tools/host.ts` and `server/drivers/agents-proxy.ts`.  Verified: `pnpm typecheck` clean; 361 targeted tests green (6 new room rows, 174 across `server/tools/**`, `http-lane-e2e`, `drivers/chat-completions/**`, `room-turn-timeout` and `turn-tools`, plus all 181 in `index.test.ts`, every one of those suites unmodified); `oxlint` reports zero findings on the new `index.ts` lines and only the patterns the sibling e2e file already carries on `main`.  Cost note: the two deadline rows spend about 130s of wall clock because the room-timeout schema floor is one minute.  Local: typecheck + targeted vitest files only, full suite on hosted CI, per the Sep 12 load pause.
- **2026-09-13 — CLAUDE — COMPLETED (PR #385 merged) — Hotfix: room turns hand the tool catalog only to driver-loop engines (regression from #365 for Grok and openai-compat room members) (branch `claude/mm-rooms-tools-gate`, worktree `~/apps/botfleet-claude-mm-rooms-hotfix`).**  MM PR 8/11 (#365) built the room-turn tool catalog unconditionally, so a room member on an HTTP driver without `capabilities.toolLoop` (Grok's `grok.ts`, or an `openai-compat.ts` instance) still received the full list_bots/ask_bot catalog whenever `integrations.agents` was set — both declare `agentsMcp: true` — with no host able to execute either tool; such a driver builds its own OpenAI-shape tools from `turn.tools`, and a tool call then settles the turn on a partial reply that the room waiter reads as done, silently dropping whatever the bot meant to say next.  Before #365, room turns passed no tools at all, so this is a regression scoped to Grok and openai-compat room members — MiniMax is unaffected, since it is the one driver declaring `toolLoop: true` today.  The fix gates `roomTurnTools` on `instance.adapter.capabilities.toolLoop === true` in `runGroupMemberTurn`, mirroring the existing gate on `roomToolHost` right below it; nothing else in the function or the waiter changed.  New row in `server/rooms-http-lane.test.ts` adds an `openai-compat-no-loop` fixture (same `agentsMcp: true` shape as the existing `minimax` instance, but no `toolLoop`) and proves the request the harness sends carries no `tools` key at all, and the room still settles once with the reply.  Deliberately untouched, since CODEX's MM PR 3 (#355) lands separately: `server/tool-executor.ts`, the `isHttpDriver` fork, every `isToolCallsStopReason` site, `server/turn-tools.ts`, `openai-compat.ts` and `grok.ts`.  Verified: `pnpm typecheck` clean; 299/300 vitest files green (3878/3897 tests, 19 pre-existing skips), composio-broker 9/9, and every `node --test` script green except one unrelated pre-existing failure in `scripts/release-workflow.node-test.mjs` (`Cannot find module 'app-builder-lib/out/targets/blockmap/blockmap.js'`, a dependency-store gap confirmed on this Mac's shared node_modules, not caused by this change); targeted `pnpm exec vitest run server/rooms-http-lane.test.ts server/index.test.ts server/tools` — 321/321.  Found by the botfleet-02 CLAUDE session's provider-parity audit.
- **2026-09-13 — CLAUDE — COMPLETED (PR #386 merged) — Chat-completions lane: bounded abort-aware retry with backoff for transient HTTP and network failures (branch `claude/mm-chat-completions-retry`, worktree `~/apps/botfleet-claude-mm-retry`).**  There was no retry or backoff anywhere under `server/drivers/chat-completions/`, so a single 502 or a reset socket failed a MiniMax turn outright where Claude and Codex ride the same hiccup out silently through `server/drivers/retry.ts` — found by the botfleet-02 CLAUDE session's provider-parity audit (G5), severity P1.  `loop.ts` now wraps each round's request in a bounded attempt loop, so every driver on the loop inherits it rather than MiniMax alone: at most three attempts, backoff from the fleet's one existing schedule in `drivers/retry.ts` (1s then 3s, ±25% jitter) instead of a second private policy, and the classification split so `errors.ts` says what a status is worth and `loop.ts` decides what to do about it.  Retried: 408, 500/502/503/504, and network failures through the same `classifyError` text classifier every CLI driver already uses; a 429 gets the full schedule when it named a `Retry-After` (honoured, capped at 30s) and exactly one polite retry when it did not, so a real quota wall still reaches the fallback ladder as `quota_or_region_restriction` instead of disappearing into a backoff nobody's balance outlasts; 400/401/403/404/413/422 and the permanent 5xx (501, 505) are never retried, and classification after the last failed attempt is byte-for-byte what it was.  Two rules keep it honest: a round that already PUBLISHED anything — a text delta, a reasoning delta, a tool call announced off the stream, reported by the driver through the new `onPublished` seam on `runRound` — is never retried, because replaying it would show a person text they have already read; and an attempt that reported usage is settled and billed rather than silently redone, which is what makes "a failed attempt's usage is never summed" true by construction instead of by throwing real tokens away.  An attempt also never starts without 20s of headroom on BOTH the round's own 180s ceiling — shared across every attempt at that round, not re-armed per try, so two retries cannot quietly turn 180s into nine minutes — and the turn's 900s wall clock, and the backoff sleep is abort-aware on the round's combined signal, so Stop, a sweep or the round deadline during a wait settles at once.  The single-terminal-event invariant does not move: the attempt loop sits INSIDE one round, every exit still falls through the same one `finally`, the closed `TurnLoopExit` union is unchanged, and a retried turn is still exactly one `turn.completed`.  Each retry emits one `turn.retrying` — the same event every CLI driver already emits — so the routine receipt stays quiet and keeps the run running, the Sentry span takes a warning breadcrumb, and `index.ts` renders its standard "retrying — attempt N/3" activity chip; `turn.completed` has no metadata bag to carry a `retries` count, so the count stays where every other engine already reports it.  `minimax.ts` changed only at the request function's seam (`res.headers` into `httpErrorFor`, `onPublished` from the two stream callbacks), and `openai-compat.ts`, `grok.ts`, `tool-executor.ts`, `turn-tools.ts` and the `isHttpDriver` / `isToolCallsStopReason` sites were left alone for CODEX's #355.  Three review findings on the PR were real and fixed in 0d88cc82: a parsed `Retry-After` is now propagated on EVERY retryable branch (a 503 or a 408 that names a cool-down was having it dropped and falling back to the ~1s schedule, retrying into the same outage); a 429 that reaches the loop as bare text — a driver still throwing `new Error("HTTP 429")` — gets the same one polite retry the parsed one does, through a shared `UNHINTED_RATE_LIMIT_ATTEMPTS`, so the policy cannot depend on which driver threw it; and `turn.retrying` gained an optional `maxAttempts` so the activity chip names THIS failure's ceiling instead of always printing `RETRY_MAX_ATTEMPTS` and reading "attempt 2/3" right before a no-cool-down 429 gave up after attempt 2.  Verified: `pnpm typecheck` clean; 361 targeted tests green across `chat-completions/**`, `minimax.test.ts`, `http-lane-e2e.test.ts`, `thread-events.test.ts` and `index.test.ts` with 34 rows new (20 policy rows in `loop.test.ts`, 13 in `errors.test.ts`, 2 end-to-end in `minimax.test.ts` against the real `fake-openai-server.ts` socket, plus the existing 502 row extended to prove a persistent outage still spends exactly three requests and settles once); the whole local vitest suite green at 3906 passed / 19 skipped over 300 files, and every `pnpm test` script after it green except one row in `test:release-workflow` that cannot resolve `electron-builder` because this lane's `node_modules` had to be cloned from a lockfile-identical sibling (the registry was unreachable from the sandbox) — CI settles it, with `typecheck + test` green on ubuntu, macOS and Windows; `oxlint` reports no finding on the touched source files that the branch did not already carry on `main`.  Vercel is red repo-wide on a deployment rate limit, unrelated.  Review round: Codex's five threads settled on the branch — `Retry-After` now propagates on every retryable status and `turn.retrying` carries THIS failure's own `maxAttempts` so the chip never promises an attempt that is not coming (both in `678a1f23`), HTTP 529 joins the retryable server set so the plan's own simulated-529 criterion actually holds, Node's `TypeError: terminated` / `UND_ERR_SOCKET` premature close is recognised by shape in `errors.ts`'s `isPrematureCloseError` so a 200 SSE that dies before its first delta is retried instead of read as terminal, and the bare-text 429 seam already spends the same one polite retry a parsed no-`Retry-After` 429 gets so no future driver on the loop can earn a more generous policy by throwing a plain `Error`.
- **2026-09-13 — CLAUDE — COMPLETED (PR #384 merged) — Honest MiniMax callout copy: up to seven tools, create_bot qualified by Chief-of-Staff and direct-chat-only, none at the recursion ceiling, correct sentence-gap markers, no DSH recommendation (branch `claude/mm-callout-copy`, worktree `~/apps/botfleet-claude-mm-copy`).**  The EnginesSettings row and ModelPicker rail callouts for `driverKind: minimax` still said "two tools today — list_bots and ask_bot" and pointed bots needing more tools at "an ACP engine (Claude, Codex, DSH, Droid)" — both false since MM PRs 5-8 (#362, #363, #366, #365) landed the full HTTP tool loop.  Five rounds with chatgpt-codex-connector's automated PR review caught four more defects in my own rewrites before merge: (1) `create_bot` named as one of eight tools every bot gets, when `server/tools/registry.ts` gates it on `chiefOfStaff` (`CREATE_BOT.gate = chiefOnly`) so an ordinary bot gets seven; (2) every write called "gated by the same approval cards as every other engine", when `create_bot` carries no `approval` record and `server/tools/host.ts` only asks when one exists, so it runs with no card at all; (3) even the seven-tool baseline called unconditional, when `server/index.ts` only grants `integrations.agents` (and so any of these seven) when `commsDepth < MAX_COMMS_DEPTH` (== 1) — "A comms-invoked turn (depth >= cap) gets none — hard recursion stop" — so a peer-invoked turn gets none of them; (4) `create_bot` claimed available to any Chief of Staff, when the room-turn path (`runGroupMemberTurn` in `server/index.ts`) never threads `chiefOfStaff` through to `buildTurnTools`/`createTurnToolHost`, and `buildTurnTools` (`server/turn-tools.ts`) defaults it to `false` when no `gate` argument is given, so a room turn never grants it regardless of role; and (5), P1 this time — a real sentence-gap bug, not a factual one: the two new sentence boundaries I added to ModelPicker.tsx used literal `.  ` (two ASCII spaces typed directly in JSX text) instead of the file's own `{"\u00a0 "}` (NBSP + space) convention, so HTML's whitespace collapse would have rendered them as one space, violating AGENTS.md's two-space sentence-gap rule — fixed by switching both to the same marker every other sentence break in the file uses, then verified with a small render-simulation script that every period-into-capital-letter boundary in both boxes is preceded by the marker.  Final copy: up to seven tools in a turn the bot starts itself — `list_bots`, `ask_bot`, `list_routines`, `delegate_bot`, `request_credential`, `propose_routine`, `propose_routine_action` — and none of them in a peer-invoked turn; a section's Chief of Staff also gets `create_bot`, but only in a direct chat, since rooms don't grant it yet; `ask_bot`/`delegate_bot` go through the same approval flow as any other engine, `request_credential`/the routine tools show their own confirmation card, `create_bot` runs immediately with no card.  It still has no file, shell, web, Composio, computer-use, or image tools because those come from a vendor CLI the harness never spawns for an HTTP driver, so the redirect names engines that actually carry them — Claude, Codex, Antigravity, Cursor — dropping DSH, which mounts no MCP servers (`server/drivers/acp/dsh.ts`).  `src/components/MiniMaxCallout.test.ts` pins the copy claims (seven top-level tool names, the Chief-of-Staff and direct-chat-only qualifiers, the recursion-ceiling qualifier, the approval breakdown, no "two tools" / DSH strings — 14 assertions) and now also carries a permanent `expectNoCollapsingSentenceGaps` guard per box (2 more) that walks every period/semicolon-into-capital-letter boundary in the raw source and fails if it is not preceded by the `{"  "}` marker, so this class of regression cannot land silently again.  `pnpm exec tsc -b` and `pnpm exec tsc -p tsconfig.server.json` clean, `pnpm exec vitest run src/components` 100/100 green across 13 files, `pnpm exec oxlint` clean on both touched files and the test file (one unrelated pre-existing finding at EnginesSettings.tsx:465, a `typeof` check in the icon-upload handler).  Overlaps with AG's open #354, which drops only DSH from these same two lines; this PR's copy already satisfies that intent, so whichever lands second rebases trivially.
- **2026-09-12 — CLAUDE — COMPLETED (PR #362 merged) — MM PR 5/11: prompts derived from the catalog, memory and skills prompts suppressed where they cannot act, transcript sent once (board `4564fe5e`, branch `claude/mm-05-prompts-from-catalog`, worktree `~/apps/botfleet-claude-mm-05`).**  `server/tools/prompts.ts` adds `promptFragmentsFor(tools)` (reads each tool's own `promptFragment`, so a tool absent from the catalog contributes nothing) and `availableAgentToolNames` / `credentialPromptFor` / `routinePromptFor`, which gate the `request_credential` and `propose_routine` sentences on whether THIS turn's tool set actually has them, replacing the single `integrations.agents` boolean that fired the same sentences for every agents-capable driver regardless of what it could call.  `chief-of-staff.ts`'s `chiefOfStaffSystemPrompt` now takes the available tool-name set instead of a `canDelegate: boolean`, so a Chief bot is told it can `ask_bot` without also being told it can `create_bot`/`delegate_bot` when those are not in its catalog.  Both the 1:1 dispatch (`startTurn`) and the room dispatch (`runGroupMemberTurn`) compute the same set and call the same helpers, so the room's duplicate copy of these sentences is fixed by the same fix.  `hasFileTools(worksInWorkspace, toolLoopDriver)` replaces the `driverKind !== "grok" && driverKind !== "boxAgent"` gate on `memorySystemPrompt`/`skillsSystemPrompt` in both paths — a toolLoop driver keeps its workspace for `cwd`/checkpoint bookkeeping but stops being handed `MEMORY.md` verbatim and skill-file instructions for file tools it does not have.  `ProviderAdapter.capabilities` gains `replaysTranscript` (`server/contracts.ts`), set `true` on minimax, openai-compat and grok — confirmed by grep that all three already `flatMap` `turn.transcript` into real messages — so `index.ts`'s `buildTurnContext` call derives `replaysNatively` from that capability instead of `driverKind === "grok"`, stopping a fresh or rewound chat-completions turn from inlining its whole history on top of the structured transcript it already expands.  Signal choice: both the "has file tools" and "gets the five MCP-only legacy agents tools" gates read `capabilities.toolLoop === true`, which is exactly right for minimax today and self-corrects for openai-compat and grok with zero further changes once Codex's #355 gives them `toolLoop` — until then, openai-compat carries the SAME pre-existing over-promise it has on `main`, not a regression.  Deliberately untouched for #355: `tool-executor.ts`, the `isHttpDriver` local, all `isToolCallsStopReason` sites, `turn-tools.ts`; `contracts.ts`/`openai-compat.ts`/`grok.ts` each got only the one `replaysTranscript` addition.  Verified: `pnpm typecheck` clean; 174 targeted tests green (147 across `prompts.test.ts` (new), `registry.test.ts`, `agents.test.ts`, `host.test.ts`, `chief-of-staff.test.ts`, `turn-context.test.ts`, `minimax.test.ts`, `agents-proxy.test.ts`, `turn-tools.test.ts`, `tool-executor.test.ts`, plus 27 more across `openai-compat.test.ts` and `grok.test.ts`); `oxlint` on every touched file showed zero new findings.  Stacked on #358 (still open) with base `claude/mm-04-tool-registry`; auto-merge intentionally NOT armed pending retarget to `main`.  Local: typecheck + targeted vitest files only, full suite on hosted CI, per the Sep 12 load pause (1-minute load 39 at verification time).
- **2026-09-12 — CLAUDE — COMPLETED (PR #361 merged) — MiniMax onboarding honesty: API-key setup path, no phantom sign-in (branch `claude/models-first-class`, worktree `~/apps/botfleet-claude-models`).**  Onboarding, NoEngines, and the ModelPicker Cloud pane all called MiniMax's setup card "Install MiniMax" and offered an "Open install in Terminal" button that would paste API-key guidance prose as a shell command — dishonest for a driver that spawns no CLI (`server/drivers/minimax.ts`), and `EngineSetup.tsx`'s `needsSignIn` can never fire for it either, since MiniMax's `snapshot()` never returns `authenticated: false`.  Fixed generically, not with a MiniMax string match: `EngineInstall.apiKeyOnly` (`server/contracts.ts`, mirrored in `src/state/store.tsx`) is a driver-level fact minimax.ts and openai-compat.ts both set true, and `EngineSetup.tsx` reads it — title becomes "Add a MiniMax API key", the description says it takes a key rather than a CLI install and points at Settings → Engines, and the Terminal-paste affordance is suppressed for guidance-only commands (`CommandRow`'s new `copyOnly`).  One shared component fixes all three surfaces at once; openai-compat's identical "Install the engine… run it with local models" inject-pane lie is fixed the same way.  `EnginesSettings.tsx`'s MiniMax callout was already honest and is untouched.  Deferred as a proposal, not built: an in-app MiniMax key-entry field (would touch `EnginesSettings.tsx` and `src/lib/custom-engine.ts`'s credential flow) — today the key only comes from `MINIMAX_API_KEY` or `~/.mmx/config.json`, both outside the app.  Tests: extended `src/components/EngineSetup.test.ts`, `server/drivers/minimax.test.ts`, `server/drivers/openai-compat.test.ts`.  Local: typecheck + targeted vitest files only, full suite on hosted CI, per the Sep 12 load pause.
- **2026-09-12 — CLAUDE — COMPLETED — Engine parity sweep: every engine a first-class option (hardcoded engine lists, picker and settings copy, ladder membership, iOS marks) (branch `claude/models-first-class`, worktree `~/apps/botfleet-claude-models`).**  Parity matrix in progress from a fresh origin/main checkout at cdba649d; findings become small PRs plus board rows.
- **2026-09-12 — CLAUDE — COMPLETED (PR #358 merged) — MM PR 4/11: one tool registry, agents tools with explicit deps, one list_bots implementation (board `4564fe5e`, branch `claude/mm-04-tool-registry`, worktree `~/apps/botfleet-claude-mm-04`).**  `server/tools/registry.ts` is now the ONE description of a tool — a `HarnessTool` record carrying the JSON schema, the MCP and HTTP surface flags, a pure gate predicate, a side-effect class, `settles`, a prompt fragment and an optional approval policy — and `mcpToolDefinitions()` / `httpToolDefinitions()` render it rather than restating it, so the drift that shipped `list_bots` offering a bot its own row on one lane and hiding `busy` on the other is now a test failure.  The single genuine wire deviation (`ask_bot` is `message` on MCP and `task` on HTTP; renaming either breaks in-flight transcripts) is DECLARED on the record with a reason, and a test asserts no undeclared one exists.  `server/tools/agents.ts` exports `createAgentTools(deps)` with every dependency explicit, removing the `import … from './index.ts'` cycle; `selectPeerBots` is the one peer filter and `listAgentsResponse` the one `/api/internal/agents` body, which `index.ts` exports as `executeListAgentsRequest` alongside a new `executeListRoutinesRequest`, both handed to the turn tool host as dependencies — so a MiniMax bot and a Claude bot run the same function with the same guards and only the transport differs.  `AgentToolCallContext` is constructible only inside the host, so a `fromBotId` in the model's arguments is ignored and `ask_bot` resolves only against the roster the model was shown.  Read-only `list_routines` joined the registry on both lanes; `agents-proxy.ts` keeps its Bearer fetch transport but renders its catalog from the registry in the shipped publication order; `turn-tools.ts` is a thin shim (PR 11 deletes it).  Deliberately untouched for Codex's #355: `server/tool-executor.ts`, the `isHttpDriver` dispatch fork and all five `isToolCallsStopReason` sites.  Verified: `pnpm typecheck` clean; 113 targeted tests green across `registry.test.ts` (23 new), `agents.test.ts` (15 new), the rewritten `host.test.ts`, `turn-tools.test.ts`, the `agents-proxy.test.ts` MCP golden (all eight tools in shipped order, unmodified), `minimax.test.ts` and `tool-executor.test.ts`; `comms.test.ts` (20) and `peer-approval.test.ts` (9) green in isolation as the e2e net — both failed once under concurrent server-spawning suites at load 58, the known boot race, not this diff.  Local: typecheck + targeted vitest files only, full suite on hosted CI, per the Sep 12 load pause.
- **2026-09-13 — CODEX — COMPLETED (PR #403 merged `f7758839`) — OpenAI-compatible driver-owned tool loop (#355; board `99b43d34`).**  Worktree `~/apps/botfleet-codex-openai-tool-loop-20260912` @ `codex/openai-tool-loop-20260912`.  Replaced the legacy harness executor with the shared one-terminal loop, preserved approval routing and custom-engine credential isolation, requested streamed usage, retained cached tokens and failed-stream usage, and rejected unsupported Grok tool calls truthfully.  Issue #355 closed.  GROK pickup 2026-09-15 closed board `99b43d34`.  No live rollout claimed.  <!-- codex-openai-tool-loop-20260912 -->
- **2026-09-12 — CODEX — COMPLETED — Removed stale tracked root artifacts (#320; board `e24dac98`; branch `codex/root-artifact-cleanup-20260912`, worktree `~/apps/botfleet-codex-root-cleanup-20260912`).**  Removed 48 unreferenced `patch*`, 10 `fix*`, 3 `rename*` one-off scripts and the root Mach-O `test` artifact after a repository-wide reference inventory.  Preserved nine ambiguous helpers/source/doc files, every untracked file, and all runtime/helper processes.  Validation: `pnpm typecheck && pnpm test` passed (3,561 Vitest tests, 19 skipped, plus every chained suite).
- **2026-09-12 — CODEX — COMPLETED — Cost telemetry billing semantics (#282; board `ad5536b6`; PR #344; merge `21c93912`).**  Settled turns now distinguish actual billing from subscription-equivalent estimates; estimates remain visible as metadata but do not inflate persisted task totals or Usage Monitor actual spend.  Full local gate passed with 3,587 Vitest tests and 19 skips plus every chained suite; hosted run `34689092432` passed all seven repository jobs after one Windows fixture rerun.  The optional Vercel preview remained unavailable because of its external build-rate limit.  No live runtime rollout was performed. <!-- wb-agent-report:ad5536b6440f42c69152443fa5a4d432 -->
- **2026-09-12 — CODEX — COMPLETED — Reduced full CI for documentation-only changes (#318; board `34813b74`; PRs #332 and #339).**  Landed a fail-closed classifier while preserving all six required check names.  Receipt PR #339 run `34687437005` used 26 summed protected-job seconds versus the 1,483-second baseline, a 98.2% reduction; code, tests, lockfiles, workflows, packaged/test-enforced documents, generated/native inputs, scheduled runs, and manual full-checks retain the complete matrix.
- **2026-09-12 — CODEX — COMPLETED — Renderer hydration, settings-save, reconnect, and RAG status reliability (issues #266–#268; PR #330; merge `9346a0f3`; boards `62a56733`, `1ca6cb49`, `5ee62380`).**  Preserved the last good renderer snapshot across partial hydration failure, added bounded reconnect hydration, surfaced update/Qdrant save failures without optimistic drift, displayed the RAG route and freshness, announced new errors while suppressing scrollback history, and stopped attached-UI retries after downstream close.  Full local gate passed 3,556 Vitest tests with 19 skips plus every chained suite; final hosted run `34687934887` passed all seven jobs.  Screenshot captured the visible route/state/freshness UI.  Source only; live deployment remains with the Mac rollout owner. <!-- wb-agent-report:62a567333ee945999c908620023e0318 -->
- **2026-09-12 — CODEX — COMPLETED (PR #331 merged `c6ef1103`) — Keep Bot Settings open after failed saves (#293; board `f16194b9`; branch `codex/ios-bot-save-error-20260912`, worktree `~/apps/botfleet-codex-bot-save-20260912`).**  origin/main Save dismisses only after `ProfileSaveGate.run` succeeds; failed `updateProfile` returns nil and keeps the draft.  GROK pickup 2026-09-15 verified source and closed issue #293 plus board `f16194b9`.
- **2026-09-12 — CODEX — COMPLETED (PR #326 merged `1a8b7412`) — End stale Live Activities when iOS suspends (#294; board `699ea1ae`; branch `codex/ios-live-activity-suspend-20260912`, worktree `~/apps/botfleet-codex-live-activities-20260912`).**  Explicit-teardown policy is in source.  GROK pickup 2026-09-15 closed board `699ea1ae`.  Remaining on issue #294: physical-device acceptance and APNs `pushType.token`.  BOTFLEET-E hang landed separately in #414.
- **2026-09-12 — CODEX — COMPLETED — Companion and iOS safety follow-ups (#93, #291, #292, #310; PR #323; merge `ae8abe7d`; boards `6ff6f355`, `9af28de9`, `ced11b51`, `1e36bd92`, `4bb6be10`).**  Paired profile writes now enforce a safe field subset, permission copy no longer promises a denied standing grant, uploaded images round-trip through companion reads, engine edits preserve primary and fallback effort, and working rooms expose an exact-task Interrupt action.  Local full gate passed with 3516 tests and 19 skips, Swift passed 255 tests, and hosted unsigned iOS build passed.  Simulator screenshot and physical-device acceptance remain open because the local iOS runtime was unavailable and its official download was not viable.
- **2026-09-11 — CODEX — COMPLETED — Engine turn ownership and safe fallback routing (issues #275–#277; boards `46f08797`, `fbef84b2`, `adae362e`; commits `494305cb`, `434827f7`, `e38c5fff`, `4973e3bb`, `005f114e`, `cc54bbfb`, `52c847b6`, `dd4dd8ca`; branch `codex/engine-turn-safety-20260911`, worktree `~/apps/botfleet-codex-engines-20260911`).**  Landed in PR #315 as `9878e65c`.  Attribute fallback outcomes to the engine/model that ran; stop every actual stalled owner without releasing a live process or VM lease; filter automatic fallback by availability, authentication, caps, and cooldown; serialize provider reloads against new work; retain generation and completion-fold ownership until safe release; and keep the prior dispatcher current when Claude preflight blocks before provider launch.  Final review added exact request-owner approval routing, stable fallback-health refresh across unrelated reload generations, reload-safe queued successor dispatch, exact dispatch fencing through asynchronous turn setup, retained room fallback selection, and deferred-work drains after either reload outcome.  Full `pnpm typecheck && pnpm test` passed with 3,490 Vitest tests and every chained suite before additive review fixes.  Final hosted CI run `34648428120` passed on macOS, Ubuntu, Windows, package smoke, iOS, and control-plane; after the final #316 integration, 86 safety/Claude/room-queue tests plus four relevant real-server regressions and typecheck passed, covering fallback room approval, provider-reload overlap, stale setup suppression, successor cursor ownership, and persisted cursor freshness across blocked preflight.  Runtime rollout remains pending. <!-- wb-agent-report:46f08797c6e146469e9ed768aaf064c0 -->
- **2026-09-11 — CODEX — COMPLETED — Harness data ownership (#264, PR #313, board `f781c56d81f44461a81bf0acb8b3aa42`).**  Landed as `1a418e01` with green macOS/Linux/Windows full CI, packaged-server checks, Linux package smoke, control-plane checks, and hosted iOS build.  Runtime rollout remains pending; first update must stop legacy harness binaries through the normal lifecycle. <!-- wb-agent-report:f781c56d81f44461a81bf0acb8b3aa42 -->
- **2026-09-11 — AG — COMPLETED / DEPLOYED — Infisical timeout handling and full write-through sync (PR #309 squash `2246f19e`, board `42937fa4`, branch `ag/infisical-timeout-and-write-through`, worktree `~/apps/botfleet-ag-infisical`).**  Bumped default network call timeout from 8s to 30s and boot cap to 25s; wrapped AbortError/TimeoutError in InfisicalError with 504 and phase context; enabled full write-through for all non-empty SECRET_FIELDS so Infisical is the sole source of truth and settings survive rebuilds; hardened timeout body consumption, landed-write tracking, ambiguous write reconciliation, and canonical snapshot preservation.  Merged to main and deployed to local /Applications/BotFleet.app and LaunchAgent com.jay.botfleet-server via update-botfleet.sh.
- **2026-09-11 — GROK — COMPLETED — Map Remote Access Test Connection pill to product copy (board `f3c03a10`, branch `grok/remote-access-test-copy`, worktree `~/apps/botfleet-designer-remote-access-copy`).**  Designer follow-up to #298.  Pill Reachable / Couldn't reach this Mac. / The request timed out.  aria-label matches Test Connection.  Extra-ship no.
- **2026-09-10 — GROK — IN PR #299 — Codex P1 boot-recovery unattended: `bootRecoveryTurnOpts` (board `3d9ff839`, branch `fixer/thread-append-auto-role`, worktree `~/apps/botfleet-fixer-thread-append`).**  Always forward persisted `automationSource`; pass `unattended: true` for webhook/resource or role=system without consulting empty in-memory `isUnattended` after restart.  Extra-ship no.  No merge.
- **2026-09-09 — CLAUDE — IN PR #260 — Post-merge PR #247 review-thread triage; redact failed tool-result detail before Sentry (board `a5688de705dd4413bfdd9e0c107d9f6b`, branch `claude/sentry-redaction-followup`, worktree `~/apps/botfleet-claude-sentry-redact`).**  Triaged the four unresolved chatgpt-codex-connector threads posted after PR #247 merged.  All four real: `server/sentry-ai.ts:311` sent a failed tool call's `event.detail` (real provider stdout/stderr/error text from codex.ts/claude.ts/pi.ts/acp/core.ts, via `describeResult()`) to Sentry with zero redaction — fixed here with `redactSecretsInText()` before the 200-char slice, plus a regression test (`server/sentry-ai.test.ts`, 21/21 green); `src/lib/sentry.ts:268`'s renderer diagnostics kill switch is a permanent no-op in every packaged build (`buildTimeDsnActive`, baked in by `release.yml`) despite UI copy promising it "stops reporting straight away" — filed as board `13008da2f66841f295b3bb7eead7ec94` (P1, needs a product call, not a quick patch); `server/sentry-ai.ts:392` double-reports one failed turn as two Sentry exceptions (openai-compat.ts/grok.ts emit `runtime.error` then `turn.completed{ok:false}` for the same failure) — board `a079421914ab4a73b15cabc98e2f3582` (P2); `src/lib/observability-config.ts:89` can't clear a custom environment back to default (DSN's write-only-field omit rule wrongly applied to a plain visible input) — board `c5217e2bf42f432a8c89c62045c30725` (P2).  All four threads replied to and resolved on GitHub.  `pnpm typecheck` clean; ran only the affected test file (Mac load ~390-400 during this work; full suite not attempted).  **Follow-up (same day):** Codex was right that the fix sat on the wrong side of the clip — every driver runs the raw provider result through `describeResult()` (`shared/tool-activity.ts`), which cuts it to 240 characters, so a PEM lost its `END … PRIVATE KEY` footer, a JWT lost its third segment and a long quoted value lost its closing quote before redaction ever ran.  The redactor moved to `shared/redact.ts` (re-exported from `server/redact.ts`, so all twelve server importers are untouched) and `describeResult()` now redacts BEFORE it clips; the patterns also learned the truncated shapes as belt-and-braces for pre-clipped details.  Regression tests drive the real driver-to-Sentry path for all four driver payload shapes.  Typecheck clean, 465 tests green across the affected and adjacent files.
- **2026-09-08 — CLAUDE — IN PR #258 — config.json lost-update race between Electron and server (PR #251 review) (board `a2a3a586`, branch `claude/config-lock`, worktree `~/apps/botfleet-claude-config-lock`).**  Shared advisory file lock (electron/config-file-lock.mjs) around every config.json read-modify-write: server saveConfig, Electron recordAutomaticCheck, and the boot credential migrations.  Electron no longer writes autoUpdate.enabled; the harness owns the toggle.  Two-process regression tests on both sides.  Stacked on PR #251.
- **2026-09-08 — CLAUDE — IN PR #251 — settings-rev2 C repairs: throttle persists, fingerprint compared, webhook URL clears (board 2a77066b, branch claude/settings-c-repairs, worktree ~/apps/botfleet-claude-settings-c).**  `recordSuccessfulAutoCheck` now actually writes the updated `autoUpdate` record to disk (was a no-op `appendFileSync(path, "")`), so the 6-hour throttle engages; `shouldRunAutomaticCheck` bypasses the throttle when the Mac bundle fingerprint changed since the last check; clearing the webhook URL and pressing Save now clears it server-side (the client stopped converting `""` to `undefined`); Test Setup falls back to the saved URL, Save has its own error line, and `update:set-enabled` reports failures again.  Review round 2 (Codex + Sentry): fixed the coordinator swallowing a failed check into a false "success" (would have throttled retries for 6h), fixed the Mac bundle fingerprint hashing the wrong install when two copies coexist, fixed a Save-error/Test-result render race.  The config.json lost-update race between the Electron and server processes is filed separately (board a2a3a58690d748f0b30aaf38be028636), out of scope for this repair.  Typecheck clean; `pnpm test:updater` and every other `pnpm test` chain step green; the vitest `test-floor` run showed 4 failing files that are a pre-existing `"server never came up"` 20s boot race under this Mac's shared load (reproduced in isolation, unrelated to this diff — see PR body for the root-cause evidence).
- **2026-09-08 — CLAUDE — COMPLETED (PR #257 merged) — MM PR 9/11: MiniMax price table, real turn cost, and LLM observability span (board `264770ee`, branch `claude/mm-09-prices-cost-span`, worktree `~/apps/botfleet-claude-mm-09`, parent `4564fe5e`).**  `src/lib/minimax-prices.ts` and a matching table in `server/drivers/minimax.ts` price all three catalog models from platform.minimax.io's paygo page (verified today) — MiniMax-M3's published >512K-input-token tier at double the base rate is modeled, not flattened away.  `server/drivers/chat-completions/pricing.ts` adds `costUsd(usage, prices, model)`, returning `null` — never `0` — for an unpriced or unknown model; `chat-completions/loop.ts` gained an optional `computeCost` hook so the terminal `turn.completed` carries a real cost on the success path AND every error path, from whatever usage already streamed.  `minimax.ts` reads `prompt_tokens_details.cached_tokens` (verified field name against MiniMax's own example response) into `cachedInput`, wraps each round in `withChatSpan` for a `gen_ai.chat` span, and does NOT also call `recordExecutedTools` — the loop already emits real `item.started`/`item.completed` per tool call, which `observeRuntimeEvent` turns into `execute_tool` spans generically; calling both would double every tool span, and a driver-level test proves the count stays single.  `generateText` (titles, summaries) now runs on `MiniMax-M2.7-highspeed` instead of the 1M-context flagship.  Consumers (chat-header cost chip, bot-panel Cost cell, Usage Monitor's `_count.costUsd` pricing-coverage metric) needed zero changes — verified by grep, they already gate purely on a finite cost.  `pnpm typecheck` clean; 77 targeted tests green; oxlint introduces zero new violations over the pre-existing `main` baseline.
- **2026-09-08 — CLAUDE — COMPLETED (PR #254 merged) — MM PR 2/11: driver-owned tool loop, MiniMax emits exactly one terminal event per user turn (board `838bf3c5`, branch `claude/mm-02-driver-owned-loop`, worktree `~/apps/botfleet-claude-mm-02`).**  The correctness PR of the MiniMax first-class series.  `server/drivers/chat-completions/loop.ts` runs model-to-tool rounds inside `sendTurn` behind a closed `TurnLoopExit` union and ONE try/finally, so round cap, wall clock, request timeout, provider error, abort, suspend and an unexpected throw each emit exactly one `turn.completed` instead of leaving the bot busy until the 20-minute stall watchdog.  Budgets 12 rounds / 900s wall clock / 180s per request / 90s per tool, concurrency 4 with results re-sorted to call order; usage sums across rounds and is reported on the error path; `item.completed` carries the host's real outcome.  `server/tools/host.ts` runs `list_bots` (caller excluded, `busy` reported — parity with `/api/internal/agents`) and `ask_bot` (through `executeAskBotRequest`) with dependencies injected, so `server/tools/*` never imports `index.ts`.  `EventBus.attach` drops and logs a second terminal event for the same turn.  openai-compat stays on the old executor until PR 3.  No TestFlight.
- **2026-09-08 — CLAUDE — COMPLETED — Allowlist wins over the auto fallback; apply-defaults goes through the per-bot guard (board `315b1fd7` `ff8dd550`, branch `claude/settings-a-repairs`, worktree `~/apps/botfleet-claude-settings-a`).**  Repairs settings-rev2 lane A (#220): `resolveGrants` no longer returns the unfiltered workspace default with `auto: true` when the operator allowlist empties the intersection, so an unconfigured bot can no longer run on This Computer after the operator disabled it.  `botDefaults.allowedComputers` accepts `null` (a fresh install's every save used to 400), `apply-defaults` runs the per-bot acknowledgement guard and host-revoke interrupt and persists the operator's unfiltered default, `apply-model-defaults` skips busy bots instead of swapping their engine mid-turn, and the Local VM mode switch reckons with per-bot targets rather than only the shared one.  Merged as `a189e21f` (PR #253); full CI green on macos/ubuntu/windows plus iOS, control-plane and package+smoke.  Review round two fixed four more findings: each auto mount is gated on its own allowed destination (a boolean let `["local"]` mount a Box and `["cloud"]` fall back to the host), apply-defaults is all-or-nothing because a "skipped" bot still inherited the persisted default, the host-revoke interrupt reads the legacy `computer` field, and a deleted bot's per-bot Local VM is removed with it.
- **2026-09-08 — CLAUDE — COMPLETED (PR #255 merged) — MM PR1: HTTP-lane fixtures + MiniMax stream hardening (board `aad7d970`, branch `claude/mm-01-fixtures-hardening`, worktree `~/apps/botfleet-claude-mm-01`).**  Real `fake-openai-server.ts` http fixture (SSE + JSON, scripted queue, recorded bodies) peer to `fake-claude-cli.ts`; `tool-executor.test.ts` swapped its inert bus stub for a real pub/sub; MiniMax stream hardening (trailing frame flush, first-write-wins tool ids, `reasoning_content` on both paths, usage on the failure path, extracted `resolveMinimaxCredentials`); honest `"MiniMax"` displayName/install copy and matching `EnginesSettings.tsx`/`ModelPicker.tsx` tool-support text with real NBSP sentence gaps.  Typecheck clean; PR-relevant tests 53/53; full suite 3200/3222 passed, 2 unrelated pre-existing `server/index.test.ts` failures under extreme host load (321 load average).  Auto-merge armed.
- **2026-09-08 — CLAUDE — IN PR #251 — settings-rev2 C repairs: throttle persists, fingerprint compared, webhook URL clears (board 2a77066b, branch claude/settings-c-repairs, worktree ~/apps/botfleet-claude-settings-c).**  `recordSuccessfulAutoCheck` now actually writes the updated `autoUpdate` record to disk (was a no-op `appendFileSync(path, "")`), so the 6-hour throttle engages; `shouldRunAutomaticCheck` bypasses the throttle when the Mac bundle fingerprint changed since the last check; clearing the webhook URL and pressing Save now clears it server-side (the client stopped converting `""` to `undefined`); Test Setup falls back to the saved URL, Save has its own error line, and `update:set-enabled` reports failures again.  Review round 2 (Codex + Sentry): fixed the coordinator swallowing a failed check into a false "success" (would have throttled retries for 6h), fixed the Mac bundle fingerprint hashing the wrong install when two copies coexist, fixed a Save-error/Test-result render race.  The config.json lost-update race between the Electron and server processes is filed separately (board a2a3a58690d748f0b30aaf38be028636), out of scope for this repair.  Typecheck clean; `pnpm test:updater` and every other `pnpm test` chain step green; the vitest `test-floor` run showed 4 failing files that are a pre-existing `"server never came up"` 20s boot race under this Mac's shared load (reproduced in isolation, unrelated to this diff — see PR body for the root-cause evidence).
- **2026-09-08 — GROK — TIP FIX — PR #235 composer HEIC/paste types: structural ClipboardFileSource + globalThis createImageBitmap/document guards so server (no DOM) typecheck stays green without dropping paste/HEIC (branch `grok/composer-paste-heic`, worktree `~/apps/botfleet-grok-composer-paste`).**  Extra-ship no.  No merge.
- **2026-09-08 — CLAUDE — IN PR #252 — settings-rev2 B repairs: DeepSeek key env round-trip, balance chip hides on error, bounded balance fetch (board 01f4994a, branch claude/settings-b-repairs, worktree ~/apps/botfleet-claude-settings-b).**  Added the missing `DEEPSEEK_API_KEY`/`DEEPSEEK_URL` env overlay so a packaged-app credential save survives the external-secret tombstone, hid the DeepSeek balance line on error instead of showing it forever, restored the cap/disabled/reason status cascade over headline lines, and bounded `getDeepSeekBalance` to a 4s timeout so `/api/quotas` never blocks on a hanging upstream.
- **2026-09-08 — GROK — TIP FIX — PR #238 Message.steered for ChatView mid-turn chip (board `e92b04b5`, branch `compiler/ios-composer`, worktree `~/apps/botfleet-compiler-composer`).**  Aligns iOS Message with web `steered` + ChatView "Sent mid-turn".  Extra-ship no.  No TestFlight.  No merge.
- **2026-09-08 — BF-DESIGNER — COMPLETED — iOS ST app save unknown room member + queued X no route (board `e8a8e5f1`, branch `grok/ios-save-queue`, worktree `~/apps/botfleet-grok-ios-save-queue`).**  PATCH drops leftover deleted-bot roster ghosts so a phone save is not blocked.  Packaged companion 1.0.30 still 404s DELETE /queue/; source already allowlists it (#224).  No TestFlight.  Ask before bouncing live BotFleet.  Rollout: `docs/rollouts/2026-09-08-ios-save-queue.md`.
- **2026-09-08 — GROK — COMPLETED — Settings Models layout: larger dialog, wrap pills, iOS stacked fallbacks (board `991b22a3`, branch `grok/settings-models-layout`, worktree `~/apps/botfleet-grok-settings-models`).**  Designer pass from Jay screenshot.  Dialog 1100×720.  Pills wrap.  iOS primary then fallback rows.  Extra-ship no.  No TestFlight.  PR, do not merge.
- **2026-09-04 — MINIMAX — IN PR #220 — settings-rev2 A: bot computer allowlist, VM mode, and per-bot defaults (board `3da59409`, branch `minimax/settings-rev2-a`, worktree `~/apps/botfleet-minimax-a`).**  Lane A of the settings revamp.  `botDefaults.allowedComputers` + `allowedBotComputers()` filter at every `resolveGrants` call; new `POST /api/bots/apply-defaults` and `POST /api/bots/apply-model-defaults` reuse the existing `botDefaults` shape, intersected with the allowlist; new `POST /api/local-computer/mode` flips the shared/per-bot switch and removes the existing desktop on the way out.  LocalComputerSection reads `cfg.localVm.mode` (no more hardcoded `perBot = false`); BotComputerDefaults gains a top-of-page Allowed Computers card and a Set-All-Bots button; FleetModelsSection gains a Default block above the bots table.  23 new test cases across `computer-grants.test.ts`, `config.test.ts`, `index.test.ts`.  Coordination: lane-local renames only; Claude's terminology lane owns the canonical label across ComputerPanel / CloudBackendPicker / SettingsPanel / WebhooksPanel.  `pnpm typecheck` clean.  `pnpm vitest run server/` clean.
- **2026-09-08 — MM — COMPLETED — HTTP-driver tool executor: replay the model's tool_calls on the harness side (branch `minimax/tool-executor`, worktree `~/apps/botfleet-minimax-tool-executor`).**  Catalog `server/turn-tools.ts` exposes only the agents surface (list_bots, ask_bot); Composio + computer-use return a "not wired" string so the model stops calling them.  Dispatch routes HTTP drivers (minimax, openai-compat) through `sendTurnWithToolLoop`; CLI drivers keep their own loop.  HTTP drivers now set `capabilities.agentsMcp: true` so the agents integration is populated.  Removed a pre-existing duplicate-transcript loop in minimax that would have garbled the executor's tool-result replay.  `SendTurnInput.transcript` extended with `toolCalls` and `toolResults`; both drivers already translate those to OpenAI's `role: "tool"` / `tool_calls` wire shape.  Streaming-text fix (ChatMarkdown bubble for live `streaming[threadId]`) is the prior commit on the same branch.
- **2026-09-04 — GROK — COMPLETED — Quota rows hide Gemini/Cursor monthly; MiniMax CLI first-tier. PR #217 squash `2ac1d35a`. Board `d4d3343b`. Issue #216. Mac app needs `update-botfleet.sh`.**
- **2026-09-04 — GROK — IN PR #215 — WAVE1 Infisical: ios-testflight.yml exports six Apple/iOS signing names from Infisical prod (board `2f422f46`, branch `grok/infisical-ios-testflight`, worktree `~/apps/botfleet-grok-infisical-tf`).**  Identity stays on GitHub.  PR open, not merged.  GH APPLE_/IOS_ secrets not deleted.
- **2026-09-04 — GROK — IN PR #214 — Sentry max-features: Node profiling + iOS Replay + Seer Autofix (board `af1ab6e9`, branch `grok/sentry-max-features`, worktree `~/apps/botfleet-grok-sentry-max`).**  Seer Autofix ENABLE BotFleet only (`autofixAutomationTuning=always`).  Billing did not block.  Rollout: `docs/rollouts/2026-09-04-sentry-max-features.md`.
- **2026-09-04 — GROK — COMPLETED — Usage Monitor URL and ingest token persist; Save + Test Connection. PR #192 squash `5c4b4304`. Board `5c0ee072`. Issue #191. Mac app needs `update-botfleet.sh` to load origin/main.**
- **2026-09-04 — GROK — COMPLETED — antigravity-usage N/A remaining means exhausted (board `d8306719`, branch `grok/quota-na-exhausted`).**  Owner: Gemini is fully exhausted.  Skip omitted remaining until reset.
- **2026-09-03 — GROK — COMPLETED — Poll `antigravity-usage quota --json`; skip exhausted Antigravity models until reset. PR #187 squash `62f10385`. Board `087cd1b0`, issue #186. Direct CLI remaining, not LiteLLM or EasyCLIProxy. Mac harness still on prior build until next `update-botfleet.sh`.**
- **2026-09-01 — GROK — COMPLETED — Sentry DSN hygiene: no hardcoded iOS fallback (branch `grok/sentry-dsn-hygiene`, worktree `~/apps/botfleet-grok-sentry-dsn`).**  Board `aecc129faf6842b480b229ca92b143dc`.  Cocoa init is plist-only / build-injected; empty DSN = no-op.
Protocol: /Users/jay/apps/EFFORT-LOG-PROTOCOL.md (canonical). Live board: this file
(mirror: docs/EFFORT-LOG.md in the repo). As of 2026-08-29.

## In Progress
- **2026-09-24 - AG - IN_PROGRESS - Fix underlying BotFleet routine failures: VPS SSH timeouts & non-cloud degradation, unattended antigravity host_control_policy refusal, and DSH bridge process leak rpc_errors.** <!-- wb-agent-report:75660cb699f14283afc8474d6219ae29 -->
- **2026-09-24 - CODEX - IN PROGRESS - Align Codex OAuth fallback with live models (board `16e1afdb924c4693bb19967908d9d379`, issue #552, branch `codex/codex-oauth-fallback-20260924`, worktree `~/apps/botfleet-codex-oauth-fallback-20260924`).**  Claimed Thu, Sep 24, 2026.  Replace fallback-only unavailable GPT-6 Sol/Luna slugs with live GPT-5.6 Sol/Luna, keep saved legacy choices round-trippable, and verify timeout behavior without treating direct API catalog names as OAuth models.  Coordinate with picker PR #539.  <!-- wb-agent-report:16e1afdb924c4693bb19967908d9d379 -->
- **2026-09-24 - CODEX - IN PROGRESS - Reconcile Grok 4.7 across CLI and direct API catalogs (board `27e858b8ed064cf4a85445fd841e68ff`, issue #567, branch `codex/grok-47-catalog`, worktree `~/apps/botfleet-codex-grok-47-catalog`).**  Promoted Grok Build and direct xAI API catalogs to 4.7 with transport-specific options; kept Build Fast CLI-only, preserved 4.6/4.5 selections, and aligned API-equivalent pricing/context metadata without changing subscription cost.  Focused catalog, round-trip, and pricing assertions added; local tests deferred under severe host load.  Hosted seven-check PR gate pending.  <!-- wb-agent-report:27e858b8ed064cf4a85445fd841e68ff -->
- **2026-09-24 - CODEX - IN PROGRESS (source fix PR #581 merged as 744a2497) - Classify terminal Usage Monitor telemetry responses, durably quarantine bounded failed batches, and release the queue head (#551; board 0e195d95).**  Seven required CI checks passed on f71174bd.  Receiver readback, historical overflow classification, and live installed verification remain open; no harness restart or ingest probe.  <!-- wb-agent-report:0e195d95f52b49f5b5d9c75d1d560891 -->
- **2026-09-24 - CLAUDE - IN_PROGRESS - [BF][CLAUDE] Upstream OpenMausBot review: what to learn from and borrow since the fork (report).** <!-- wb-agent-report:ab19befc16c547cdb99a20a8ae3d1bbd -->
- **2026-09-24 - CODEX - IN_PROGRESS - Refresh token, model, and telemetry audit with live evidence (board `87cfe7dd5ca041ae9b8e6812ad0c6584`, issue #562).**  Isolated branch `codex/audit-refresh-20260924`; publish after pending telemetry PRs so statuses match merged code. <!-- wb-agent-report:87cfe7dd5ca041ae9b8e6812ad0c6584 -->
- **2026-09-24 - CODEX - COMPLETED (merged PR #579, commit 2aa29161) - Suppress only hosted paired-gateway tunnel-offline 530/1033 iOS Sentry errors (board `80071e1585094f818f4659d9abb73b35`, issue #574, branch `codex/ios-sentry-gateway-5xx`, worktree `~/apps/botfleet-codex-ios-sentry-io10`).**  The event filter matches the saved connection's policy-allowed hosted origin and exact HTTP 530 status; 502 and 503 remain reportable because they can indicate companion or harness faults on a reachable gateway.  Unrelated hosts, other 5xx errors, and transport failures remain reportable.  Owner refinement and failable-endpoint test fix are included; merged after all seven required hosted checks passed on final head c8e6a02d; Cloudflare HTTP 530 is suppressed only when cf-error-type is 1033 and the origin matches a saved policy-allowed hosted route.  Local Apple builds remain skipped under severe system load. <!-- wb-agent-report:80071e1585094f818f4659d9abb73b35 -->
- **2026-09-24 - GROK - PLANNED - Engine picker: collapse Why This Engine; Codex GPT-6 Astra/Sol/Luna.** <!-- wb-agent-report:37a5d910e16e41a9baeda10d2ed1706f -->
- **2026-09-19 - BF-FIXER - PLANNED - Recurring session/prompt timeout on BotFleet Compiler thread 567846db despite PR #467 (PD #175).** <!-- wb-agent-report:e503e6d182114378a15199d58cdd281f -->
- **2026-09-23 - BF-DESIGNER - IN_PROGRESS - Queued message: Steer, Edit, Cancel as separate labeled actions.** <!-- wb-agent-report:369e41b7ef84494eb2462f26eb813a45 -->
- **2026-09-23 - MM - IN_PROGRESS - Linq iMessage integration — Linq v3 transport + voice messages + LinqSettings.** <!-- wb-agent-report:9c415bb589124389b133e4072d694101 -->
- **2026-09-22 - MM - PLANNED - [BF][MM] APNs push: 1825 SendFailed since 21:50; likely HTTP/2 transport issue (native fetch + APNs HTTP/2 requirement).** <!-- wb-agent-report:851f6868afc748d78ac2da02fa0541f9 -->
- **2026-09-22 - MM - IN_PROGRESS - [BF][MM] BotFleet bundle-ID rename: com.botfleet.* + com.jay.botfleet-server -> app.botfleet.<platform>; add group.app.botfleet + botfleet.app associated domain.** <!-- wb-agent-report:581f1f0b297f4f6b9f108cabdf8b3fbd -->
- **2026-09-21 - MM - IN_PROGRESS - [BF][MM] Computer Use settings: per-provider enable/disable + impact-confirm + matrix view.** <!-- wb-agent-report:9613f1355e424e5d943d7d7d91e42348 -->
- **2026-09-19 - AG - IN_PROGRESS - Fix stale grok-3-mini hardcoded in Grok driver generateText.** <!-- wb-agent-report:24ca78d75a24438187e9bfb4b37de67a -->
- **2026-09-19 - MM - IN_PROGRESS - [BotFleet][MM] engines polish: collapsible MiniMax callout, drop MM-M2.7 from DSH, Codex default → Luna.** <!-- wb-agent-report:b5953d961a754f98a6cd85feb3ec0b24 -->
- **2026-09-03 — GROK — COMPLETED — Hosted macos-latest ios-ship path filter like CT/DealDex/ST (board `f9a51214`, issue #185, branch `grok/hosted-ios-ship`, worktree `~/apps/botfleet-grok-hosted-ios-ship`).**  Vendor `scripts/ios-fleet`, map existing `APPLE_API_*` + `IOS_CERT_*`, no extra flags, no mint.  PR opening this lane.  Merge may trigger the hosted job with `force_ship=0`.
- **2026-09-06 — GROK — COMPLETED — Settings Remote Access UX for named tunnel https://botfleet.jays.services (board `a4b7edb0`, issue #226, branch `grok/remote-access-named-tunnel`, worktree `~/apps/botfleet-grok-remote-access`).**  Designer copy.  Copyable Remote URL.  No TryCloudflare on this path.  Companion Gateway on Phone.
- **2026-09-08 — CLAUDE — COMPLETED — FLEET-INFRA-C6 (F09) verify/close, sentry-ci-report ios-ship coverage (F10/CM) (branch `claude/ios-bot-computer-refs`, worktree `~/apps/botfleet-claude-ios-bot-computer-refs`).**  F09: grepped every `.computer` reference across `ios/**/*.swift` — the dangling `Bot.computer` compile error PR #117 left in `ComputerView.swift:67` was already fixed by PR #199 (2026-09-04); confirmed via CI history (every "Swift tests + iOS build" job run since has passed) since this Mac lacks the iOS platform component for a local `xcodebuild` repro.  Resolved FLEET-INFRA-C6 via Sentry API (root cause fixed; its "escalating" status since has been driven by an unrelated windows-latest CI flake under the same broad fingerprint — separate issue, not fixed here).  F11: `sentry-ci-report.yml` exists, intentionally diverges from the ai-fleet-coordinator template (adapted from Usage-Monitor's gold copy per its own header) — no sync needed.  F10/CM: `ios-ship.yml` has `schedule: '18,48 * * * *'` but was missing from both the `workflow_run.workflows` observed list and `CRON_SCHEDULES` (stale entries still named the deleted `ios-testflight.yml`'s "iOS TestFlight Release") — added correct entries in both files.  FLEET-INFRA-CM was already resolved (self-resolved, moot). <!-- wb-agent-report:c630ceed -->
- **2026-09-19 - MM - IN_PROGRESS - BotFleet rooms: a stalled or timed-out room turn returns before its exact-key lease release and relies on releaseSoleOwner, which declines while another member is in flight on the same thread; narrow, self-healing on the next sole-owner completion; release the exact key from the stall and timeout settlement path (follow-up to #460).** <!-- wb-agent-report:aac035dd920c47398e0086b3c875d29c -->
- **2026-09-16 - CLAUDE - IN_PROGRESS - BotFleet: take every agent quota field AgentBar publishes, give every engine full computer, tool and skill capability, and reach fleet recall optimally from every lane (owner ask Wed, Sep 16).** <!-- wb-agent-report:151387ee7a5044cca7e20de9d292d849 -->
- **2026-09-16 - GROK - IN_PROGRESS - DSH ACP mounts BotFleet MCP (local computer / CUA).** <!-- wb-agent-report:3bfa4c2cc4504fe1894902b610295e11 -->
- **2026-09-16 - BF-COMPILER - IN_PROGRESS - Compiler webhook turns: slim GitHub payloads so Grok does not time out onto Antigravity argv.** <!-- wb-agent-report:34f10c9d65a046ee8ba31af61a75ea6c -->
- **2026-09-15 — AG — COMPLETED — Engine capability parity: toolLoop for Grok/OpenAI, workspace file tools for HTTP, and DSH MCP (board `db44b219`, branch `ag/engine-capability-parity`, worktree `~/apps/botfleet-ag-fixes`, PR #442).**

- **2026-09-13 - AG - COMPLETED - Native computer tools for HTTP models and direct DSH MiniMax support.** <!-- wb-agent-report:5241e335df964cd0a3e785cdd3faa496 -->
- **2026-09-13 — CODEX — IN PR #402 — Local VM lifecycle and host Auto consent (issues #345, #346, #349; boards `7c2db2d7`, `1156a06d`, `1c63f177`; branch `codex/local-vm-safety-20260912`, worktree `~/apps/botfleet-codex-local-vm-safety-20260912`).**  Bot deletion is fenced against VM mode changes; bulk and configuration-wide grants bind confirmation to exact displayed identities; per-bot Auto validation covers explicit, inherited, and automatic host access while preserving Off.  GROK 2026-09-15: remaining Codex P2 now gates automatic-host consent on Darwin plus `localComputerMcp`; explicit and inherited Local still require ack.  No live containers, permissions, or configuration changed.  <!-- codex-local-vm-safety-20260912 -->

- **2026-09-13 — AG — IN PR #354 — Action chips draft population, bot profile sheet cancellation guard & model cache, turn replay byte cap, honest tool-support copy (board `77582325`; branch `ag/audit-fixes`, worktree `~/apps/botfleet-ag-audit-fixes`).**  Populate composer draft from predictive action chips instead of immediate dispatch to prevent accidental runs; rethrow Swift Concurrency CancellationError in CompanionCore Client and filter cancellations in Session to prevent 'Something went wrong / cancelled' alerts and empty model lists; cache engine instances in Session; cap mid-thread turn context replay to 128 KB with omission notice in turn-context.ts to avoid prompt limit crashes during fallback turns; increase ACP init timeout to 30s; remove DSH from tool recommendations.  <!-- wb-agent-report:77582325 -->





- **2026-09-12 — BF-DESIGNER — COMPLETED — iOS roster keeps Bot Chats separate from user rooms (board `bf86a7bf`, branch `grok/ios-bot-chats`, worktree `~/apps/botfleet-designer-ios-bot-chats`).**  `group.dm` rooms leave the Channels/Rooms section and sit in Bot Chats at the bottom, default collapsed, matching Mac.  Extra-ship no.  No TestFlight.  <!-- wb-agent-report:bf86a7bff1e345dda62ab689bb3bf249 -->



## Completed
- **2026-09-24 - MINIMAX - PLANNED - [BF][MINIMAX] Bound replay-history byte cap across minimax/grok/openai-compat drivers (Codex review 09-24).** <!-- wb-agent-report:c7ac17fce96a49da8d50c9b429eb9956 -->
- **2026-09-24 - CODEX - COMPLETED (PR #580 merged `61e7bab5`) - Keep expected bot stops out of Sentry error spans (board `1d2e5764f4f14728bbb6a03b6f7c143e`, closed issue #566).**  Expected stops leave neutral spans unless a provider error was reported; true failures retain error status.  All seven hosted CI checks passed on the merged head.  Mac install remains pending.  <!-- wb-agent-report:1d2e5764f4f14728bbb6a03b6f7c143e -->
- **2026-09-24 - CODEX - COMPLETED (PR #580 merged `61e7bab5`) - Repair packaged BotFleet Sentry SDK loading (board `a6a3793c`, closed issue #573).**  ESM bundle banner fixes SDK loading, and package smoke verifies initialization using a synthetic DSN.  All seven hosted CI checks passed on the merged head.  Mac install remains pending.  <!-- wb-agent-report:a6a3793c -->
- **2026-09-24 - CODEX - COMPLETED (PR #565 merged `a9a309dd`) - Compact list_routines summaries and add authorized ID-scoped instruction details (board `912b435002224f02af6833f9b6075906`, issue #556, branch `codex/routine-output-budget`, worktree `~/apps/botfleet-codex-routine-output-budget`).**  The list returns serialized-byte-bounded previews, counts rows omitted by output and the 100-row cap, and preserves omission counts through MCP and HTTP.  Full secret-redacted instructions remain available by ID only to the authorized bot and thread.  Focused preview and transport tests passed 64/64 on final head 89061304; all seven required hosted CI contexts passed.  A repeat local server integration test timed out before its fake-CLI fixture appeared under severe Mac pressure; the earlier focused integration test passed.  Vercel preview remained rate-limited and is nonrequired.  <!-- wb-agent-report:912b435002224f02af6833f9b6075906 -->
- **2026-09-24 - CODEX - COMPLETED - Measure real turn latency in Usage Monitor telemetry (board `8f3629503b6349dbaac9403035b14542`, issue #564, PR #548 merged `39528d9c`).**  Completion telemetry carries measured dispatch duration for direct and room turns; unknown duration is omitted.  All seven hosted CI contexts passed.  Mac runtime has not been updated. <!-- wb-agent-report:8f3629503b6349dbaac9403035b14542 -->
- **2026-09-24 - CLAUDE - IN_PROGRESS - [BF][CLAUDE] Efficiency and performance audit: app, harness, iOS companion, integrations (report).** <!-- wb-agent-report:107f90439d0942698fe247ccd6b2a45e -->
- **2026-09-24 — CODEX — COMPLETED (PR #563 merged `2051385`) — Bound Android `read_screen` accessibility output (board `7c2aa751d9fe4e7b8293587dd84d6b4a`, issue #555, branch `codex/android-screen-output-budget`, worktree `~/apps/botfleet-codex-android-screen-budget`).**  Long labels and identifiers are clipped, full output is capped at 12 KiB, and each response reports omitted nodes and truncated labels while retaining bounds.  Seven focused tests and all seven required hosted CI contexts passed.  Board claim initially failed while mac-collab was down; retry succeeded.  <!-- wb-agent-report:7c2aa751d9fe4e7b8293587dd84d6b4a -->
- **2026-09-24 - CODEX - COMPLETED - Preserve Antigravity cache-read tokens in BotFleet usage telemetry.**  PR #544 merged `846ce854` after all seven hosted CI contexts passed; issue #542 closed.  Live Usage Monitor/Sentry receipt remains tracked in #559. <!-- wb-agent-report:f4172bb0c8ba44918a21a44228092c30 -->
- **2026-09-24 - CODEX - COMPLETED - Gate BotFleet auto-merge on the complete hosted CI matrix.**  GitHub ruleset 21771270 now requires all seven hosted CI job contexts; fresh GET confirmed preserved deletion, non-fast-forward, and review rules.  PR #544 remains blocked while its platform jobs run.  No Vercel preview gate added. <!-- wb-agent-report:bd921f1a0b1c4b5e80b609ff2180956d -->
- **2026-09-24 - CODEX - COMPLETED - Stop unviewed six-second screen captures during computer-capable turns.**  PR #550 merged `7cc763ba`; all hosted CI passed afterward. <!-- wb-agent-report:f7de459782ce4ddeb802d22a0ad991bf -->
- **2026-09-24 - CODEX - COMPLETED - Diagnose Windows config-lock race timeout under full CI.**  PR #547 merged `cf6470c9` with all seven hosted jobs green. <!-- wb-agent-report:b318a8b6780b4723ac5079953e8add7f -->
- **2026-09-24 - CODEX - COMPLETED - Audit and reduce BotFleet token waste and runtime overhead.**  PR #543 merged `d9ec5da5`; current-prompt and remaining runtime findings tracked separately. <!-- wb-agent-report:93e15d9a55b54f37bfe61403db0f2285 -->
- **2026-09-23 - AG - IN_PROGRESS - Effort-level gating per model, fast engine discovery cache, dynamic avatar fallback logo.** <!-- wb-agent-report:bf789dc6b514415eb1d954162cb578a4 -->
- **2026-09-21 - AG - IN_PROGRESS - Fix Antigravity host policy refusal and unblock model fallback.** <!-- wb-agent-report:2a9a509b4a5547a787126385f67bc1d6 -->
- **2026-09-23 - MM - IN_PROGRESS - Usage Engines Settings Revamp — capability matrix, usage expand, API-vs-sub projection, grok quota (PR #528).** <!-- wb-agent-report:eb774a6a76b04ce29736a2a0c71f7691 -->
- **2026-09-22 - CLAUDE - IN_PROGRESS - [BF][Claude] Voice mode: status review (PR #513 MiniMax-default TTS, Apple STT, Mac verification, DSH scope).** <!-- wb-agent-report:ca46a7d126364023bb5c29d5a7056cf1 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Rewrite MiniMax TTS + avatar against native contract (PR #513 broken: invented OpenAI shape, wrong endpoints, silent ElevenLabs breakage, wrong MIME).** <!-- wb-agent-report:0e0ff08dbfd442f6b8f29e0e78b409b0 -->
- **2026-09-20 - MM - IN_PROGRESS - feat(tts,avatar): default to MiniMax; preserve ElevenLabs as opt-in (PR #513).** <!-- wb-agent-report:24bcc6b6036b46a1aff18579ec02fd0f -->
- **2026-09-21 - AG - IN_PROGRESS - [BF][AG] Fix DSH MiniMax model option serialization (provider minimax).** <!-- wb-agent-report:41b6134a6e9a4c988d6bb5f952c47c48 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Audit defect: sentence-gap regression on Bot Computer Use copy (single space after period, missing second space).** <!-- wb-agent-report:bbe48a9ae62e4c809123487486ea9c59 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Audit defect: ASCII.dev Box defaults over self-hosted VPS, no VPS-setup hint when alias missing.** <!-- wb-agent-report:31636bd1731a4e3dacdb2518a333a1fa -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Audit defect: Settings nav 'Local VM' mislabels the computer-use section (also covers Local Mac + Cloud VPS).** <!-- wb-agent-report:ce2e419406ea4b7cb9e489f2fc89041e -->
- **2026-09-21 - AG - IN_PROGRESS - Stop bot cancels all queued runs and snoozes auto-restart loop.** <!-- wb-agent-report:9c03e8cbcc4c4e5f93794ec36fecbbc8 -->
- **2026-09-21 - AG - IN_PROGRESS - [BF][AG] iOS companion: add 'Open BotFleet on Mac' action to connection banner with fallback error.** <!-- wb-agent-report:88d9275692d94c6da761ca3820e13559 -->
- **2026-09-21 - MM - COMPLETED - MiniMax account balance 402 records only a per-bot cooldown, so each engine bot re-pages (board 555c5227 closed via PR #509 `420ca26b`).**  Polling-path writer `applyMiniMaxBalanceToRegistry(snapshot, registry?)` mirrors `applyAntigravityUsageToRegistry`; registry wildcard `*:${instanceId}:${model}` provides the broadcast shape.  164/164 vitest green across `minimax-balance`, `model-fallback`, `harness/registry`, `quota-window-map`, `antigravity-quota`.  V2 chip-path hardening filed separately on board `12d09a016aab44aa88507c72f3183bf3`. <!-- wb-agent-report:555c5227685c4a2c9f1cdfa87cb68dcb -->
- **2026-09-20 - MM - IN_PROGRESS - MiniMax account balance 402 records only a per-bot cooldown, so each engine bot re-pages.** <!-- wb-agent-report:555c5227685c4a2c9f1cdfa87cb68dcb -->
- **2026-09-19 - AG - COMPLETED - Add @ bot and # app/channel autocomplete, style mentions bolder in prompts/messages.**  Landed in PR #492 (commit d4acd2c5) and deployed via ubf. <!-- wb-agent-report:ace58dcbb79c437fb5ebcfa1ca397b0c -->
- **2026-09-03 — BF-DIRECTOR — OPEN — Owner copy: Title Case chrome + never display &nbsp; as text (board `09e01a3c` docs; Claude `296205dc` settings).  Renderer/settings leak routed to Builder.  No extra-ship.**
- **2026-09-12 - CLAUDE - PLANNED - ask_bot from a room thread fails with 'source thread does not belong to sender' on both the MCP and HTTP lanes.** <!-- wb-agent-report:ce7aba72f40f4d218a325e03e41a9387 -->
- **2026-09-13 — CODEX — COMPLETED — Historical credential alert remediation (#390; board d24c7fd2).**  Isolated `~/apps/botfleet-codex-ios-build-exclusion-20260913` @ `codex/ios-build-exclusion-20260913` excludes Xcode build output.  GitHub alerts remain open with validity unknown; credential replacement/revocation is separately outstanding.  <!-- codex-ios-build-exclusion-20260913 -->
- **2026-09-12 — CODEX — COMPLETED — Complete signed macOS update feed and release automation (#285; board `5a2b2e02bf5c4debbc0559d2009032d0`; branch `codex/release-linux-feed-20260912`, worktree `/Users/jay/apps/botfleet-codex-release-feed-20260912`).**  Source PRs merged: #334 at `4b7d57fe` and Linux feed #395 at `f1050202`.  GROK pickup 2026-09-15: remaining is owner-blocked hosted MAC_CERT_* / RELEASES_PAT plus live publish and installed-device acceptance.  Did not mint certs or keys.  Did not extra-ship.  Board stays in_progress. <!-- wb-agent-report:5a2b2e02bf5c4debbc0559d2009032d0 -->
- **2026-09-08 — BF-COMPILER — COMPLETED — iOS composer: smaller radius, Return not send, pending own-bubble, Steer, suppress in-thread banners, italic desktop placeholders (board `e92b04b5`, branch `compiler/ios-composer`, worktree `~/apps/botfleet-compiler-composer`).**  Capsule composer ballooned into an oval.  Command and mic moved into +.  No TestFlight extra-ship.
- **2026-09-04 — GROK — COMPLETED — Quota rows hide Gemini/Cursor monthly; MiniMax CLI first-tier (board `d4d3343b`, issue #216, branch `grok/quota-breakdown-minimax`, worktree `~/apps/botfleet-grok-quota-breakdown`).**  Claimed Fri, Sep 4, 2026.
- **2026-09-04 — GROK — COMPLETED — Usage Monitor URL and ingest token do not persist in Settings (board `5c0ee072`, issue #191, branch `grok/usage-monitor-save`, worktree `~/apps/botfleet-grok-usage-save`).**  `saveConfig()` omitted `usage`; adding Save + Test Connection.  Claimed Fri, Sep 4, 2026.  PR #192.
- **2026-09-12 — CODEX — COMPLETED — Authenticated build/API identity and Mac update acceptance (#265/#274, boards e4190ce1 / 7e582b82).**  Signed app and detached harness/dependencies installed at `ae8abe7d`; authenticated PID 11581, one SQLite owner, loaded desktop and unsent draft verified.  Newer fixes and full engine/iPhone acceptance remain pending; prior rollback copies retained.  <!-- codex-mac-rollout-20260912 -->
- **2026-09-03 — GROK — COMPLETED — Thread lanes, drag-reassign/merge, hourly TestFlight without tags (board `8aee326915b34c7c8c27a4cdb93fafca`, branch `grok/thread-lanes`, worktree `~/apps/botfleet-grok-followups`).**
- **2026-09-03 — GROK — COMPLETED — Sticky 8799 banner auto-clear, calendar Auto, curl|python3 -c, routine copy (board `6251b088`, branch `grok/sticky-8799`, worktree `~/apps/botfleet-grok-followups`).**  #175 merged (retry, thinking chip, thread reuse).  TestFlight 1.0.29 in flight for the new iOS icon.
- **2026-09-03 — GROK — COMPLETED — Ship trio app icon on iOS and macOS from `botfleet-ios-app-icon-1024x1024.png`; favicon from transparent just-bots (board `a959d7bf`, issue #164, branch `grok/app-icons-trio`, worktree `~/apps/botfleet-grok-icons`).**
- **2026-09-03 — GROK — COMPLETED — Harness 8799 banner, Publisher drain, thinking model chip, routine thread reuse, iOS TestFlight icon (board `6251b088`, issue #174, branch `grok/owner-followups`, worktree `~/apps/botfleet-grok-followups`).**
- **2026-09-03 — GROK — COMPLETED — Drop Polar support-payment links; rewrite README as BotFleet add-ons (all in testing) then OpenMausBot fork features (board `1af79606`, issue #160, branch `grok/readme-no-support-payments`, worktree `~/apps/botfleet-grok-readme`).**  GitHub About updated; social preview is owner-set and left alone.
- **2026-09-03 - CLAUDE - COMPLETED - Sidebar overhaul: room terminology incl. custom term (web + iOS), Title Case settings entries + "Team Map", renameable task threads nested under channels with drag-reassign, collapsible channels showing N recent threads + "More Threads...", and thread-content search.**  Board `296205dc`.  Branch `claude/sidebar-terminology-threads`, worktree `~/apps/botfleet-claude-sidebar`.  Owner-requested 2026-09-03.  Landed: #156 terminology presets + custom word + Title Case; #158 nested conversations, rename, drag, collapse, More Threads, search surfaced; #162 right-click move to channel/bot + iOS resolved labels; #163 File menu export/import + Windows ui-copy fix.
- **2026-09-01 - GROK - COMPLETED - Product reach: iPad, chat files, APNs wake, iMessage LaunchAgent, Vercel 1/hour, desktop release host.**  Board `b43584ea` `a9683ae2` `4677da28` `af8f2776` `02ca3c98` `80dd2680` `9051c3ac`.  Issue #107.  Worktree `~/apps/botfleet-grok-reach` @ `grok/product-reach`.
- **2026-09-01 — GROK — COMPLETED — Add fleet sentry-ci-report.yml + scripts/sentry-ci-report.py (branch `grok/sentry-ci-report`, worktree `~/apps/botfleet-grok-sentry-ci`, board `e70a89f7`).**  Gold copy UM PR #1394.  APP=`botfleet`.  Fingerprint `[ci-failure, botfleet, workflow]`.  <!-- wb-agent-report:e70a89f7 -->
- **2026-09-01 - GROK - COMPLETED - Implement 2026-09-01 delta audit batches. Worktree ~/apps/botfleet-grok-delta @ grok/delta-audit-fixes.**  Board `9e922f65`.  Fallbacks, honesty/docs, data/permissions, companion trust.  Electron #91 and iOS ATS #92 already merged.
- **2026-09-01 — GROK — COMPLETED — Delta-audit Batch 2: iOS ATS rollback + light-first + iOS truth (branch `grok/delta-audit-ios`, worktree `~/apps/botfleet-grok-delta-ios`).**  Board `95e445e5` `a9683ae2`.  Remove `NSAllowsArbitraryLoads` and `botfleet.app` cleartext; keep local networking + `ts.net`; `preferredColorScheme(.light)`; no APNs.  I2 chat image composer remains a documented gap.  GROK note 2026-09-01: ATS rollback landed as #92 on main; this row is the remaining iOS-truth slice.
- **2026-09-01 — GROK — COMPLETED — Sentry production deploy records (`sentry-cli releases deploys new -e production`) (branch `grok/sentry-deploys`, worktree `~/apps/botfleet-grok-sentry-deploys`, board `2d1c8565`).**  Additive workflow for `botfleet` project on CI success for `main` push.  VERSION = full git SHA.  Soft-fail.
- **2026-09-01 — GROK — COMPLETED — Sentry fleet adoption: Vercel `VITE_SENTRY_DSN`, User Feedback widget, harness gen_ai agent spans (conversation/tool/model/tokens/errors, no prompts) (branch `grok/sentry-fleet-adoption`, worktree `~/apps/botfleet-grok-sentry-adopt`).**  Board `d99cee7f21ad4a2ba6f74b50c85fda04`.  Rollout: `docs/rollouts/2026-09-01-sentry-fleet-adoption.md`.
- **2026-09-01 — GROK — COMPLETED — Companion trust leftovers C3/C4/D2 (`grok/delta-companion-trust`).**  Board `149843e8`.  Pairing replay after revoke, phone always-allow/authorize deny, DSH `--mcp` quoting.  Merging into `grok/delta-audit-fixes`.
- **2026-08-31 - GROK - COMPLETED - Top-to-bottom full-stack audit (web all sizes, native Electron+iOS, backend).** Mac ~/apps/botfleet-grok-audit @ grok/full-stack-audit. Board f5abf14d. Issue #22. <!-- wb-agent-report:f5abf14dd6904945ad3219d5e7964d3d -->
- **2026-09-01 - CLAUDE - COMPLETED - BotFleet top-to-bottom analysis v2 (Mac app, iOS app, site, fleet-procedure integration) + reconcile with owner Delta Audit docx.**  Board `781554fd`.  GROK correction 2026-09-01: finder journal salvaged; report is GROK's.  Row left in place.
- **2026-09-16 - BF-MONITOR - PLANNED - [BotFleet][BF-MONITOR] Two required main CI compile reds — #447 macos local-vm fence race, #450 windows config-lock timeout (suspected OS test races).** <!-- wb-agent-report:3c6f4f9352954b85bdd823a6f1c13ec5 -->
- **2026-09-13 - CLAUDE - COMPLETED - Remove stray tracked root scripts and the OpenMausBot mascot preview page.** <!-- wb-agent-report:a1a56bdb06e34c329a983d3bdd7f8a32 -->
- **2026-09-03 - CLAUDE - COMPLETED - Sidebar overhaul: room terminology (incl. custom), Title Case settings + Team Map, renameable task threads nested under channels with drag-reassign, collapsible channels with N recent threads, content search.** <!-- wb-agent-report:296205dc243548ab9065d2062c2230ac -->
- **2026-09-04 - MINIMAX - COMPLETED - settings-rev2-C: general settings (webhooks enable+save+test, max-turn band 1m-24h, auto-update 6h throttle + Mac fingerprint) (board `810e6dac`, branch `minimax/settings-rev2-c`, worktree `~/apps/botfleet-minimax-c`).**  Lane C of the settings revamp.  Builds on #217 + parent's settings-rev2 umbrella.  CustomIngressFields gains enable/save/test; new POST /api/ingress/test confirms the URL answers and names a Cloudflare Tunnel / Caddy target; max-turn range confirmed 1-1440 minutes with a recommended 5-60 minute band surfaced in copy; auto-update rate-limited to 6h via `autoUpdate.lastCheckMs` in config, plus a Mac app-bundle fingerprint check (`/Applications/BotFleet.app/Contents/Info.plist` CFBundleVersion); UpdatesRow subtitle advertises the 6h cap; UpdateNotificationsRow left intact.  PR #219 squash, auto-merge armed.  CI: typecheck+test running on mac/ubuntu/windows, control-plane + iOS Swift + package+smoke in flight.
- **2026-09-19 - MM - COMPLETED - [MM] quota dual-window: route non-MiniMax engines through DRIVER_KIND_PROVIDERS.**  Landed in #481 (squash c1bf6aa8). Fix: server/harness/registry.ts:418 was building providerKey from inst.driverKind, but quota windows are tagged with canonical provider keys ('anthropic', 'openai', 'xai'). Switched to quotaProviderForDriver() from server/quota-window-map.ts. Sentry thread PRRT_kwDOUHUvas6j6pZT (CRITICAL) resolved via GraphQL. 39/39 server/harness/registry.test.ts pass. CI green on macos/ubuntu/windows. <!-- wb-agent-report:ebecfe8e9adb4b6a9df61a5e784c8dc1 -->
- **2026-09-19 - MM - COMPLETED - BotFleet: fleet-user_id Composio integration (MM personal-agent lane).**  Landed in #498. Fix-add: persist mcp_url alongside session_id so --check-only returns the API-issued URL verbatim, not a guessed template. <!-- wb-agent-report:dd16c6ab9165468b88036807b80d4857 -->
- **2026-09-19 - MM - COMPLETED - [MM] iOS Settings: render push-sender health from /api/companion/push-health (#489).**  Landed in PR #489 with follow-up PR #496 (BotFleet iOS app build fix — un-private SettingsIcon). <!-- wb-agent-report:c3d62588291f4ae3ba98dcc92d26fee0 -->
- **2026-09-19 - MM - COMPLETED - [MM] ubf: skip when local checkout is already at origin/main; BOTFLEET_FORCE=1 to override (#482).**  Landed in PR #482 (BotFleet, commit e126cb17). Tracked scripts/update-botfleet.sh has the same logic. Local helper ~/apps/update-botfleet.sh is also patched for immediate effect. <!-- wb-agent-report:084f91f32e3141078fb6231e2bd2afa5 -->
- **2026-09-17 - AG - COMPLETED - Fix Sentry DSN Infisical propagation and local packaging.**  Landed in #462. <!-- wb-agent-report:853b773b8a3a4e1f915c02dfed7e2032 -->
- **2026-09-17 - AG - COMPLETED - Fix agy stdin prompt delivery and rebuild dist-server.**  Landed in PR #461. <!-- wb-agent-report:3f3b419c1d064b85adfe1b48c809c757 -->
- **2026-09-16 - CLAUDE - IN_PROGRESS - MiniMax connectability follow-ups from the post-merge review of #387: endpoint badge hides the local config host, JSX sentence gaps collapse, unlisted driver labelled OpenAI-compatible, added-connection remedy names a control that does not exist, explicit global host overridden by the local config.** <!-- wb-agent-report:0b92ea0b5a374b8f9e02fb20607797d9 -->
- **2026-09-16 - CLAUDE - IN_PROGRESS - Mac update control follow-ups from the post-merge review of #382: check holds its own admission so Install hides after a check, run refusals report a busy Mac, stale progress never settles, zero-ahead offers, unhandled spawn error, unconfined log paths, toolbar install silently checks, stale hook error, up-to-date tick after a failed check, banner copy without reasons, docs promises.** <!-- wb-agent-report:264af3ddadce4187a4cb022f01b4c497 -->
- **2026-09-16 - CLAUDE - IN_PROGRESS - Push hardening follow-ups from the post-merge review of #383: drain drops an alert on a key fault, thrown fetch gets no retry, unguarded drain can crash the sidecar, key guard by object identity, stranded queues after a key fault, hello cursor adopted on a resumed stream, closed-app pushes delivered twice on iOS, lock-screen answer failure raises the modal alert.** <!-- wb-agent-report:86655204743144ac9bfb2116088d5089 -->
- **2026-09-16 - CLAUDE - IN_PROGRESS - Quota parity follow-ups from the post-merge review of #388: MiniMax balance block discards the driver's own capped verdict, weekly reset shown as a bare clock time, key re-resolved live instead of the captured one, balance host heuristic, Spent line printed twice, near-cap outranks unavailable.** <!-- wb-agent-report:e820540be87843dc830376eff0e6e883 -->
- **2026-09-16 - CLAUDE - IN_PROGRESS - iOS Mac Update card follow-ups from the post-merge review of #381: global alert on offline or older Mac and on every poll tick, unbounded install poll, millisecond timestamps never parse, duplicated 409 sentence, cancellation alert, TestFlight throttle and persistence, status-text case.** <!-- wb-agent-report:559bb1d654254d6896891e56a5ed2556 -->
- **2026-09-16 - CLAUDE - IN_PROGRESS - Mac updater relaunch loop: launchctl submit keeps the failed update job alive, so a build that cannot download Android platform tools reran 204 times and left 344 stage dirs (2.8 GB).** <!-- wb-agent-report:2c42d6b243f74cd6a3588b6111af1c06 -->
- **2026-09-04 - CLAUDE - PLANNED - claude driver: 'exited 1 before result' with a wall of claude.ai connector tool names.** <!-- wb-agent-report:513597095c5540338e511ea6ef1ebb7d -->
- **2026-09-04 - CLAUDE - PLANNED - Box cloud desktops pool 4 ways: two bots silently share one VM/mouse/Chrome profile, and Sleep archives a peer's live VM.** <!-- wb-agent-report:dc91bbc0eb6a41baadf99001ef6a378d -->
- **2026-09-15 - AG - COMPLETED - Engine capability parity: toolLoop for Grok/OpenAI, workspace file tools for HTTP, and DSH MCP.** <!-- wb-agent-report:db44b21924ee488c81aa4c96d88bb02c -->
- **2026-09-15 - AG - COMPLETED - MiniMax models on DSH with native MiniMax branding and model catalog expansion.** <!-- wb-agent-report:f978874c66304ca8b0c17fe3e7677098 -->
- **2026-09-01 - GROK - COMPLETED - Vercel auto-deploys skip unless site files changed, plus 1/hour (branch `grok/vercel-site-watch`, worktree `~/apps/botfleet-grok-vercel-watch`).**  Board `46837afd`.  Script `apps/site/vercel-ignore-hourly.sh` watches `apps/site` so iOS/docs commits do not ship botfleet.app.
- **2026-09-01 — GROK — COMPLETED — Pickup CLAUDE cap: BotFleet analysis v2 (`claude/analysis-v2`).**  Board `781554fd`.  PR #97 squash-merged `e3dfce4`.  Report `docs/audits/2026-09-01-botfleet-analysis-v2.md`.  Raw 238 / Claude tech-confirmed 144 / unique P0 still open 5.  No product code.
- **2026-09-03 — GROK — COMPLETED/MERGED #155 (`51a69366`) — Pickup AG usage cap: display-aware transcript window, Safari-style thread tabs, Live Activities unread + widgetURL, iOS terminology and collapsible sections (board `32712701`, branch `grok/bf-ag-pickup-ui`).**  GitHub CI green.  Deploy-all was on the active path; missing UI was activity crowding the raw-row window.  Rollout: `docs/rollouts/2026-09-03-ag-pickup-ui.md`.
- **2026-09-03 — GROK — COMPLETED — APNs killed-app wake, desktop 1.0.30 aligned with iOS.**  PR #184 merged `ff35d759`.  Board `f7d09c8b`.  Issue #183.  Release dispatched as draft; GH missing MAC_CERT/APPLE_API/RELEASES_PAT (board `5a2b2e02`). <!-- wb-agent-report:f7d09c8ba41c4be8b2e413c8bd31799b -->
- **2026-09-03 — AG — COMPLETED — DSH driver hardening: fallback error classification, DSH_HOME-aware auth, honest model-set (branch `ag/dsh-first-class`, PR #189 auto-merge armed, board `9effb254`, issue #188 for the dsh acp backend gap).**
- **2026-09-03 — GROK — COMPLETED — MiniMax review hygiene: delete congress compose.yml, gitignore patch junk, Windows path tests, require latest-mac.yml.**  PR #182 merged `f16255d0`.  Board `41169a1e`.  Owner: defer Windows/Linux first-class; make APNs; keep all in testing; file splits only if no behavior change; patch scripts stay on disk gitignored; align 1.0.x; full Mac release with latest-mac.yml + zips.
- **2026-09-03 — GROK — COMPLETED — iOS opaque chat chrome, compose menus, Simple/Projects workspace modes, room crop, quota Available fix.**  PR #181 merged `18e66cdd`.  Board `4498c349`.  Issue #180.  GitHub TestFlight job lacks Apple API secrets; Mac wrapper is the ship path.
- **2026-09-12 — CODEX — COMPLETED (PR #397 merged `6e275b29`) — Throttle highlighting during streamed replies (#353; board 001dfbb5).**  Live reply bubble restored; streaming flag passed to ChatMarkdown; highlight once after completion.  Issue #353 closed.  GROK pickup 2026-09-15 closed board `001dfbb5`.  <!-- codex-streaming-highlight-20260912 -->
- **2026-09-13 — CODEX — COMPLETED (PR #398 merged `f4b1fe1f`) — Composio connected-account readiness source (#270).**  Typed credential/transport/upstream status, last-success evidence, scoped-key 403 fallback.  GROK pickup 2026-09-15 closed board `0ade868c`.  Remaining live engine tool-call acceptance stays on issue #270 / #274.  <!-- wb-agent-report:0ade868cdc2a4593be27ce1e6c1e46dd -->
- **2026-09-15 — GROK — COMPLETED (PR #437 squash `5e677709`) — recall-cli Test Connection deadline (#271, board `2d627f55`, branch `grok/recall-cli-deadline`).**  `RECALL_SKIP_PRIVATE=1` skips Tailscale.app and the 120s private Qdrant path.  AFC #225 `40dc75a` treats CLIError 3 as down (public twin).  Live GET /api/recall/status ready in 6.0s.  Issue #271 closed completed.  No extra TestFlight.
- **2026-09-12 — CODEX — COMPLETED — Fleet RAG routing and readiness (#271/#272, boards 2d627f55 / 432930fc).**  PR #327 merged at `3a02f23a`; #272 completed.  Remaining CLIError 3 / 120s hang closed by GROK AFC #225 `40dc75a` + BotFleet #437 `5e677709`.  Issue #271 closed completed.  <!-- codex-recall-readiness-20260912 -->
- **2026-09-15 - DSH - PLANNED - UM #1468 ships Usage Local Monitor, contradicting the 'LocalUsageMonitor stays skipped' policy.** <!-- wb-agent-report:7c61bc8a0d2d48a9bcc98a0eca2d8041 -->
- **2026-09-15 - GROK - COMPLETED - Reopen accidentally closed unique-work PRs and land remaining BotFleet PRs.** <!-- wb-agent-report:3cda81df1b6e47b28ab166ea84e50f5c -->
- **2026-09-13 - CODEX - COMPLETED - Durable BotFleet usage telemetry outbox.** <!-- wb-agent-report:82ab4e6dbdf142dabdbc159cee29eb99 -->
- **2026-09-12 - CODEX - COMPLETED - [P1] Require Auto consent for inherited host computer grants.** <!-- wb-agent-report:1c63f177953c4b939a5a6095a62beb61 -->
- **2026-08-31 - CLAUDE - PLANNED - iOS batch1 blocker: PATCH profile modelSelection will 400 — BOT_PROFILE_PATCH_FIELDS omits it (strict parse) and sidecar allowlist gaps (groups PATCH, avatarUrl on Room).** <!-- wb-agent-report:d261ef00c0ab415aa58dc9facb924691 -->
- **2026-09-15 — GROK — COMPLETED (PR #348 squash `b3be62cd`) — Make routine calendar timezones explicit (issue #337; boards `d308d7a8` `5ecf195a`; branch `codex/calendar-timezone-20260912`, worktree `~/apps/botfleet-codex-calendar-timezone-20260912`).**  Calendar placement, headers, date inputs, and next-run labels use America/Chicago; daily recurrences carry optional IANA zones; legacy zone-less routines stay host-local.  Required CI green: typecheck+test ubuntu/macos/windows, control-plane, package+smoke, Swift tests+iOS.  Issue #337 closed completed.  Signed Mac rollout remains separate.  Codex authored; Grok closeout after cap.  <!-- codex-calendar-timezone-20260912 -->
- **2026-09-13 - CODEX - PLANNED - Display Usage Monitor subscription pools and reset times in BotFleet.** <!-- wb-agent-report:0c8fbb1a8fe94007838f4bb8ece68239 -->
- **2026-09-15 — GROK pickup — COMPLETED Codex cap boards whose PRs already merged (pickup `5794a1c8`).**  Closed `7326161c` #393 `5a425b79`, `001dfbb5` #397 `6e275b29`, `0ade868c` #398 `f4b1fe1f`, `99b43d34` #403 `f7758839`, `2784c3c7` #404 `d2b68473`, `f16194b9` #331/`#293` `c6ef1103`, `66958de4` #350 `dff6af37`, `699ea1ae` #326 `1a8b7412`, `d308d7a8` #348 `b3be62cd` (other workers landed #348).  Left in_progress: `5a2b2e02` owner-blocked MAC_CERT_*/RELEASES_PAT, `d24c7fd2` P0 BF-PLUMBER, plus other-owned open PRs #401 and #402.  `2d627f55` / #271 closed via AFC #225 `40dc75a` + BotFleet #437 `5e677709`.  Did not push.  Did not edit BotFleet git trees.
- **2026-09-13 — CODEX — COMPLETED (PR #350 merged `dff6af37`) — Board reconciliation audit (#286; board 66958de4).**  Audit parser handles plural/slash/ranged PR references; historical metadata corrections preserve peer ownership.  GROK pickup 2026-09-15 closed board `66958de4`.  Broader live duplicate effort-row hygiene remains on issue #286.  <!-- codex-board-reconciliation-20260912 --> <!-- wb-agent-report:66958de400b04f9892a2bc9a14c71a09 -->
- **2026-09-13 — CODEX — COMPLETED (PR #404 merged `d2b68473`) — Native DeepSeek Harness ACP integration (issue #188; board `2784c3c7`; branch `codex/dsh-native-acp-20260912`, worktree `~/apps/botfleet-codex-dsh-native-20260912`).**  The driver uses the published `dsh --profile acp` transport with standard MCP, native resume, verified opaque model/reasoning selection, strict stock-version readiness, cancellation-safe preflight reservation, and credential-free version probes.  Issue #188 closed.  GROK pickup 2026-09-15 closed board `2784c3c7`.  No live DSH install. <!-- wb-agent-report:2784c3c740204d1db5a26f67cc784d06 -->
- **2026-09-13 — CODEX — COMPLETED (PR #403 merged `f7758839`) — OpenAI-compatible tool loop (#355), handed over from CLAUDE MM PR3.**  Shared approval-aware loop replaces the legacy executor.  Issue #355 closed.  GROK pickup 2026-09-15 closed board `99b43d34`.  <!-- wb-agent-report:99b43d34fbdd450894bc18edf3c6a9c0 -->
- **2026-09-13 — CODEX — COMPLETED (PR #397 merged `6e275b29`) — Streaming reply visibility and reconnect recovery (#353).**  Active code renders without invoking highlighting; completion invokes it once.  GROK pickup 2026-09-15 closed board `001dfbb5`.  <!-- wb-agent-report:001dfbb5126b4f2c82ac8c38166249b4 -->
- **2026-09-13 — CODEX — COMPLETED (PR #393 merged `5a425b79`) — Codex model catalog refresh (#391).**  Current CLI catalog remains authoritative; the fallback offers current coding models and preserves stored retired or provider-qualified selections.  Issue #391 closed.  GROK pickup 2026-09-15 closed board `7326161c`.  <!-- wb-agent-report:7326161cab684a25a12e9a686f3aa5ec -->
- **2026-09-15 — GROK — COMPLETED (PR #348 squash `b3be62cd`) — Make routine calendar timezones explicit (issue #337; boards `d308d7a8` `5ecf195a`; branch `codex/calendar-timezone-20260912`, worktree `~/apps/botfleet-codex-calendar-timezone-20260912`).**  Calendar placement, headers, date inputs, and next-run labels use America/Chicago; daily recurrences carry optional IANA zones; legacy zone-less routines stay host-local.  Required CI green: typecheck+test ubuntu/macos/windows, control-plane, package+smoke, Swift tests+iOS.  Issue #337 closed completed.  Signed Mac rollout remains separate.  Codex authored; Grok closeout after cap.  <!-- codex-calendar-timezone-20260912 -->
- **2026-09-14 - AG - COMPLETED - Bot ↔ Bot heading and roster section parity across Mac and iOS.**  Landed in #422 <!-- wb-agent-report:9a9f4cef8c544f8fa7b67bc40f74787d -->
- **2026-09-13 - CLAUDE - COMPLETED - MiniMax connectability: vault and config.json key sourcing, encrypted store, second instance from the app (parity G3+G6).** <!-- wb-agent-report:f59883b6952e45c2b703fe6e2133b81b -->
- **2026-09-06 - BF-DIRECTOR - PLANNED - Settings copy lies: MiniMax labeled CLI and pointed at DSH for tools.** <!-- wb-agent-report:77582325672347b581c9bd2b62215046 -->
- **2026-09-04 - CLAUDE - PLANNED - Local VM panel hardcodes perBot=false and its lifecycle acts on SHARED_LOCAL_VM_TARGET while config mode is per-bot.** <!-- wb-agent-report:47a0a78cd20642b19525e46174080259 -->
- **2026-09-06 - BF-DIRECTOR - PLANNED - Grok Agent: images off for whole engine; auth checks HOME not GROK_HOME.** <!-- wb-agent-report:abbcca7a9a9547cfba60e9370c73b156 -->
- **2026-09-14 - AG - COMPLETED - Fix desktop local updater exit 127 (Homebrew PATH) and iOS Live Activity main-thread hang (BOTFLEET-E).**  Landed in #414. Mac desktop exits 127 are fixed by injecting node paths. iOS Live Activity hang BOTFLEET-E fixed by shifting XPC call off MainActor. <!-- wb-agent-report:ffc71298b94340ba856dda553b5ad090 -->
- **2026-09-13 - CLAUDE - COMPLETED - Quota parity: MiniMax balance and cap status, window mapping for six engines, dual-window labels for every engine (parity G11+G12+G13).** <!-- wb-agent-report:a72269bdfec84418a6fd2405035c75cc -->
- **2026-09-13 - CLAUDE - COMPLETED - iOS: Mac Update card with remote install, and a newer-TestFlight-build notice.** <!-- wb-agent-report:5d5d55b75e404a688ebef504af0d6a28 -->
- **2026-09-13 - CLAUDE - COMPLETED - Push hardening: time-sensitive kinds, retries, cached provider token, sender health, lock-screen Approve/Deny.** <!-- wb-agent-report:761055f9973348a9b94369c207bb9512 -->
- **2026-09-13 - CLAUDE - COMPLETED - Check and install Mac updates from the app and the phone: harness /api/update routes, detached launcher, desktop UI.** <!-- wb-agent-report:baf4f722e2944d568adffeb41b23feb7 -->
- **2026-09-13 — CODEX — COMPLETED (PR #405 merged `18448f9c`) — Correct DeepSeek API rates and billing-window display (#396; board `beb8234081e546fd846143581e111226`).**  DeepSeek V4.1 Flash and V4 Pro now show official off-peak–peak input, cache-read, and output ranges with UTC windows and dated source copy; API reference estimates remain separate from provider charges and subscription limits.  Six pricing regressions, a rendered UsageSection proof, and the complete seven-job hosted CI gate passed.  No DSH driver, subscription, credential, or live deployment changes.  <!-- wb-agent-report:beb8234081e546fd846143581e111226 -->
- **2026-09-13 - AG - COMPLETED - Fix chat bottom scroll with attachments and move Sentry bug report off send button.** <!-- wb-agent-report:df0a3beec1b642d7979394c4e96b6cd6 -->
- **2026-09-13 — CODEX — COMPLETED (PR #356 merged) — Prevent successful replies from triggering quota cooldown/fallback (issue #351; board `79fa2368`; branch `codex/quota-classifier-evidence-20260912`, worktree `~/apps/botfleet-codex-quota-classifier-20260912`).**  Quota fallback now requires structured provider evidence or a strict terminal chip; ordinary success prose about subscription, timeout, HTTP 429, and request limits remains a successful reply.  All 62 focused classifier tests and the complete hosted macOS, Ubuntu, Windows, package, control-plane, and Swift/iOS gates passed at `dd01b19f`.  Source/tests only; no live config or runtime changes. <!-- wb-agent-report:79fa2368ca1447afae7015b619d7109c -->
- **2026-09-13 — CODEX — COMPLETED (PR #352 merged) — Sentry diagnostics reliability (issues #341–#343; boards `13008da2`, `c5217e2b`, `a0794219`; branch `codex/sentry-diagnostics-reliability-20260912`, worktree `~/apps/botfleet-codex-sentry-diagnostics-20260912`).**  The packaged renderer waits for the saved diagnostics switch before starting Sentry, clearing the visible environment restores the default, and same-turn runtime/completion failures produce one issue while preserving setup/cancellation suppression and distinct later turns.  The final 14/14 renderer tests and TypeScript passed, and the complete hosted macOS, Ubuntu, Windows, package, control-plane, and Swift/iOS gates passed at `cdf4eb04`.  Source and tests only; no live runtime changes. <!-- wb-agent-report:13008da2f66841f295b3bb7eead7ec94 -->
- **2026-09-13 - CLAUDE - COMPLETED - Provider parity audit (MiniMax first tier; matrix across functionality, telemetry, quota/subscription, autonomy, connectability) — read-only, feeds lanes.** <!-- wb-agent-report:2ca7e5db51c5417289cb2c7f0cf72959 -->
- **2026-09-13 - CLAUDE - COMPLETED - APNs and Live Activities audit: end-to-end push wiring, LA lifecycle, BOTFLEET-E hang, better uses.** <!-- wb-agent-report:c871445c9268425c85a91dc4ccd8416c -->
- **2026-09-13 - CLAUDE - COMPLETED - Bot roles brainstorm note for the owner: current roster, tuned variants, alternative role cuts, alternative bot definitions.**  Note published: [BF, Claude] Bot roles brainstorm. <!-- wb-agent-report:c9d5b0cb9ae44b43bc6040cd84135abd -->
- **2026-09-13 - AG - COMPLETED - Dual-window quota display (5hr/Week), DeepSeek balance and rolling spend.** <!-- wb-agent-report:137f0c87ef8a4cd085edf126a7f9e824 -->
- **2026-09-13 - CLAUDE - COMPLETED - Mac updater hardening after post-merge review of #371: stamped stage rollback path, vnode-based survivor detection, bundle-internal daemons, orphan and installing-receipt reconciliation.** <!-- wb-agent-report:7e929296f5104175a2cd6f23f13ad158 -->
- **2026-09-13 - CLAUDE - COMPLETED - Bound native/events transcript growth under ~/.botfleet (1.9 GB per-thread ndjson files).** <!-- wb-agent-report:71499ed63b5d48599e1ebb60edfffe92 -->
- **2026-09-13 - CLAUDE - COMPLETED - Tests leave orphaned harness node children (26 GB resident) — process-group kill on teardown.** <!-- wb-agent-report:acfc0c9f197e4629abb2473611bad0d1 -->
- **2026-09-13 - CLAUDE - COMPLETED - Boot error page: text only, Try Again relaunch link (owner request after #359).** <!-- wb-agent-report:f95739cb34784d399347a92a44412bce -->
- **2026-09-13 - CLAUDE - COMPLETED - Mac updater: keep rollback bundles out of /Applications, prune after verification, no rollback-named running app.** <!-- wb-agent-report:859bcdf085524ba99ee1e54bfbdb27ae -->
- **2026-09-13 - CLAUDE - COMPLETED - Claude driver passes --permission-prompt-tool mcp__ogb__approve without the proxy registered (Sentry BOTFLEET-8).** <!-- wb-agent-report:014bfe35001347df841d1df798da494f -->
- **2026-09-13 - CLAUDE - COMPLETED - Per-thread transcript ndjson logs grow without bound (1.9 GB on the owner's Mac).**  Duplicate of 71499ed6; PR #373 tracked there. <!-- wb-agent-report:cf8a05167ee44f74b8dd864f1ffe3e15 -->
- **2026-09-06 - GROK - COMPLETED - Settings Remote Access UX for named tunnel botfleet.jays.services.** <!-- wb-agent-report:a4b7edb03bde406ab94bf8e386d59e79 -->  CODEX #286 verified completion in PR #227; this prior claim is retained as history.
- **2026-09-12 - GROK - COMPLETED - Map Remote Access Test Connection pill to product copy.**  Implemented by GROK in #308 (7d5a7cd8f9c03e1da93ff0771f1d9460fd99b605).  CODEX #286 reconciliation verified Reachable / Could not reach / timeout mapping and matching Test Connection accessible name in current source.  No new runtime deployment claimed. <!-- wb-agent-report:f3c03a1060cd4be1b21c83b0b4ef4e67 -->  CODEX #286 verified completion in PR #308; this prior claim is retained as history.
- **2026-09-12 — CODEX — COMPLETED — Routine outcome reliability (#284, board c0f364ea).**  Worktree `~/apps/botfleet-codex-routine-outcomes-20260912` @ `codex/routine-outcomes-20260912`.  Combined receipts now retain and share actual execution outcomes; safe causes/phases, actual engine identity, and per-routine seven-day metrics distinguish cancellation/denial from failure.  Typecheck, 54 focused tests, isolated renderer screenshots, peer corrections, and full gate passed (3,561 Vitest, 19 skipped, all chained suites).  PR #335 merged at 71d932c0; signed rollout remains pending #274.  Active-history retention and calendar timezone follow-ups are #336/#337.  <!-- codex-routine-outcomes-20260912 --> <!-- wb-agent-report:c0f364eab041404aa6bba35afc9a5bbf -->

- **2026-09-12 — CODEX — COMPLETED — Credential restoration and authenticated alert delivery (#269/#270/#273/#329, PR #333).**  Merged as `cdba649d`.  Durable nonsecret presence markers and authenticated replay gate only consumers of missing encrypted credentials; interactive, room, fallback, boot-recovery, and routine work resumes once without exposing keys.  Candidate credential preparation is metadata-only and rollback-safe.  The full source gate passed 3,617 Vitest tests with 19 skipped except one corrected obsolete source-name assertion, then all related and chained suites passed; every final hosted Mac/Linux/Windows, Linux package, control-plane, Swift/iOS, and Vercel check passed.  Signed live credential acceptance remains under #274.  <!-- codex-integrations-20260912 -->
- **2026-09-12 - CODEX - COMPLETED - Replay encrypted workspace credentials when desktop attaches to the always-on harness.**  Landed in PR #333 at `cdba649d`; signed live credential acceptance remains under #274. <!-- wb-agent-report:eebedc2e674b442d99ae3e71aff1eccd -->

- **2026-09-01 — BF-DIRECTOR — COMPLETED — Quota/session-limit must fail over even after tools and in rooms (branch `grok/quota-cap-fallback`, worktree `~/apps/botfleet-director-quota-fallback`).**  Board `96f2ec55`.  Issue #118.  Owner: anytime a bot hits usage cap/quota, use the saved fallback model.  <!-- wb-agent-report:96f2ec55370d437bb68cef79049fb0d1 -->
- **2026-09-02 - CLAUDE - COMPLETED - B9 computer-subsystem tests red after #117/#122 (26 failures) — make pnpm test green.** <!-- wb-agent-report:3307a1db9a744b4d99b002c1c9067255 -->
- **2026-09-04 - CLAUDE - PLANNED - Flaky: 'packaged CUA runtime files remained after Electron quit' fails package+smoke.** <!-- wb-agent-report:4616df224ad44aa89ec732bfe31f62e9 -->
- **2026-09-06 - BF-DIRECTOR - PLANNED - Engine picker conflates two Groks and two DeepSeeks.** <!-- wb-agent-report:06d8d7dd613f4c14929f6b095663b203 -->
- **2026-09-07 - CLAUDE - PLANNED - iOS chat header shows no logo for the model currently in use.** <!-- wb-agent-report:6b4b2da481bc448389398405ae18e770 -->
- **2026-09-08 - BF-DESIGNER - COMPLETED - Sidebar: reorder contexts, Bot Chats, recency, engine logos, merge threads; RAG timeout.** <!-- wb-agent-report:2d627f55356c4795ac0679e0c054bdbc -->
- **2026-09-08 — BF-FIXER — COMPLETED — Antigravity quotas Gemini + Third-Party only; Bot Profile avatar drag-drop (board `2bf6c493`, branch `fixer/quota-gemini-avatar-drop`, worktree `~/apps/botfleet-fixer-quota-avatar`).**  Two shared numbers, not per-model rows.  Avatar box accepts GIF, HEIC, BMP, SVG and existing formats.  No TestFlight. <!-- wb-agent-report:2bf6c493a56943768e4b32185599af7e -->
- **2026-09-09 - CLAUDE - PLANNED - Fleet acknowledgement dialog hides which/how many bots get Auto host control.** <!-- wb-agent-report:1156a06d643f4b39a2a32c47928f84ef -->
- **2026-09-09 - CLAUDE - PLANNED - VM mode switch races with bot DELETE - no fence, partial cleanup errors.** <!-- wb-agent-report:7c2db2d7b9de489ea3d300f673b77d60 -->
- **2026-09-09 - CLAUDE - PLANNED - observability-config.ts: clearing environment field does not clear stored value.** <!-- wb-agent-report:c5217e2bf42f432a8c89c62045c30725 -->
- **2026-09-09 - CLAUDE - PLANNED - sentry-ai.ts: one failed turn reports two Sentry exceptions.** <!-- wb-agent-report:a079421914ab4a73b15cabc98e2f3582 -->
- **2026-09-02 — BF-PLUMBER — COMPLETED — Live-test BotFleet engines + official quota-chip corpus (branch `grok/quota-chip-corpus`, worktree `~/apps/botfleet-plumber-quota-corpus`).**  Board `a59fcff2`.  Expand matcher to Codex/Cursor/Claude/Gemini/Kimi/DeepSeek official chips.  Near-cap alert: `@Plumber near-cap <engine>`.  <!-- wb-agent-report:a59fcff213d4440490fd53b32891f2da -->
- **2026-09-12 - CODEX - PLANNED - [P2] Unify routine calendar and outcome timezones.** <!-- wb-agent-report:d308d7a8ecee443490e6bd7a4561ca46 -->
- **2026-09-12 - CODEX - PLANNED - [P2] Preserve active routine receipt groups during history pruning.** <!-- wb-agent-report:f34fe9d7fece46bba54d7d6fe77b3497 -->
- **2026-09-12 — CODEX — COMPLETED — Preserve active routine receipt groups (#336, PR #347; board f34fe9d7).**  Merged as `6bc0ac68` with all seven hosted CI jobs green.  Active groups and unsettled confirmation results survive the 2,000-record terminal tail; durable reload/retry behavior passed peer review and the full local gate (3,588 Vitest tests, 19 skipped, all chained suites).  Signed rollout remains in #274.  <!-- codex-routine-retention-20260912 -->

- **2026-09-12 - CODEX - COMPLETED - Remove stale one-off root patch scripts and compiled artifact (#320).** <!-- wb-agent-report:e24dac98660b49078c01ec98d5e621de -->
- **2026-09-12 — CODEX — COMPLETED — Engine resume, deadline, and VPS lease safety (PR #324; merge `c9807099`; issues #280, #281, #321; boards `89a63172`, `689d0f4e`, `6ca81ed5`; branch `codex/engine-resilience-20260912`, worktree `~/apps/botfleet-codex-engine-resilience-20260912`).**  ACP and Codex now preserve the saved cursor instead of silently starting an empty session; Codex retries transient resume failures against that cursor; Codex and ACP classify nested authentication failures as setup and otherwise end visibly with `resume_failed`.  ACP prompts use a validated 1-second to 20-minute deadline, flush cancellation within a bounded grace, retain ownership until the child exits, and settle once.  VPS ownership uses exact dispatch leases so delayed screenshot cleanup cannot release a successor.  Validation: `pnpm typecheck && pnpm test` passed, including the 279-file Vitest suite and every trailing package, broker, boot, iOS ship, and Infisical check.  Local stubs only; no deployed process or real VPS lifecycle changes. <!-- wb-agent-report:6ca81ed5c9764eea8a8e1f710c3a281c -->
- **2026-09-12 — CODEX — COMPLETED — Correct Antigravity quota routing (#283, PR #338, board 09d689cb).**  Merged as `abe978c6`.  Maps live opaque model labels to exact catalog identities, preserves conservative reset bounds, and clears recovered aliases.  Full gate passed 3,554 Vitest tests plus chained suites; 84 focused tests passed after peer corrections.  Installed Mac acceptance remains in #274.  <!-- codex-quota-routing-20260912 -->
- **2026-09-11 - CODEX - PLANNED - Reduce full CI runs for documentation-only changes.** <!-- wb-agent-report:34813b74e32a4b57b2687ff255f9bcfe -->
- **2026-09-11 - CODEX - PLANNED - Make local BotFleet updates preserve active work and support rollback.** <!-- wb-agent-report:e296144327fc4cdab5960e6cff0f7ae5 -->
- **2026-09-11 - CODEX - PLANNED - [P2] iOS cannot interrupt a busy room.** <!-- wb-agent-report:4bb6be1026dd4b109d945e4ca59c78bb -->
- **2026-09-11 - BF-COMPILER - PLANNED - BotFleet main CI red: package + smoke (Ubuntu) — packaged-app SIGTERM lifecycle smoke, Electron exited 127.** <!-- wb-agent-report:134116982972424e8a22de3bc9a76e58 -->
- **2026-09-11 — CODEX — COMPLETED — [P1] Patch vulnerable Next and image dependencies in the docs workspace (issue #311, PR #316, board `b9102d9e2b4a498183a0808556791fdd`).**  Next.js 16.3.3 and Sharp 0.35.4 are locked; hosted docs type generation and production build passed.  Landed as `7ae5c8c1`; no deployment or restart was performed. <!-- wb-agent-report:b9102d9e2b4a498183a0808556791fdd -->
- **2026-09-11 — CODEX — COMPLETED — Remediate vulnerable Electron packaging dependencies and verify generated artifacts (issue #296, PR #316, board `2c27a6351b18472993007b0a9f86d24d`).**  Electron Builder 26.16.1 passed hosted Linux AppImage/DEB, native Windows NSIS, and unsigned macOS arm64 packaging checks.  Landed as `7ae5c8c1`; native package evidence is from unchanged packaging inputs at `5b034a70`.  No deployment or restart was performed. <!-- wb-agent-report:2c27a6351b18472993007b0a9f86d24d -->
- **2026-09-11 — CODEX — COMPLETED — Provider request isolation (#278/#279, PR #314, boards `5344a9835c2648b5b96cb58bec81f122` / `93839dda22d84857932b4bebff31f01c`).**  Landed as `86410c70` with green final hosted CI.  Strict MCP configuration covers bot turns and tool-free title/review helpers; cached capability checks guard launches; Grok history is constructed once.  Full local gate passed 3,491 tests plus all chained suites.  Runtime rollout remains pending; preflight terminal-event preservation is included in engine PR #315 as `4973e3bb`.
- **2026-09-11 — CODEX — COMPLETED — iOS task-bound Send/Stop, idempotent retry, and background APNs safety (boards `39f7be5c`, `18341d8c`, `118caef5`, `4505f828`; issues #287–#290).**  Landed in PR #312 as `cf51b991`.  Actions now bind to the displayed thread, lost-response retries reuse one idempotency key, stale committed sends replay safely, and background APNs refreshes without navigation.  Final PR-head CI passed on macOS, Ubuntu, Windows, package, control-plane, Swift tests, and the iOS build.  Push run [34634018784](https://github.com/jaywedgeworth22/BotFleet/actions/runs/34634018784) uploaded version 1.0.43 build 202609111834 and reached internal TestFlight state `IN_BETA_TESTING`.  Scheduled run [34634263228](https://github.com/jaywedgeworth22/BotFleet/actions/runs/34634263228) skipped because the same head was already shipped; installation and device acceptance were not performed.
- **2026-09-11 - AG - COMPLETED - [P1] Infisical timeout handling and full write-through sync.**  Landed in PR #309 (squash 2246f19e) on BotFleet and deployed via update-botfleet.sh. <!-- wb-agent-report:42937fa4ac3f40f79c29c7569dae7a1a -->
- **2026-09-07 - BF-DESIGNER - PLANNED - Composer: paste screenshots, HEIC, and stop error overlaying the thread.** <!-- wb-agent-report:dae12ab6687446148bdb6dffc2e989ba -->
- **2026-09-11 - AG - COMPLETED - Scope PATCH /api/instances to affected engine and fix settings lock stall.**  Landed in PR #297 on BotFleet <!-- wb-agent-report:2256455865414dd2bf90096f23dbf725 -->
- **2026-09-11 — AG — COMPLETED / DEPLOYED — Quota telemetry countdowns, rates table cleanup, MiniMax & Custom Engines in Settings, DeepSeek & Codex logo adjustments (PR #303 squash `2303ec3c`, board `3271270141be46768817d261e11df9d8`, branch `ag/quota-rates-custom-engines`).**  Merged to main and deployed to local /Applications/BotFleet.app and LaunchAgent com.jay.botfleet-server via update-botfleet.sh.
- **2026-09-11 — GROK — COMPLETED / DEPLOYED — Map Remote Access Test Connection pill to product copy (PR #308, board `f3c03a10`, branch `grok/remote-access-test-copy`).**  Merged to main and deployed via update-botfleet.sh.
- **2026-09-09 — AG — COMPLETED / DEPLOYED — Scope PATCH /api/instances to affected engine and fix settings lock stall (PR #297, board `22564558`, branch `ag/scoped-engine-reload`).**  Merged to main and deployed.
- **2026-09-09 - CLAUDE - PLANNED - sentry-ai.ts: failed tool result detail sent to Sentry unredacted.** <!-- wb-agent-report:a5688de705dd4413bfdd9e0c107d9f6b -->
- **2026-09-08 - CLAUDE - PLANNED - HTTP tool executor: five P2 follow-ups from the Codex re-review of #236 (all confirmed on main).** <!-- wb-agent-report:049bad66db014723a0c1538149f8e077 -->
- **2026-09-08 - CLAUDE - PLANNED - HTTP tool executor (#236): rejected dispatch is swallowed so the bot never leaves working, and the tool_calls: prefix collides with the grok driver.** <!-- wb-agent-report:5d67971ed432466faa48571757901e21 -->
- **2026-09-08 - CLAUDE - PLANNED - HTTP tool executor: 60s listener timeout abandons slow MiniMax/OpenAI-compat rounds and hangs the bot.** <!-- wb-agent-report:29a2bdfefe1d4d98984dd4d3243469be -->
- **2026-09-08 - CLAUDE - PLANNED - HTTP tool executor: round cap leaves the bot busy until the 20-minute stall watchdog.** <!-- wb-agent-report:677de64466954b538ab1a815041c1618 -->
- **2026-09-09 — CLAUDE — IN PR #357 — MM PR 10/11: typed HTTP errors, honest MiniMax snapshot, MiniMax on the failover ladder (board `83e9a706`, branch `claude/mm-10-typed-errors-snapshot-ladder`, worktree `~/apps/botfleet-claude-mm-10`, parent `cdba649d`).**  New `server/drivers/chat-completions/errors.ts` maps an HTTP status onto the shared `ProviderErrorCode` union the ACP drivers already speak — 401/403 to `invalid_credentials` with `setup: true`, 402/429 to `quota_or_region_restriction`, 404 to `model_catalog_outage`, 5xx to `upstream_outage` — while a status the family has no opinion on (400, 422) stays a plain `Error` and keeps today's behaviour byte for byte.  `chat-completions/loop.ts` turns a classified `ProviderError` into an `error:<code>` terminal stopReason and puts `setup: true` on the `runtime.error` chip, so a revoked key reaches the setup affordance instead of idling behind a red chip; the single-terminal invariant is untouched, still exactly one `turn.completed` per turn on every exit.  `model-fallback.ts` gains `providerErrorCodeFromStopReason` and `quotaOrCapFromErrorCode`, and `index.ts` prefers the structured code over regexing chip prose when one is present — both strictly additive, returning `undefined` with no structured code so a CLI engine is decided by the existing regexes unchanged; a 502 now consults the fallback chain where it previously just failed, and `invalid_credentials` deliberately does not, because another engine does not fix a bad key.  The #90 auto-failover ladder moves out of `index.ts` into the exported `AUTO_FALLBACK_PRIORITY`, unit-testable without booting the server, with `minimax` after `codex` and ahead of `openaiCompat` per the owner-approved default, handed straight to `eligibleAutoFallbackChain` so main's health-and-cooldown filtering is unchanged.  `minimax.ts`'s `snapshot()` stops reading Available-until-a-turn-fails: a GET /models probe with an 8s ceiling, cached 60s and shared with `refreshModels` as one fetch with two readers, reads unavailable-with-a-reason on 401/403, available-but-capped on 402/429, and keeps the last confirmed state on a 5xx, a 404, or a network failure rather than flipping a working key on a blip; `refreshModels` reads the live catalog off that same fetch, retiring the hand-maintained three-model table to a fallback, so a new MiniMax model no longer needs a code change and a release.  `pnpm typecheck` clean; 156 targeted tests green across errors/loop/model-fallback/minimax; oxlint clean on the two new files.  Local gate limited to typecheck plus the touched test files, full suite on hosted CI, per the Sep 12 fleet load pause. <!-- wb-agent-report:83e9a70679b4410da29a1b00089d47af -->
- **2026-09-08 - CLAUDE - PLANNED - MiniMax first-class engine: driver-owned tool loop on a shared chat-completions base (design A, 11 PRs).** <!-- wb-agent-report:4564fe5eebda442baa3caf43da144701 -->
- **2026-09-08 - CLAUDE - COMPLETED - MM PR9: MiniMax price table, real cost, observability span.**  Branch claude/mm-09-prices-cost-span, worktree ~/apps/botfleet-claude-mm-09. <!-- wb-agent-report:264770eecc15459f8630347c9ac89508 -->
- **2026-09-08 — CLAUDE — IN PR #258 — config.json lost-update race between Electron and server (PR #251 review) (board `a2a3a586`, branch `claude/config-lock`, worktree `~/apps/botfleet-claude-config-lock`).**  Shared advisory file lock around every config.json read-modify-write (server saveConfig, Electron recordAutomaticCheck, boot credential migrations) plus a two-process regression test.  Stacked on PR #251. <!-- wb-agent-report:a2a3a58690d748f0b30aaf38be028636 -->
- **2026-09-08 — BF-COMPILER — DEPLOYED — iOS composer on TestFlight 1.0.39 / 202609081436 (board `e92b04b5`, PR #238 squash `114ca1a9`).**  Hosted ios-ship run 34239286614 uploaded from origin/main.  VALID, IN_BETA_TESTING.  Phone still on 1.0.38 needs a TestFlight update.  Did not extra-ship. <!-- wb-agent-report:e92b04b5afcb40d3a7f1b0a441775b92 -->
- **2026-09-08 - GROK - COMPLETED - Settings Models layout: larger dialog, wrap pills, iOS stacked fallbacks.** <!-- wb-agent-report:991b22a3d5414296afcbcd1b21ea55c8 -->
- **2026-09-08 — CLAUDE — DEPLOYED (#249, Mac harness 11:01Z, vault=13 applied=5) — BotFleet sources every secret possible from Infisical (BotFleet prod `836aebe2…`, automation machine identity): harness boot preload into env, desktop shell, CI workflows, Mac install migration (board `26e37b1f`, branch `claude/infisical-secrets` stacked on `claude/sentry-usage-telemetry`, worktree `~/apps/botfleet-claude-infisical`).**  Owner directive 2026-09-08.  Design pass first (read-only), implementation after the Sentry lane commits so `server/config.ts` and `server/index.ts` do not collide.

- **2026-09-08 — GROK — COMPLETED — Designer UI copy only on remaining PRs after #240 Title Case (boards `6ce09e3d` `#237`, `57910d13` `#231`, `e92b04b5` `#238`).**  Title Case `Update From This Mac`, `Cloud Desktop` / `Cloud Provider` (keep This Mac / My VPS), composer rollout heading.  Extra-ship no.  No functional/server.
- **2026-09-08 — CLAUDE — DEPLOYED (#247, Mac harness 11:01Z) — Sentry receives zero BotFleet telemetry: harness has no DSN, renderer bundle inert, iOS `secrets.SENTRY_DSN` missing; audit Usage Monitor stream (board `f7293d06`, branch `claude/sentry-usage-telemetry`, worktree `~/apps/botfleet-claude-sentry`).**  DSN saved to a chmod 600 handoff file (never printed).  Plan: config-driven `observability.sentryDsn` + runtime DSN for the renderer + Infisical/GH secret for ios-ship + Usage Monitor payload per token type and room turns.
- **2026-09-08 — GROK — COMPLETED — Fixer: address unresolved review threads on PRs 240 238 237 236 231 (board `1e657d54`).**  Push fixes onto PR branches.  Do not merge.  #233 Monthly usage dedupe already resolved in `2db2fd68`. <!-- wb-agent-report:1e657d542be94bcc85185e7961ca1e22 -->
- **2026-09-05 - GROK - PLANNED - Drain open BotFleet PRs to main (production).** <!-- wb-agent-report:b17f7a9aad1a447eb7328114a2f899c1 -->
- **2026-09-08 - BF-DESIGNER - COMPLETED - iOS: ST app save fails on ghost room member; queued X is no route.** <!-- wb-agent-report:e8a8e5f15e00403cb1b7ea8ee6f04c1b -->
- **2026-09-08 — BF-DESIGNER — COMPLETED — iOS ST app save unknown room member + queued X no route (board `e8a8e5f1`, branch `grok/ios-save-queue`, worktree `~/apps/botfleet-grok-ios-save-queue`).**  PATCH drops leftover deleted-bot roster ghosts so a phone save is not blocked.  Packaged companion 1.0.30 still 404s DELETE /queue/; source already allowlists it (#224).  No TestFlight.  Ask before bouncing live BotFleet.
- **2026-09-08 — BF-COMPILER — IN PR — Bot-to-bot chats out of Apps/Channels; restore ubf checkout; local Update when GitHub has no latest-mac.yml (board `6ce09e3d`, branch `compiler/bot-chats-sidebar`, worktree `~/apps/botfleet-compiler-ubf`).**  Detached `~/apps/botfleet-server` restored so `ubf` works.  No TestFlight upload.
- **2026-09-08 - BF-COMPILER - COMPLETED - Bot-to-bot chats land in Apps/Channels; ubf checkout missing.** <!-- wb-agent-report:6ce09e3d84164aca9509e8cbb98b41e3 -->
- **2026-09-04 - CLAUDE - PLANNED - VPS is described to agents as Shared VM / 'shared with the other bots' while provisioning is one isolated container per bot.** <!-- wb-agent-report:4b6673fcc60c4c969c26c4cee4f515bf -->
- **2026-09-04 - CLAUDE - PLANNED - iOS computer picker labels are inverted: the toggle named VPS enables the Mac Local VM; the real VPS row is named Cloud VM.** <!-- wb-agent-report:57910d13186c4bb389bee361b1dac36e -->
- **2026-09-06 - GROK - COMPLETED - Default theme System Auto + add User Auto pair.** <!-- wb-agent-report:c718a8c409a24395af2992f6e8872ec2 -->
- **2026-09-03 - BF-DIRECTOR - PLANNED - Bot settings and marketing HTML leak nbsp entity as visible text.** <!-- wb-agent-report:a948e08b49244942b474ce62193171bf -->
- **2026-09-04 - CLAUDE - COMPLETED - Bots can hold several computers and choose per task (multi-grant).** <!-- wb-agent-report:239fd65e1b234b0ca1f460c1fe91a99a -->
- **2026-09-04 - GROK - COMPLETED - Treat antigravity-usage N/A remaining as exhausted.** <!-- wb-agent-report:d83067193bc64229bcbc288fb9d123ec -->
- **2026-09-03 - GROK - COMPLETED - Poll UM routing caps; skip model types until reset. PR #187 `62f10385`.** <!-- wb-agent-report:087cd1b0f2c243b88788e5fc998a5c0d -->

## Deployed
- **2026-09-07 — AG — COMPLETED/DEPLOYED — Fix Vercel auto-deploy rate-limit query (&state=READY) and refresh glossy robot trio assets (PR #230 merged `6f9fbacb`).**  Added &state=READY filter to `apps/site/vercel-ignore-hourly.sh` so canceled or skipped builds do not lock out production deployments; updated Vercel project ignore command fallback to `exit 1`.  Refreshed hero image, app icons, and social banner with Sep 3 glossy robot trio.  Verified live production deployment on `https://botfleet.app` (HTTP 200, matching MD5).
- **2026-09-03 - GROK - PLANNED - Thread reassign/merge DnD + hourly iOS ship without tags.** <!-- wb-agent-report:8aee326915b34c7c8c27a4cdb93fafca -->
- [AG] 2026-08-30 — Fixed Codex model fallbacks and DeepSeek model logos/settings, updated local iOS UI, fixed PR 1 tests and merged to main.
- [AG] 2026-08-29 — Always-on iMessage bidirectional relay daemon (`botfleet-imessage-relay`) and LaunchAgents (`com.jay.botfleet-imessage-relay` + `com.jay.botfleet-server`) connecting 12 BotFleet bot group chats in Messages.app with BotFleet backend.

## Completed
- **2026-09-11 - BF-DIRECTOR - COMPLETED - Settings Remote Access test button and Infisical sync verification.**  Landed: BotFleet PR #298 merged 2026-09-11 01:06Z (e60f68a6). <!-- wb-agent-report:9a4266a573f140e8890d3a0be16b6f60 -->
- **2026-09-11 - BF-DIRECTOR - COMPLETED - UI pass: system-instruction card chrome on PR #299.**  Landed: BotFleet PR #301 merged 2026-09-11 00:29Z (566f398e). <!-- wb-agent-report:1476cdc17ee047a3be906615a5da0e83 -->
- **2026-09-11 - BF-DIRECTOR - COMPLETED - Same-source/routine re-fires must append; auto instructions not user bubbles.**  Landed: BotFleet PR #299 merged 2026-09-11 00:13Z (6c395ebd). <!-- wb-agent-report:3d9ff839fbe6437e8ca6b22ddee351d1 -->
- **2026-09-10 - CLAUDE - COMPLETED - Redaction wrapper scan: a prose quote opens a false wrapper, an escaped wrapper is missed, a short credential after a scheme is unmasked.** <!-- wb-agent-report:3f57540fc5e6470cb70f57f6592e792b -->
- **2026-09-11 - BF-DIRECTOR - COMPLETED - BOTFLEET-C grokAgent auth_required while signed in.**  Landed: BotFleet PR #304 merged 2026-09-10 21:08Z (787a814d). <!-- wb-agent-report:e86443a4cc2043df90834fafb7caa6ba -->
- **2026-09-10 - CLAUDE - COMPLETED - Redaction auth-header pattern leaks a long OAuth parameter and eats JSON siblings.**  Landed in #306 (09cc49f6). <!-- wb-agent-report:d7b670383f7f4d4e978890d9df8a0032 -->
- **2026-09-09 — CODEX — COMPLETED — End-to-end iOS, macOS, engines and integrations audit (PR #295, board `55bff4ee`, issue #263).**  Completion record for this documentation merge: 35 findings, 32 new follow-up issues plus expanded #93/#188, 35 canonical Mac board items, report and structured ledger under `docs/audits/2026-09-09-*`.  Full pnpm gate and 234 Swift core tests passed; local unsigned iOS builds stopped before compilation at unavailable destinations, with hosted CI at `6cab38ff` passing the full unsigned iOS build.  Runtime, credentials, routes, apps and releases were not changed.  Implementation findings remain open; living Apple Note `[BF, Codex] End-to-end app and integration audit`. <!-- wb-agent-report:55bff4ee870442cb8aea8a0a6597c901 -->
- **2026-09-08 - CLAUDE - PLANNED - settings-rev2 C (#219): the 6-hour auto-update throttle never persists, plus two P2 defects.** <!-- wb-agent-report:2a77066b65ab4c69851808286f8e83ee -->
- **2026-09-08 - CLAUDE - COMPLETED - MM PR1: HTTP-lane fixtures, MiniMax stream hardening, honest engine metadata.** <!-- wb-agent-report:aad7d97067334b0bb370e4e8ea3581d1 -->
- **2026-09-08 - CLAUDE - PLANNED - settings-rev2 A (#220): three P1 + three P2 defects confirmed on main.** <!-- wb-agent-report:ff8dd5501b984e8491ef13b593f00bd3 -->
- **2026-09-08 - CLAUDE - PLANNED - Computer allowlist is bypassed for unconfigured bots: resolveGrants returns the UNFILTERED set when the intersection empties.** <!-- wb-agent-report:315b1fd7083844f1b175fff476218454 -->
- **2026-09-08 - CLAUDE - COMPLETED - MM PR2: driver-owned tool loop, MiniMax emits one terminal event per turn.** <!-- wb-agent-report:838bf3c55f3e41ad92472b8ecb3ebe46 -->
- **2026-09-08 - CLAUDE - PLANNED - settings-rev2 B (#218): DeepSeek balance credential is dead in the packaged app, and /api/quotas blocks on an unbounded fetch.** <!-- wb-agent-report:01f4994a33e0493382e095c74d7a88be -->
- **2026-09-04 - MINIMAX - COMPLETED - [MINIMAX] settings-rev2-C: general settings (webhooks, max-turn, 6h throttle).** <!-- wb-agent-report:810e6dac2479405d84da6c0eb41c7c52 -->
- **2026-09-08 - CLAUDE - COMPLETED - [BF] BotFleet must source every secret possible from Infisical (BotFleet prod, automation machine identity): harness runtime, desktop shell, CI workflows, plus migration of the Mac install's stored keys.** <!-- wb-agent-report:26e37b1fb3794bffbe2e2b932cb273ca -->
- **2026-09-08 - CLAUDE - COMPLETED - [BF] Sentry gets zero telemetry: harness has no DSN, web bundle inert, iOS secret missing; audit Usage Monitor stream.** <!-- wb-agent-report:f7293d0631c04e4d9a9d04d68f3fee83 -->
- **2026-09-08 — BF-FIXER — COMPLETED — Antigravity quotas Gemini + Third-Party only; Bot Profile avatar drag-drop. PR #246 squash `3a5f57c1`. Board `2bf6c493`.**  Two shared numbers, not per-model rows.  Avatar box accepts GIF, HEIC, BMP, SVG and existing formats.  Group min skips exhausted 0% models.  No TestFlight.  Mac app needs `update-botfleet.sh`. <!-- wb-agent-report:2bf6c493a56943768e4b32185599af7e -->
- **2026-09-08 — BF-FIXER — COMPLETED — BOTFLEET-2 EADDRINUSE 127.0.0.1:8793. PR #248 squash `f5dcf46d`. Board `dd86060b`.**  Named EADDRINUSE handling so a bind collision is not an uncaught fatal.  8791-8793 are named after collision, not reserved before bind.  Live seat-mcp 8793 and BotFleet 8799 still 200.  No TestFlight. <!-- wb-agent-report:dd86060b15d346e5ade856183fa42d18 -->
- **2026-09-08 - CLAUDE - COMPLETED - Mac download links on botfleet.app, README, and docs point at BotFleet-1.0.30 filenames no release carries; move to stable BotFleet.dmg names on the app repo.** <!-- wb-agent-report:2b054fbd7f5c45a9994288eee70da9f0 -->
- **2026-09-08 - CLAUDE - COMPLETED - Restore the original DeepSeek mark viewBox (owner ruling Sep 8).** <!-- wb-agent-report:eaa9441ce73a48fea1e0c16053b1b8cb -->
- **2026-09-08 — BF-FIXER — COMPLETED — Simple-mode merge-all + iMessage trigger cards / [to iMessage] gated replies. PR #245 squash `25c010aa`. Board `8bfc6b0b`.**  Switching to Simple offers Merge All Threads or Keep Extra Threads Hidden.  iMessage inbound renders as a Mac-style trigger card (`[from iMessage]`); only `[to iMessage]` bot replies are relayed, tag stripped.  Relay LaunchAgent stays Retired/Forbidden under jay.  No TestFlight.  Mac harness needs `update-botfleet.sh`. <!-- wb-agent-report:8bfc6b0b675e42799941f392f527f2f9 -->
- **2026-09-06 - CLAUDE - PLANNED - BotFleet PR #228 Blocked By A Verified-Wrong Seer Thread.** <!-- wb-agent-report:164badbd7c3544649c7ba59cdbc60f0b -->
- **2026-09-06 - PRODUCER - COMPLETED - PR #228 BotFleet: BLOCKED waiting for required review.** <!-- wb-agent-report:cb8b991a773942ff9d255a7ff55a8b58 -->
- **2026-09-08 — BF-FIXER — COMPLETED — Webhook payloads as collapsible cards; fleet-infra Sentry to Plumber. PR #239 squash `e938e7d6`. Board `28f76152`.  Sentry-named hooks with project.slug fleet-infra go to Plumber.  Web + iOS Details card, not a blue user bubble.  No TestFlight.  Mac harness needs `update-botfleet.sh`. <!-- wb-agent-report:28f761520dc14cfa92221aa68f2a3cbd -->
- **2026-09-05 - PRODUCER - COMPLETED - PR #209 BotFleet: BLOCKED package+smoke Ubuntu failed.** <!-- wb-agent-report:bc3373e2b19a40d198f93236bc7831c4 -->
- **2026-09-05 - PRODUCER - COMPLETED - PR #221 BotFleet: BLOCKED typecheck+test failed on 3 OS.** <!-- wb-agent-report:e3874bf4b3fa4fcdaa0811d09854d21b -->
- **2026-09-05 - PRODUCER - COMPLETED - PR #222 BotFleet: BLOCKED typecheck+test failed on 3 OS.** <!-- wb-agent-report:743b0185363648bd9c21445f1d45cf9a -->
- **2026-09-05 — BF-Fixer — COMPLETED — iOS busy-bot messages vanish: keep the 202 queueId as a pending chip. PR #224 squash `6ee076ef`. Board `b614a1c6`.**  Steer-queue holds the line out of the transcript; iOS discarded the 202 body.  Chip stays until drain (`message.queued`), not until `bot.busy` flips.  Quota fixture reset times are relative.  No TestFlight upload.  Rollout: `docs/rollouts/2026-09-05-ios-busy-queue.md`. <!-- wb-agent-report:b614a1c62b624f00ac2462c4a46d311e -->
- **2026-09-04 - GROK - COMPLETED - WAVE1 Infisical: ios-testflight.yml reads Apple/iOS signing from Infisical prod.** <!-- wb-agent-report:2f422f461f2a4a90a2d0393e5cf91955 -->
- **2026-09-03 - GROK - COMPLETED - Hosted macos-latest ios-ship path filter (CT/DealDex/ST).** <!-- wb-agent-report:f9a512146c094db49917320be7b318e0 -->
- **2026-09-04 - MINIMAX - PLANNED - [MINIMAX] Settings rev2 — bot desktops, engines toggles, webhooks, defaults, usage, naming.** <!-- wb-agent-report:d08ad6ebf5f1460bb83a031825931d4b -->
- **2026-09-04 - MINIMAX - IN PR #220 - [MINIMAX] settings-rev2-A: bot computers allowlist, VM mode, set-all-to-default, model defaults.** <!-- wb-agent-report:3da59409e04b41d9af291e29fa5d5ab1 -->
- **2026-09-04 - MINIMAX - COMPLETED - [MINIMAX] settings-rev2-B: engines + usage.** <!-- wb-agent-report:0ad2b059ef6944499725449325b62c6c -->
- **2026-09-04 - GROK - COMPLETED - Quota rows hide Gemini/Cursor monthly; MiniMax CLI not first-tier.** <!-- wb-agent-report:d4d3343b8f9842d089019635f0027451 -->
- **2026-09-04 - CLAUDE - PLANNED - recall: redirect manual turns benign 3xx into a false Cloudflare Access diagnosis.** <!-- wb-agent-report:217a8436077d4a128a7ba6ce7134ea37 -->
- **2026-09-04 - CLAUDE - COMPLETED - Agent RAG connection cannot reach the fleet recall service (6s probe timeout + no Cloudflare Access support).** <!-- wb-agent-report:2fec813248854af58ab451a3dd52fd29 -->
- **2026-09-04 - CLAUDE - COMPLETED - VPS desktop budget: size for a shared host, make it configurable.**  Landed in BotFleet #197.  The per-desktop cap was one constant shared by the Local VM and the VPS path.  That is right for the Local VM (sole desktop on the person's own workstation, 8 GiB / 4 CPUs) and wrong for a VPS, which runs one desktop PER BOT on one machine — the same number fits two bots on a 30 GB server.  VPS desktops now default to 3 GiB / 2 CPUs and are configurable via vps.memoryGib / vps.cpus, bounded 2-64 GiB and 1-32 CPUs.  dockerSecurityIsHardened takes the caps to expect rather than reading the Local VM constants; still an exact comparison, because a container built to a size the caller did not ask for is one the caller no longer controls.  Two test fixtures had the old numbers hardcoded in fake docker inspect responses — one surfaced as a 30s timeout reading 'the echoed turn never happened' — and now derive from the same defaults the run args use. <!-- wb-agent-report:75d53f85482643b0be069ef7f5e5c74d -->
- **2026-09-04 - CLAUDE - COMPLETED - Off button granted the bot the user's Mac; [] and unset were the same state.**  Fixed in BotFleet #200.  bot.computers had three meanings collapsed into two representations: undefined (never configured, auto), [] (Off, or deselecting the last destination), and [...] (those destinations).  The server read it as 'bot.computers ?? []', so [] became auto, auto reached the host fallback, and on macOS shouldMountLocalComputer({requested: undefined}) returns true — an explicitly-Off bot got the user's own Mac.  ComputerPanel already treated [] as off, so the UI and the server disagreed.  Extracted the rule into resolveGrants() with tests, and split ComputerDestination (cloud|vm|local, what you pick) from ComputerKind (box|vps|vm|local, what mounts) since 'cloud' has no mount kind of its own. <!-- wb-agent-report:32aa4deecb32467ba7960840642eccfe -->
- **2026-09-04 - GROK - COMPLETED - Usage Monitor URL and ingest token persist; Save + Test Connection. PR #192 `5c4b4304`.** <!-- wb-agent-report:5c0ee072649d4158af7d4423101e11ae -->
- **2026-09-03 - AG - COMPLETED - DSH driver hardening: fallback classification, DSH_HOME auth, honest model-set.** <!-- wb-agent-report:9effb254010f4ec3b6b2ca2681a8cff5 -->
- **2026-09-03 — GROK — COMPLETED — Skip exhausted models + Settings remaining %. PR #187 squash `62f10385`. Board `087cd1b0`. Issue #186. Local `antigravity-usage` poller; skip only at 0% / `isExhausted`. LiteLLM idle. EasyCLIProxy not reinstalled.**
- **2026-09-03 - BF-DIRECTOR - COMPLETED - BotFleet + Grok Bot routine/instruction sentence gaps.**  BotFleet live routines, About fields, bulletins, webhook prompts, and Grok Bot local MCP instructions now use two ASCII spaces.  ContactLogo.com bulletin waits until this room is idle (409 while working).  Grok Bot cloud role prompts are not in local JSON. <!-- wb-agent-report:84a23c4fdbb54cbd976d3664189b464d -->
- **2026-09-03 — GROK — COMPLETED — APNs killed-app wake, desktop 1.0.30 aligned with iOS.**  PR #184 merged `ff35d759`.  Board `f7d09c8b`.  Issue #183.  Release feed still needs GH MAC_CERT/APPLE_API/RELEASES_PAT or a local signed package. <!-- wb-agent-report:f7d09c8ba41c4be8b2e413c8bd31799b -->
- **2026-09-03 - GROK - COMPLETED - MiniMax review: delete congress compose.yml, gitignore patch junk, Windows path tests, require latest-mac.yml.**  PR #182 merged `f16255d0`. <!-- wb-agent-report:41169a1e25b24381b650c33691f028ea -->
- **2026-09-03 — GROK — COMPLETED — MiniMax review hygiene: delete congress compose.yml, gitignore patch junk, Windows path tests, require latest-mac.yml.**  PR #182 merged `f16255d0`.  Board `41169a1e`.
- **2026-09-03 — GROK — COMPLETED — iOS opaque chat chrome, compose menus, Simple/Projects workspace modes, room crop, quota Available fix.**  PR #181 merged `18e66cdd`.  Board `4498c349`.  Issue #180.  GitHub TestFlight job lacks Apple API secrets; Mac wrapper is the ship path. <!-- wb-agent-report:4498c349a0b540e9906fe9cc19f3335a -->
- **2026-09-03 - GROK - PLANNED - Harness 8799 banner, Publisher drain, thinking model chip, routine thread reuse, iOS TestFlight icon.** <!-- wb-agent-report:6251b0884730457ebf520e3676b576c2 -->
- **2026-09-03 — GROK — COMPLETED — Ship trio app icon on iOS and macOS from `botfleet-ios-app-icon-1024x1024.png`; favicon from transparent just-bots.**  PR #165 merged `d95bebb0`.  Board `a959d7bf`.  Issue #164.  iOS 1024 is RGB 24-bit.  GitHub social preview unchanged. <!-- wb-agent-report:a959d7bf72c8446badfc98150f573129 -->
- **2026-09-03 — GROK — COMPLETED — Drop Polar support-payment links; rewrite README as BotFleet add-ons (all in testing) then OpenMausBot fork features.**  PR #161 merged `657e4b28`.  Board `1af79606`.  Issue #160.  GitHub About updated; social preview left owner-set.  botfleet.app still on the previous Vercel build (Hobby rate-limited 24h). <!-- wb-agent-report:1af79606c0d44b3bb6bc3178bb2d153a -->
- **2026-09-02 - AG - PLANNED - iOS & macOS thread navigation, Live Activities unread sync, terminology and collapsible sections.** <!-- wb-agent-report:3271270141be46768817d261e11df9d8 -->
- **2026-09-02 - AG - COMPLETED - Provider marks update (Gemini star for Antigravity, cropped Grok/Claude/DeepSeek), filter unconfigured models (Kimi), and unhide EngineProbe bot.** <!-- wb-agent-report:d58a7c9be52544e2af73995821103940 -->
- **2026-09-02 - AG - COMPLETED - Align BotFleet with fleet-agents memory platform and Fleet Recall MCP.** <!-- wb-agent-report:757e534d674046f197c20b42ba8e5eba -->
- **2026-09-02 — AG — COMPLETED — Align BotFleet with fleet-agents memory platform and Fleet Recall MCP (PR #153, branch `ag/fleet-agents-recall`).**  Board `757e534d`.  Replaced 128-dim SHA256 Qdrant proxy with canonical Fleet Recall (`recall_search`, `recall_contribute`, `recall_stats`) backed by local `recall` CLI / HTTPS fallback.  Fixed test connection endpoint to probe live fleet memory (36,887 points in `fleet-agents`).
- **2026-09-02 — AG — COMPLETED — DeepSeek-style elapsed turn timer in TurnPresence and duration in ActivityRun (PR #150, merged as `3deb8458`, deployed).**
- **2026-09-02 — AG — COMPLETED — Quota/cap telemetry parity, Gemini 3.8 Flash, Kimi/Gemini/DeepSeek-API driver cleanup, header protection & acoustic echo suppression (PR #144, branch `ag/dsh-antigravity-fallback-parity-terminology`).**  Merged to `main` as `ede6de0b`.
- **2026-09-02 - CLAUDE - COMPLETED - Consolidate releases onto the BotFleet repo; retire `botfleet-releases`; AGENTS.md no-new-repos rule (owner directive).**  Board `54a66d07`.  PR #146 (`claude/consolidate-releases`, `~/apps/botfleet-claude-consolidate`).  electron-builder publish + release.yml RELEASES_REPO + site links + docs → `jaywedgeworth22/BotFleet` Releases.  Old repo deletion is owner-only after the site serves the new links.  Merged as 734f1ce.  PR #144 (AG) also merged with main twice and pooled box naming restored (c36022c7).
- **2026-09-02 - CLAUDE - COMPLETED - B3b off-fleet defaults: Composio broker 404, accounts.botfleet.com, updater publish target, download links.** <!-- wb-agent-report:292b248b322e42bf89d46a8389cdb9d7 -->
- **2026-09-02 - CLAUDE - COMPLETED - B7b MCP fleet tools: list/answer approvals, open_app, routines/webhooks/decision-log tools, idempotent send, stdin-EOF safety (wave 2).** <!-- wb-agent-report:4b71498cd2b942448fe32128db73dc45 -->
- **2026-09-02 - CLAUDE - COMPLETED - B2 automation runs: fullAuto+local computer dispatch, scheduled turns unattended, run reconciliation.** <!-- wb-agent-report:bb10556873f947348efd72b41b921667 -->
- **2026-09-02 - CLAUDE - COMPLETED - B5 trust boundaries: re-verify PR #95 fixes after merges, fullAuto default, phone room cwd, static realpath.** <!-- wb-agent-report:05e95a0507674e1f991d0357c485d3ff -->
- **2026-09-02 - CLAUDE - COMPLETED - B8 web UI light default readability: light Shiki theme for code fences, verify Studio @theme + telemetry badge after merges, engine-rail describe memo (wave 2).** <!-- wb-agent-report:6114c9ca460e4810a54a0d5052fd69e3 -->
- **2026-09-02 - CLAUDE - COMPLETED - B1 always-on harness runs from main: repoint LaunchAgent + update script, desktop attaches to existing harness.** <!-- wb-agent-report:92cbbb3219374561bb20c11d8b210e5b -->
- **2026-09-02 - CLAUDE - COMPLETED - B4 phone migration: #82 rename broke iOS keychain service + companion device-dir migration; phone room cwd (wave 2).** <!-- wb-agent-report:a221fbd979d740e8b8dd55fe4af8136d -->
- **2026-09-02 - CLAUDE - COMPLETED - B3a site truth: features.json single source, TestFlight ER6sPNMh, AASA for universal links.** <!-- wb-agent-report:c5ffa6db20fe41e8bcacd98007aa5205 -->
- **2026-09-02 - CLAUDE - COMPLETED - Main repair + owner-authorized P0/P1 fixer wave + Delta Audit reconciliation.**  Eleven PRs merged: #130 main repair, #140 iOS CI, #132 docs (reconciliation + AGENTS.md), #133 site truth, #141 phone migration, #137 harness-ops, #143 web UI light, #138 trust boundaries, #139 automation runs, #142 MCP fleet tools, #136 off-fleet defaults.  main 28d369c CI green on every job; verified locally (typecheck clean, 2,429 tests, packaged smoke).  Mac: always-on harness on `~/apps/botfleet-server` @ origin/main; iOS platform runtime installed.  Owner actions: latest-mac.yml + zips on v0.1.38 (botfleet-releases); fleet-owned Composio broker + control plane; gh re-auth; run update-botfleet.sh.  Docx on Desktop carries §17-18.
- **2026-09-02 - CLAUDE - COMPLETED - B6 iOS CI red on main + ios-testflight.yml 30/30 failures + privacy answers after Sentry + version bump scheme (wave 2).** <!-- wb-agent-report:e03169906165458a95b63cb9d07f7713 -->
- **2026-09-02 - CLAUDE - COMPLETED - B0 main red after conflict merges: typecheck/boot/tests repaired (claude/fix-main-red).** <!-- wb-agent-report:ef0c48c6c030448bab10066e09ca9ceb -->
- **2026-08-31 - GROK - PLANNED - main typecheck red: duplicate GroupAvatar in Avatar.tsx.** <!-- wb-agent-report:e0558c8704a1411e8848462d13075890 -->
- **2026-09-01 - CLAUDE - PLANNED - grok/resource-watch must fold in live mac-resource-watch.py fixes before merging (cooldown, swap disjunct, --clean bypass, webhook header/body).** <!-- wb-agent-report:7656a1d698c24c66b32c853c842e0f00 -->
- **2026-09-02 - BF-PLUMBER - COMPLETED - Live-test BotFleet engines + official quota-chip corpus.** <!-- wb-agent-report:a59fcff213d4440490fd53b32891f2da -->
- **2026-09-01 — BF-DIRECTOR — COMPLETED — Quota/session-limit failover even after tools and in rooms.**  PR #119 merged `d2b3051`.  Board `96f2ec55`.  Issue #118.  Live harness must restart to pick up main.  <!-- wb-agent-report:96f2ec55370d437bb68cef79049fb0d1 -->
- **2026-08-31 - GROK - PLANNED - Composio broker REGISTRATION_MODE=open on workers.dev.** Board 94850736. <!-- wb-agent-report:948507366b5f44b4a6c026748a42314f -->
- **2026-08-31 - CLAUDE - PLANNED - Open BotFleet from iOS: harness endpoint (server LaunchAgent is always-on) runs open -a BotFleet; companion button + relay text command; pmset womp for sleep wake.** <!-- wb-agent-report:12ccfacd54564c41b784b1da5f76205e -->
- **2026-08-31 - CLAUDE - PLANNED - iMessage relay: com.jay.botfleet-imessage-relay LaunchAgent missing/unloaded while MAC-LOCAL-PROCESSES says Up; live listener is /Users/agents/apps/imessage-botfleet.py (agents acct) — reconcile daemon + docs.** <!-- wb-agent-report:80dd2680601442d9afd8c67347caec23 -->
- **2026-08-31 - CLAUDE - PLANNED - Closed-app phone alerts + reply-to-approve: notify.ts sink through iMessage relay (pre-APNs) and list_pending_approvals/answer_approval MCP tools.** <!-- wb-agent-report:02ca3c9845f848dfa6a538c15cb7193b -->
- **2026-09-01 - GROK - COMPLETED - iOS APNs registration for killed-app wake.** <!-- wb-agent-report:4677da28ff184afb8391e7d59ffb1bd7 -->
- **2026-08-31 - GROK - PLANNED - iOS chat images use /api/attachments URLs; agents need disk path.** Board a9683ae2. <!-- wb-agent-report:a9683ae243b149859ace650bac43723c -->
- **2026-09-01 - GROK - PLANNED - Chat: paste and drag-drop images and any files on every platform.** <!-- wb-agent-report:af8f27768c4d42f9ac547714f71d82b7 -->
- **2026-09-01 - GROK - COMPLETED - iPad: native target + layouts that look and work well.** <!-- wb-agent-report:b43584ea9d764df0a92d5a1232a15842 -->
- **2026-09-01 — GROK — COMPLETED/MERGED #111 — Sentry production deploy records (`sentry-cli releases deploys new -e production`).**  Additive workflow for `botfleet`.  Board `2d1c8565`.
- **2026-09-01 — GROK — COMPLETED/MERGED #110 — Add fleet sentry-ci-report.yml + scripts/sentry-ci-report.py (branch `grok/sentry-ci-report`, worktree `~/apps/botfleet-grok-sentry-ci`, board `e70a89f7`).**  Gold copy UM PR #1394.  APP=`botfleet`.  Fingerprint `[ci-failure, botfleet, workflow]`.  <!-- wb-agent-report:e70a89f7 -->
- **2026-09-01 - GROK - COMPLETED - Sentry DSN hygiene: no hardcoded iOS fallback.**  Merged iOS Sentry DSN hygiene PRs (plist-only / no hardcoded ingest URL fallback). <!-- wb-agent-report:aecc129faf6842b480b229ca92b143dc -->
- **2026-09-01 - CLAUDE - COMPLETED - BotFleet top-to-bottom analysis v2: Mac app, iOS app, site, fleet-procedure integration (post-2026-08-31 audit).**  GROK correction 2026-09-01: picked up on `781554fd`; CLAUDE row left in place, not deleted. <!-- wb-agent-report:781554fd38f2480cae5d8a67c851da17 -->
- **2026-09-01 - GROK - PLANNED - Companion trust leftovers: pairing replay after revoke (C3), phone always-allow/authorize (C4), DSH --mcp quoting (D2).** <!-- wb-agent-report:149843e85b2946a29c72a6399a98f5c1 -->
- **2026-09-01 - GROK - PLANNED - Client state races: hydrate wipes patches, setModel bypass, unread PATCH, patchGroup no rollback.** <!-- wb-agent-report:dc5c9b879f3b46c987d3c80df9ce351b -->
- **2026-09-01 - GROK - PLANNED - SECURITY.md still documents plaintext ~/.botfleet/config.json keys and upstream mailbox.** <!-- wb-agent-report:12c3674f561f4e68b696a870e0a7f3ce -->
- **2026-09-01 - GROK - PLANNED - telemetry.ts reads fleet handoff file ~/.secrets/global-api-keys.** <!-- wb-agent-report:8e266add2d8148eb9d2248e4be4d24e1 -->
- **2026-09-01 - GROK - PLANNED - DeepSeek UI prices 10x below billing; dsh retired model ids.** <!-- wb-agent-report:19420079a5a24744b13d239f0d0a8451 -->
- **2026-09-01 - GROK - PLANNED - CSS @theme defaults still Midnight; FOUC on dark Mac.** <!-- wb-agent-report:961d2d5057424c409b1d141fea26deff -->
- **2026-09-01 - GROK - PLANNED - drafts.ts attachment listener registered with useState leaks.** <!-- wb-agent-report:dfb0d844c9ac4f2f9d88b9caa475dcb9 -->
- **2026-09-01 - GROK - PLANNED - deleteBot leaves ghost room memberIds and defaultResponder.** <!-- wb-agent-report:96d7de9beadc450296e4030ae0b6589b -->
- **2026-09-01 - GROK - PLANNED - PATCH /api/groups/:id drops avatarCrop.** <!-- wb-agent-report:4cb4ec632a174d22b359658ce86fddf9 -->
- **2026-09-01 - GROK - PLANNED - iOS ATS: rollback NSAllowsArbitraryLoads and botfleet.app cleartext exception.** <!-- wb-agent-report:95e445e56e894eff80af0e72da045abd -->
- **2026-08-31 - CLAUDE - PLANNED - Error surfaces to recovery affordances: app-level persistent toast, computer-dispatch recovery cards (use-this-computer / create Local VM), provider-error Switch-model/Add-key/Retry-with-fallback buttons.** <!-- wb-agent-report:92a254df699c455180ec8df283c76590 -->
- **2026-09-01 - CLAUDE - PLANNED - com.jay.botfleet-server crash-looping (3,358 runs): server/resource-triggers.ts:252 parameter property breaks node --experimental-strip-types; pnpm dev:server dead on main.** <!-- wb-agent-report:362daa4280834edb9acb442dc48ab7d9 -->
- **2026-08-31 - GROK - PLANNED - Local VM stall watchdog never releases idle fence.** Board a5dabdea. <!-- wb-agent-report:a5dabdea01444140a1c37c04407e947d -->
- **2026-08-31 - GROK - PLANNED - README + botfleet.app Download CTA point at wrong GitHub artifacts.** Board d9bd4316. <!-- wb-agent-report:d9bd4316054045858e960a7de198786e -->
- **2026-08-31 - GROK - PLANNED - Auto-approve + Always-allow Bash:curl covers curl|sh; control-client fails open.** Board cd6bf0cd. <!-- wb-agent-report:cd6bf0cd4335412094995c462baf4ce2 -->
- **2026-08-31 - GROK - PLANNED - Default skin System Auto boots Midnight (owner light-first).** Board 359e1d07. <!-- wb-agent-report:359e1d07c6c64bab8463ee48b2a8e82c -->
- **2026-08-31 - GROK - PLANNED - Electron open-file/show-in-folder skip confinement; window-open no protocol allowlist.** Board 6c38e297. <!-- wb-agent-report:6c38e297ca8d4f5195871193676f91e1 -->
- **2026-08-31 - GROK - PLANNED - Model fallbacks almost never run (error activity counts as produced).** Board e556f063. P0. <!-- wb-agent-report:e556f063fb7249c4b13b65b7e9ba8037 -->
- **2026-09-01 - GROK - COMPLETED - Implement 2026-09-01 delta audit batches.**  Board `9e922f65`.  Merged BotFleet #91 #92 #95 and botfleet-site #1.  Fallbacks, ATS, Electron trust, honesty/copy, data/permissions, companion C3/C4/D2.  Deferred: APNs, native iPad, layout overlay, iMessage LaunchAgent bootstrap, iOS chat composer, Composio remote deploy. <!-- wb-agent-report:9e922f6505f64ce38be27f6f3d40dff1 -->
- **2026-09-01 - GROK - COMPLETED - Mac + server disk/RAM pressure: 18GB swapfiles, optimize GB/BF-Housekeeper routines + BotFleet resource triggers.**  AFC #157 squash-merged `b925734`.  BotFleet #80 already merged.  Swap reboot leftover. <!-- wb-agent-report:95c74e1122674df49518c098f21405fc -->
- **2026-09-01 - PRODUCER - COMPLETED - PR #80 BotFleet: unstable mergeable_state (failing/pending check) on claude/housekeeper-resource-triggers.**  BotFleet #80 already merged 2026-09-01.  Do not reopen. <!-- wb-agent-report:6155f9d7ac8c4da69d819683bf26cb0b -->
- **2026-09-01 — GROK — COMPLETED — Resource-threshold triggers that wake a bot (`grok/resource-triggers`).**  Board `3cd995b0`.  Merged as #65 / #80.  GROK correction 2026-09-01: this row was still In Progress after merge; moved in place, not deleted.
- **2026-09-01 — AG — COMPLETED — iOS Native Sentry Cocoa telemetry, crash reporting, and app-hang detection (branch `ag/ios-sentry-cocoa-expansion`).**  PR #55 merged.  GROK correction 2026-09-01: this row was still In Progress after merge; moved in place, not deleted.  Rollout: `docs/rollouts/2026-09-01-ios-sentry-cocoa-expansion.md`.
- [AG] 2026-09-01 — Automatic model failover on quota/session limits, Gemini/Antigravity harness full tool & computer execution, and shared Qdrant Agent RAG integration (branch `ag/model-failover-gemini-qdrant-rag`). Enables automatic fallback model switching on session limits/quotas/errors, unlocks local computer control and full tool loading for Antigravity & Gemini ACP harnesses, adds shared Qdrant stdio MCP proxy with semantic vector search/store, and exposes Qdrant Agent RAG custom settings in App Settings. Gate: typecheck clean, driver test suites clean, build clean.
- **2026-09-01 - GROK - COMPLETED - Sentry BotFleet: prod DSN, Replay, Feedback, agent tracing.** <!-- wb-agent-report:d99cee7f21ad4a2ba6f74b50c85fda04 -->
- **2026-09-01 - CLAUDE - COMPLETED - BotFleet main CI red since 0836c1aa2 (Swift compile + credential-env test + .desktop lint) blocking all PRs.** <!-- wb-agent-report:a7ec6f0a69d94071b1c46638545eca65 -->
- **2026-08-30 - PRODUCER - COMPLETED - PR #5 BotFleet: BLOCKED, no update in review.** <!-- wb-agent-report:24c4b4906ec544a0a06c56c0338263cc -->
- **2026-08-30 - GROK - COMPLETED - BotFleet PR #5 CI + remaining P1 review threads.** Mac ~/apps/botfleet-grok @ ag/fleet-imessage-routines-recovery. <!-- wb-agent-report:f24a7f824acc4627a98eac226ba5de9f -->
- **2026-09-01 - GROK - COMPLETED - BotFleet PR #1 CI + review threads.**  BF-Director overnight: BotFleet #1 merged 2026-08-31T06:59Z.  Review-thread lane closed.  No extra-ship. <!-- wb-agent-report:b83965289fa64a1b85f873cba817b7bb -->
- [AG] 2026-09-01 — iOS fallback provider dropdowns, DeepSeek harness GUI availability & blue logo, and Antigravity logo separation (branch `ag/ios-fallback-provider-deepseek-logos`). Adds Provider and Model pickers for every fallback row in iOS Agent Profile, fixes AntigravityMark in ProviderMark, ensures DeepSeek harness (dsh) is discoverable in GUI with official blue whale logo. Gate: Swift tests clean, typecheck clean, build clean.
- [AG] 2026-08-31 — Sentry client observability: Session Replay, error capture & distributed tracing (PR #44 merged to `main`). Integrated `@sentry/react` client error monitoring, Session Replay (100% on error, 10% baseline session, privacy-masked), and distributed browser tracing in `src/lib/sentry.ts` and `src/main.tsx`. Gated on `VITE_SENTRY_DSN`. Gate: typecheck clean, 2,271/2,271 tests clean. Rollout: `docs/rollouts/2026-09-01-sentry-client-observability.md`.
- **2026-08-30 - CLAUDE - COMPLETED - BotFleet.app site: feature list (beta/established) on Vercel + CF zone on Usage.Jays.Services + Namecheap NS repoint.** <!-- wb-agent-report:4637b497ff564bc4a30106d1baea9eba -->
- (none)

## In Progress
- **2026-09-08 — CLAUDE — COMPLETED — FLEET-INFRA-C6 (F09) verify/close, sentry-ci-report ios-ship coverage (F10/CM) (branch `claude/ios-bot-computer-refs`, worktree `~/apps/botfleet-claude-ios-bot-computer-refs`).**  F09: grepped every `.computer` reference across `ios/**/*.swift` — the dangling `Bot.computer` compile error PR #117 left in `ComputerView.swift:67` was already fixed by PR #199 (2026-09-04); confirmed via CI history (every "Swift tests + iOS build" job run since has passed) since this Mac lacks the iOS platform component for a local `xcodebuild` repro.  Resolved FLEET-INFRA-C6 via Sentry API (root cause fixed; its "escalating" status since has been driven by an unrelated windows-latest CI flake under the same broad fingerprint — separate issue, not fixed here).  F11: `sentry-ci-report.yml` exists, intentionally diverges from the ai-fleet-coordinator template (adapted from Usage-Monitor's gold copy per its own header) — no sync needed.  F10/CM: `ios-ship.yml` has `schedule: '18,48 * * * *'` but was missing from both the `workflow_run.workflows` observed list and `CRON_SCHEDULES` (stale entries still named the deleted `ios-testflight.yml`'s "iOS TestFlight Release") — added correct entries in both files.  FLEET-INFRA-CM was already resolved (self-resolved, moot). <!-- wb-agent-report:c630ceed -->
- **2026-09-08 — BF-COMPILER — IN PR — Bot-to-bot chats out of Apps/Channels; restore ubf checkout; local Update when GitHub has no latest-mac.yml (board `6ce09e3d`, branch `compiler/bot-chats-sidebar`, worktree `~/apps/botfleet-compiler-ubf`).**  `getOrCreateChannel` no longer copies `from.section`.  Sidebar lists DMs under Bot Chats.  `~/apps/botfleet-server` detached worktree restored.  No TestFlight upload.
- **2026-09-07 — AG — COMPLETED — Fix Vercel auto-deploy rate-limit query (&state=READY) across sites (branch `ag/vercel-ignore-state-ready`, worktree `~/apps/botfleet-ag-vercel-fix`).**
- **2026-09-06 — GROK — COMPLETED — Settings Remote Access UX for named tunnel https://botfleet.jays.services (board `a4b7edb0`, issue #226, branch `grok/remote-access-named-tunnel`, worktree `~/apps/botfleet-grok-remote-access`).**  Designer copy.  Copyable Remote URL.  No TryCloudflare on this path.  Companion Gateway on Phone.
- **2026-09-03 — GROK — COMPLETED — Hosted macos-latest ios-ship path filter like CT/DealDex/ST (board `f9a51214`, issue #185, branch `grok/hosted-ios-ship`, worktree `~/apps/botfleet-grok-hosted-ios-ship`).**  Vendor `scripts/ios-fleet`, map existing `APPLE_API_*` + `IOS_CERT_*`, no extra flags, no mint.  PR opening this lane.  Merge may trigger the hosted job with `force_ship=0`.
- **2026-09-02 — AG — COMPLETED — DeepSeek-style elapsed turn timer in TurnPresence and duration in ActivityRun (branch `ag/elapsed-seconds-display`).**
- **2026-09-02 — AG — COMPLETED — Brand logo crop refinement, Antigravity Gemini mark, Kimi picker filtering, unhide EngineProbe bot (branch `ag/gemini-logo-kimi-filter-probe-sidebar`).**
- [AG] 2026-09-02 — COMPLETED — Fix Vercel deploy errors and enable custom rate limiting (branch `ag/vercel-git-ignore`, worktree `~/apps/botfleet-ag-vercel-fix`). Removed .git from .vercelignore for git diff change detection, updated watch_args to :(top)apps/site in vercel-ignore-hourly.sh, cleaned up duplicate Vercel project, and synced VERCEL_TOKEN.

## Planned / Reserved
- **2026-09-24 - CLAUDE - COMPLETED (PR #621 merged `b95bdfd8`) - Add deploying-seat + PR number to Sentry release/deploy metadata (pattern from UM).**  `.github/workflows/sentry-deploy.yml` derives the deploying PR's branch prefix and PR number and tags the Sentry deploy record with `seat:<seat> pr:#<number>`, mirroring Usage-Monitor PR #1536; see `docs/rollouts/2026-09-25-deploy-seat-tagging.md`.  Board `1af9e286` (duplicate `0a4a04ed`). <!-- wb-agent-report:1af9e28693c24219b4883ee675769086 -->
- **2026-09-24 - CLAUDE - PLANNED - BotFleet: verify whether claude driver's stop_reason=stop_sequence is really a failure before allowlisting it.** <!-- wb-agent-report:64324c2c65d44e35b682a135de63c399 -->
- **2026-09-25 - CLAUDE - IN PR (#628, awaiting review) - BotFleet: 89% of invoke_agent internal_errors are ACP 'initialize timed out' (60s), concentrated on DSH/deepseek.**  Branch `claude/sentry-acp-init`, worktree `~/apps/botfleet-claude-sentry-acp-init`; board a0f9033b.  Load-scaled initialize deadline (DSH base 120 s), one relaunch cap. <!-- wb-agent-report:a682f434755c4fb2b98c4adea728ea07 -->
- **2026-09-24 - CODEX - PLANNED - Investigate current BotFleet fleet recall degraded readiness.** <!-- wb-agent-report:c01901afdc1044c6ad82e30bf9bbda28 -->
- **2026-09-24 - CODEX - PLANNED - Align DSH Flash display with canonical DeepSeek slug through Harness.** <!-- wb-agent-report:c0a77d0584ac4412a656d1d397d4c4bb -->
- **2026-09-24 - CODEX - PLANNED - Refresh Claude static picker for Opus 5.5 and transport-safe aliases.** <!-- wb-agent-report:91142572c8f44b25bb9f81d07849b670 -->
- **2026-09-24 - CODEX - PLANNED - Warn or reject oversized current API prompts without silent truncation.** <!-- wb-agent-report:9303476903b945418379b49c917dd5f6 -->
- **2026-09-24 - CODEX - PLANNED - Update Mac BotFleet checkout and installed app after active work drains.** <!-- wb-agent-report:6661ceeacdb14de7a2992ab739e74664 -->
- **2026-09-24 - CODEX - PLANNED - Complete ACP usage dimensions and fallback attribution.** <!-- wb-agent-report:2c6611a1067843c99b39a9de5228dbb8 -->
- **2026-09-24 - CODEX - PLANNED - Reduce sidebar resort and streaming Markdown work.** <!-- wb-agent-report:4185949867cc4804971d3b690894f2d0 -->
- **2026-09-24 - CODEX - PLANNED - Page BotFleet messages in SQLite and bound server thread cache.** <!-- wb-agent-report:c524b2b1daa240a0937ff141cac70a6c -->
- **2026-09-22 - BF-Director - PLANNED - [Director] Per-engine execution wisdom registry: how each engine handles its task classes best.** <!-- wb-agent-report:4a042037a1ee4c4f960844a9c6c1f541 -->
- **2026-09-22 - BF-Director - PLANNED - [Director] Engine capability uplift + wisdom registry: bots should do tasks efficiently, not just stop attempting doomed ones.** <!-- wb-agent-report:3997db1bebf14a8ebb0d19f66b6cb202 -->
- **2026-09-22 - BF-Director - PLANNED - [Director] Engine capability gate: stop wasted-token retry storms on known-doomed engine/task combinations.** <!-- wb-agent-report:5fc628cbeeec4f6cbb01613f72ffaedb -->
- **2026-09-22 - MM - PLANNED - [BF][MM] Sentry SDK silently fails to load — Dynamic require of 'util' not supported in ESM context (server/sentry.ts:194).** <!-- wb-agent-report:91d45243824948acbeab728c15d632d8 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] iOS companion: spoken replies where auto-speak is on (fetch mp3 from /api/tts/speak, play in chat view).** <!-- wb-agent-report:dfbbddbcd8c3483cbcdae21f55981144 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Voice Settings: 'Use my cloned voice' global toggle (off by default, per-bot override preserved).** <!-- wb-agent-report:18da00ff043d4270b4adf29867318527 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Voice cloning pipeline: record on Mac, upload to MiniMax Voice Clone API, persist voice_id.** <!-- wb-agent-report:c6858d6654a343b8b2c82ee745c79a79 -->
- **2026-09-21 - BF-FIXER - PLANNED - P1: agents.botfleet.app 502 - 8810 companion sidecar down (BotFleet.app not running).** <!-- wb-agent-report:a3483a9eb5a7405998283ebb3bf702a2 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Audit: fleet-wide open PR + draft-PR strategy review.** <!-- wb-agent-report:5b1f44009e094e34ac5cd6f97c66276f -->
- **2026-09-21 - MM - PLANNED - [BF][MM] iOS ProviderMark: DSH-hosted MiniMax renders DeepSeek logo (driverKind doesn't reflect underlying model engine).** <!-- wb-agent-report:ce681a5dd0914cf38a5461ade47027f1 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Mac auto-updater: latest-mac.yml is missing from v0.1.38 GitHub release, so auto-update 404s even when node_modules is restored.** <!-- wb-agent-report:9ef14d2b9303465eb54c691c8619ee59 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] iCloud sync (KVS or CloudKit) for bot configs across iPhone/iPad/Mac + Sign in with Apple capability.** <!-- wb-agent-report:5382888f68b74f969fc643c41c3bab10 -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Engines picker: dedupe detected binaries by resolved file (symlink/inode dedup) so leftovers don't show.** <!-- wb-agent-report:c32523947f63411bae14fccb99e1677a -->
- **2026-09-21 - MM - PLANNED - [BF][MM] Bundle ID review: switch com.botfleet.app -> app.botfleet.<product> at next major version.** <!-- wb-agent-report:47ead976d1e740b789fce7a794e85804 -->
- **2026-09-20 - MM - PLANNED - [BotFleet][MM] V2 chip-path hardening: route account-level 402 chips through recordInstanceCap so all bots on the same instance fall back instead of each one re-paging.** <!-- wb-agent-report:12d09a016aab44aa88507c72f3183bf3 -->
- **2026-09-19 - BF-FIXER - PLANNED - Recurring 900s turn stalls on MiniMax thread f24047c4 (PD #169/#170); update path blocked.** <!-- wb-agent-report:bf77b43412204bdc9d198a58a218e860 -->
- **2026-09-19 - MM - PLANNED - [BotFleet][MM] IN PR #494 — iOS push-health Settings row + text-input reply action (closes #389).  Lane minimax/389-push-settings-row, worktree ~/apps/botfleet-mm-389.  Files ios/Sources/CompanionCore/PushSenderHealth.swift (+116) + ios/Sources/CompanionCore/NotificationActions.swift (+40) + ios/App/Session.swift (+121) + ios/App/SettingsView.swift (+90) + ios/App/Notifications.swift (+37) + ios/Sources/CompanionCore/Client.swift (+16) + tests +544/-11.  367/367 swift pass (was 347).  Squash auto-merge armed.  iOS-only change, no edits to desktop sidecar or harness..** <!-- wb-agent-report:268b6da3ef9349bb992f1e27a82cd2f5 -->
- **2026-09-16 - CLAUDE - PLANNED - Windows CI flake: electron/attached-ui-shim.node-test.mjs around line 158 (does not retry an idempotent upstream request after its downstream closes) is a timing assertion expecting attempts 1 and sometimes sees 0; failed once on #457 and passed on rerun; needs a deterministic wait.** <!-- wb-agent-report:6697c92992f84e318103d3e7b2bb59fe -->
- **2026-09-16 - CLAUDE - PLANNED - ACP mcpServers evidence: no ACP harness (Cursor, Qwen, Hermes, OpenCode, Droid, Kimi, DeepSeek, Grok ACP) has been verified to honour session/new.mcpServers, so all nine advertise MCP on a default; build a one-turn per-engine smoke and set AcpSupport.mcpServers from evidence (gap C7).** <!-- wb-agent-report:63f63e79425343d59c49a356b75a56ec -->
- **2026-09-16 - CLAUDE - PLANNED - HTTP-lane capability expansion for MiniMax, OpenAI-compat and Grok HTTP: GUI computer, cloud desktop, VM and VPS, phone, Composio and fleet recall are unreachable because these engines cannot mount stdio MCP; needs tool-registry records and gates inside the MiniMax series keep-out (gap C3, R2-HTTP, C9 from the Sep 16 audit).** <!-- wb-agent-report:da75e2dabf454a1fa1b257733b8f19dc -->
- **2026-09-16 - CLAUDE - PLANNED - Live ~/apps/botfleet-server checkout 37 commits behind origin/main — missing turn-context byte cap and other fixes.** <!-- wb-agent-report:efcde7b5c4b343d9a3a1b74e17e8644b -->
- **2026-09-15 - MINIMAX - PLANNED - MM PR 11: generalize the chat-completions base, add image + effort + price parity, per-tool timeouts.** <!-- wb-agent-report:2bd6577a9a634b0a8e2fe7b247f48b55 -->
- **2026-09-19 - MM - IN PR #490 - BotFleet #392 ios-ship: scripts/ios-fleet/publish-ios-versions.sh + offline test (branch `minimax/360-error-page-retry`, worktree `~/apps/botfleet-mm-360`, files `scripts/ios-fleet/publish-ios-versions.sh` +211 + `scripts/ios-fleet/test-publish-ios-versions.sh` +174).  `record_successful_ship()` in `scripts/ios-fleet/ship-testflight.sh:591` calls the publisher; without it, every hosted TestFlight ship silently no-ops `ai-fleet-coordinator/site/ios-versions.json` and the iOS TestFlight-update banner in #381 never fires for `app.botfleet`.  Publisher reads `--bundle-id --version --build --apple-id --display-name`, resolves AFC root from `AI_FLEET_COORDINATOR_ROOT` or `~/Code/ai-fleet-coordinator` or `/Users/jay/apps/ai-fleet-coordinator` or `gh repo clone` tmpdir, edits `site/ios-versions.json` with jq (node fallback), bumps `updatedAt` only when the entry actually changed (idempotent re-runs do not produce empty commits on AFC main), commits the one file with author identity `BotFleet iOS Ship <ship@botfleet.local>`, and pushes to AFC main — all best-effort: a missing checkout, no-auth `gh`, or a dirty AFC tree logs a warning and exits 0 so a successful ship is not failed by a manifest glitch.  Test (`test-publish-ios-versions.sh`, runs offline in a tmpdir with a fake bare origin): 18 assertions across 6 cases — fresh ship, idempotent re-run, partial-arg merge (appleId/displayName preserved), brand-new bundle id, missing-required-arg refusal, dirty-tree refusal.  All green.  Squash auto-merge armed, PR open against `main` from `origin/minimax/360-error-page-retry`.  Owner-facing copy edit only; no behaviour change in the iOS ship itself.**
- **2026-09-19 - MM - IN PR #494 - BotFleet #389 ios-settings: show push sender health + UNTextInputNotificationAction reply (branch `minimax/389-push-settings-row`, worktree `~/apps/botfleet-mm-389`, files `ios/Sources/CompanionCore/PushSenderHealth.swift` +116 + `ios/Sources/CompanionCore/NotificationActions.swift` +40 + `ios/App/Session.swift` +121 + `ios/App/SettingsView.swift` +90 + `ios/App/Notifications.swift` +37 + `ios/Sources/CompanionCore/Client.swift` +16 + tests +544/-11).  Brings the iOS Settings page the same "Closed-app notifications" row the desktop pairing page has had since PR #383.  New `PushSenderHealth` Codable type mirrors `companion/src/apns.ts`; `PushSenderHealthView.summary/detail/notReported` are pure render helpers with locale-aware `relativeDescription` injectable so tests pin the phrase.  `CompanionClient.pushSenderHealth()` typed fetch for `GET /api/companion/push-health`; 404 returns nil, treated as "not reported by this computer".  `Session` caches the latest `PushSenderHealth`, exposes `refreshPushSenderHealth()`, resets on `signOut()`.  `SettingsView` row between pairing and connection sections, copy per the issue spec: no key, key rejected (Apple reason verbatim), recent errors or drops.  Paired with `UNTextInputNotificationAction` on `BOTFLEET_QUESTION` (identifier `botfleet.questions.reply`) wired into the same `answer` flow with typed text as the choice — the #383 follow-up the issue called out.  Tests: 367/367 swift pass (was 347).  iOS-only change, owner-facing copy edit, no edits to desktop sidecar or harness.**
- (none)

## Changelog of this log
- 2026-09-19 — MM — **`#392` shipped on PR #490 (`minimax/360-error-page-retry`, +211 +174): the missing `scripts/ios-fleet/publish-ios-versions.sh` that `record_successful_ship()` was calling under `>/dev/null 2>&1 || true` is now a real, idempotent publisher of `ai-fleet-coordinator/site/ios-versions.json`, with an offline 18-assertion bash test.  Squash auto-merge armed; CI on mac/ubuntu/windows/control-plane/iOS/package+smoke in flight.**
- 2026-09-01 — GROK picked up CLAUDE analysis v2 (`781554fd`); report-only on `claude/analysis-v2`.
- 2026-09-01 — GROK moved resource-triggers (#65/#80) and iOS Sentry Cocoa (#55) from In Progress to Completed after merge.  Claimed Batch 4+6 honesty/docs/ops on `grok/delta-audit-fixes`.
- 2026-08-31 — GROK filed P0 fallbacks (e556f063) plus seven P1s from the full-stack audit; comments on d261ef00 / 92a254df / 02ca3c98 / 12ccfacd / 80dd2680.
- 2026-08-31 — GROK claimed top-to-bottom full-stack audit (issue #22, board f5abf14d).
- 2026-08-29 — Deployed always-on iMessage bidirectional relay for 12 BotFleet bots (AG).
- 2026-08-28 — bootstrapped by onboard-new-app.sh.
- **2026-09-04 - MINIMAX - COMPLETED - settings-rev2-B: engines enable/disable, Antigravity collapse, weekly+5h window surfacing, DeepSeek balance, Cursor monthly cap end-to-end.  PR #218 squash `f6e75930`.  Board `0ad2b059`.  Lane B of the settings revamp.**
- **2026-09-17 - AG - COMPLETED - Fix packaged Sentry SDK bundling.** Landed in #463. Replaced dynamic require with awaited dynamic import to ensure @sentry/node is bundled natively by esbuild, while preserving fast boot for tests.
- **2026-09-20 - MUSE - COMPLETED - Fleet-wide PR sweep: resolved conflicts/review threads and merged #471 (c667d72f), #493 (9c5b01d5), #500 (8e56fa4b), #502 (89104b1f), #504 (e56991d0). Board `e6f3e6d79`.**
- **2026-09-21 - MUSE - COMPLETED - BotFleet PR #514 sweep lane (board e146fd05): all fixes implemented, CI green, review threads resolved — merge held for Jay's decision.  Final head b55c49a7 (11 ahead of main, mergeable_state clean).  All 8 CI check groups green on the exact head; 18/18 review threads resolved with replies.  Round 1: forced-quiesce safety (interrupted run/turn snapshot with stale-interrupt guards), durable bot snoozes.  Round 2: forced-quiesce rollback on refusal, room-turn refusal before fencing, boot recovery preserves manual snoozes.  Round 3: queued runs cancelled in the forced fence, stop-turn conflict check before mutations, installBlockedBusy helper for update UI.  Jay's session added b55c49a7 on top (routine-turn dedup, resume on rollback/unquiesce).  Validated on the Mac: typecheck clean, 52/52 routines tests, 28/28 update-control tests, unsigned iOS build succeeded.  PR left open, NOT merged — awaiting Jay's merge decision.**
- **2026-09-22 - MM - INPROGRESS - APNs HTTP/2 transport hardening on `minimax/apn-transport-hardening` in `~/apps/botfleet-mm-apn-transport`.  Replaced native `fetch` with persistent `node:http2` session (TCP+HTTP/2 keepalive, GOAWAY rebuild, per-(host,keyId) cache).  Captured real transport error via `inspectTransportError` + `classifyTransportError` (transport / http2_protocol / socket_closed / timeout).  Added circuit breaker: 20 transport failures or 5 HTTP/2 protocol errors open a 60s window that skips sends and increments `dropped`.  Surfaced Apple's `timestamp` field on `InvalidProviderToken` directly on the pairing page.  Updated `PushSenderHealth` with `failureKind` / `lastErrorCode` / `consecutiveTransportFailures` / `circuitOpenUntil`.  Audit `docs/audits/2026-09-22-apn-transport-review.md`.  Tests: 69/69 in `companion/src/apns.test.ts`.  Resolves board `851f6868afc748d78ac2da02fa0541f9` (P2 APNs 1825 SendFailed since 21:50) and `d4499f2d` (P2 follow-up).  Worker sub-agent started the lane; orchestrator finished after worker hit context ceiling.**
- **2026-09-23 - MM - COMPLETED - botfleet-server harness reset.  Always-on harness at `~/apps/botfleet-server` was at detached `2b6b907b` with one uncommitted change to `server/tools/registry.ts` (recall_contribute schema `additionalProperties: false` tightening); updater refused to update.  `git stash` + `git fetch origin main && git reset --hard origin/main` + `pnpm install --frozen-lockfile` (~58s); stashed change preserved for a separate one-line PR.  Harness now at `e3923319` (origin/main), health endpoint reporting `{"app":"botfleet","pid":1448,"static":true}` on `127.0.0.1:8799`.  LaunchAgent `app.botfleet.server` unchanged.  Updater clean to refire.**
- **2026-09-23 - MM - INPROGRESS - Computer Use settings redesign lane kicked off at `~/apps/botfleet-mm-computer-use-settings` on `minimax/computer-use-settings-redesign`.  Resolves board `9613f135` (P2 — Computer Use settings: per-provider enable/disable + impact-confirm + matrix view) and `660f5bf2` (filing parent).  The 2026-09-21 filing claimed `@TBD` worktree but no worktree was ever created; PR #517 (merged) was a different lane (toggle highlight + VPS-hint + sentence-gap regression), NOT the redesign.  Lane now dispatched to a worker with the full plan from the original MM filing.**
- **2026-09-23 - MM - INPROGRESS - Linq iMessage integration.  Worktree `~/apps/botfleet-mm-linq-imessage` @ `minimax/linq-imessage`.  Jay's 2026-09-23 reversal on the iMessage transport: drop Photon, ship Linq as a parallel transport next to the existing Mac relay (`~/apps/botfleet-imessage-relay.py` + LaunchAgent `com.jay.botfleet-imessage-relay` + `[to iMessage]` / `[from iMessage]` tags).  Added `server/linq/{types,client,dispatch}.ts` (v3 partner-API wrapper + typed `LinqApiError` + shared `ingestInbound()` core so the Mac relay and Linq keep one prompt-wrapping path) + `server/routes/linq-webhook.ts` (`POST /api/webhooks/linq` with HMAC-SHA256 via `LINQ_WEBHOOK_SECRET`, falls open when unset — hobby tier makes signing opt-in) + `server/tools/linq.ts` (`send_voice_message` tool: hosted MiniMax TTS → signed S3-style upload → iMessage mp3 attachment, gates on `ctx.linq` so unconfigured bots never burn TTS quota).  Wired `LINQ_VOICE_MESSAGE` into `HARNESS_TOOLS` + `createTurnToolHost` in `server/tools/host.ts`; per-bot `imessagePerBot` map + workspace `imessageLinq` block in `server/config.ts`; tokens stay in `process.env` (`LINQ_API_TOKEN`, `LINQ_WEBHOOK_SECRET`) — never on disk.  `src/components/LinqSettings.tsx` under Settings → Connections (phone number, sender chips, voice toggle, per-bot dropdown, ngrok tunnel instructions, "send self-test" button).  Tests: 156 passing across `server/linq/{client,dispatch}.test.ts`, `server/routes/linq-webhook.test.ts`, `server/tools/linq.test.ts`, `server/config.test.ts` imessageLinq block, `src/components/LinqSettings.test.tsx`.  `git grep -E 'LINQ_API_TOKEN\s*=\s*"'` returns zero.  PR #529 open (`minimax/linq-imessage`), `gh pr merge --squash --auto` armed; CI runs pending on typecheck + test (mac/ubuntu/windows), Swift iOS, control-plane, package+smoke.  Audit `docs/audits/2026-09-23-botfleet-linq-imessage.md`.  Resolves board `9c415bb589124389b133e4072d694101` (P2).**
[AG] repo: BotFleet. Completed efficiency improvements (preserving performance/effort) in PR #541.

## 2026-09-24 — MINIMAX — Bound replay-history byte cap across minimax/grok/openai-compat drivers (Codex review 09-24)

**Branch:** `minimax/botfleet-codex-review` @ `164759e4` (worktree `~/apps/botfleet-minimax`).  **PR:** [#591](https://github.com/jaywedgeworth22/BotFleet/pull/591).

**Why:** Codex review (thread `01a08556-967c-7043-b6e9-dc207265ad7d`, board `92effec6`, audit doc TBD) flagged that the three API-backed transcript-replay drivers — `minimax.ts`, `grok.ts`, `openai-compat.ts` — fold the FULL thread transcript into every chat-completions request.  Because the loop re-sends a byte-identical prefix on every round to keep the provider's prompt cache reachable, an oversized transcript gets re-uploaded on every tool call too.  Long threads grow past the model's prompt window or the per-request size cap, and a single pasted stack trace multiplies the cost across rounds.  This is the largest replay-driven token cost in the driver layer.

**What:** New shared helper `server/drivers/chat-completions/replay-cap.ts` exporting `capReplayedTranscript(transcript, { maxBytes?, maxEntries? })`.  Walks the transcript from newest to oldest, trims the OLDEST entries until the kept prefix fits inside both caps (defaults: 200 KiB / 60 entries), and preserves the most recent tool-call / tool-result pair even when the byte cut would orphan one half — preferring an oversized prefix over a malformed chat-completions message that 400s the request.  Counts UTF-8 bytes (not JS chars) so multibyte text is budgeted at the same rate the provider's tokenizer will see it.  Wired into the three drivers' `sendTurn` before they build the messages array.  System prompt and current user turn are always kept (uncapped); only the prior transcript is trimmed.

**Tests:** 11 unit tests for the helper + 3 driver-level regression tests (one per driver, asserting that an 80×5 KiB transcript is trimmed before it reaches the request body).  `pnpm typecheck` clean; 105 driver tests green (replay-cap 11 + grok 15 + minimax 49 + openai-compat 30).  Existing short-transcript tests preserved — the cap only trims over-budget transcripts.

**Files:**
- `server/drivers/chat-completions/replay-cap.ts` — new helper
- `server/drivers/chat-completions/replay-cap.test.ts` — new unit tests
- `server/drivers/minimax.ts`, `server/drivers/grok.ts`, `server/drivers/openai-compat.ts` — wire cap into sendTurn
- `server/drivers/{minimax,grok,openai-compat}.test.ts` — driver-level regression tests

**Followups:** None blocking.  Cap is configurable per-driver if a future model wants a larger budget (the helper accepts overrides); no other chat-completions driver in this repo uses the transcript-replay pattern (claude/codex/antigravity/etc. are CLI drivers with their own session state).  The 97-finding audit's other findings (telemetry collector resends, DSH gap, Grok 4.7 catalog staleness) remain open under their existing board items.

<!-- wb-agent-report:164759e4f52b49f5b5d9c75d1d560891 -->
