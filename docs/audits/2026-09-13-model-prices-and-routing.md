# BotFleet Model And Pricing Audit — 2026-09-13

This is a bounded, read-only audit of the model IDs and transports exposed by BotFleet.  Prices below are official USD per 1M tokens retrieved 2026-09-13, unless marked as unavailable.  Subscription and OAuth products are reported separately because their quotas do not establish API token prices.

## Codex: Current Choices And Transport Boundaries

| Surface | Verified current IDs | Access and evidence | Price guidance |
|---|---|---|---|
| Installed Codex app-server catalog (`~/.codex/models_cache.json`) | `gpt-6-astra`, `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-5.5` are visible and `supported_in_api: true`; `gpt-reserve` and `codex-auto-review` are hidden; `gpt-5.3-codex-spark` is listed but `supported_in_api: false` | OAuth/subscription Codex CLI.  Local cache reports 272k context / 872k maximum for the 5.6 family and current supported effort sets; this is local app-server metadata, not API billing. | Do not attach API prices to OAuth usage.  Prefer the dynamic `codex app-server model/list` catalog over the static fallback. |
| BotFleet static fallback (`server/drivers/codex-catalog.ts`) | `gpt-6-astra`, `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-5.5`, `gpt-5.3-codex-spark`; default `gpt-5.6-sol` | Fallback rows are not current availability evidence.  The official Codex model guidance says GPT-5.4 and GPT-5.4 Mini were retired from ChatGPT sign-in on 2026-08-31; the local cache also has no 5.4 rows.  The local cache snapshot does not establish availability for any fallback row. | Sol is the compatibility fallback default.  Keep 5.5 as a compatibility row, and keep Spark as the legitimate CLI-only specialty; dynamic app-server results decide account availability. |
| OpenAI API | `gpt-6-astra`, `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`; separate API model `gpt-5.3-codex` | Official API model and pricing pages.  The `gpt-5.6` alias explicitly routes to `gpt-5.6-sol`; no equivalent Terra/Luna alias was shown.  BotFleet's Codex driver removes `OPENAI_API_KEY` so the driver stays on ChatGPT login rather than silently switching to API billing. | Standard short-context prices: Astra $10 input / $1 cached input / $50 output; Sol $4 / $0.40 / $20; Terra $2 / $0.20 / $12; Luna $0.20 / $0.02 / $1.20; GPT-5.5 $5 / $0.50 / $30.  Long-context prices (over 272k) are Astra $20 / $2 / $75; Sol $8 / $0.80 / $30; Terra $4 / $0.40 / $18; Luna $0.40 / $0.04 / $1.80; GPT-5.5 $10 / $1 / $45.  Cache writes are $12.50/$5/$2.50/$0.25 short and double those long where published; Batch/Flex is 50% of standard. |
| OpenAI API specialized coding row | `gpt-5.3-codex` | Official API pricing lists this model; it is not `gpt-5.3-codex-spark`.  Spark is explicitly not API-supported in the local cache. | Standard $1.75 input / $0.175 cached / $14 output; Fast mode $3.50 / $0.35 / $28. |

The rational Codex shortlist is `gpt-5.6-terra` for everyday coding, `gpt-5.6-sol` for difficult changes, `gpt-5.6-luna` for high-volume low-cost work, and `gpt-6-astra` for the hardest end-to-end work when the live OAuth catalog exposes it.  GPT-5.5 is a priced API compatibility row and previous flagship, not the current OAuth default.  For BYOK/API workflows, Terra is the cost/quality default, Sol is the stronger general coding choice, Astra is the premium choice, and `gpt-5.3-codex` is the specialized coding option.  The local cache and app-server catalog should decide OAuth availability at runtime.

OpenAI sources: [Codex model guidance](https://learn.chatgpt.com/docs/models), [API model comparison](https://developers.openai.com/api/docs/models/compare), [API pricing](https://developers.openai.com/api/docs/pricing), [GPT-5.5 model page](https://developers.openai.com/api/docs/models/gpt-5.5), [GPT-5.6 Sol model page](https://developers.openai.com/api/docs/models/gpt-5.6-sol), and [GPT-6 Astra model page](https://developers.openai.com/api/docs/models/gpt-6-astra).

## Other BotFleet Providers

| Provider and BotFleet IDs | Canonical ID / alias finding | Official API prices per 1M | Transport constraint and recommendation |
|---|---|---|---|
| Anthropic: `claude-fable-5`, `claude-opus-5`, `claude-sonnet-5`, `claude-haiku-4-5` | Anthropic API Fable is `claude-fable-5-1`; `claude-opus-5` and `claude-sonnet-5` are current exact IDs; Haiku alias `claude-haiku-4-5` maps to pinned `claude-haiku-4-5-20251001`.  Claude Code officially supports moving aliases `fable`, `opus`, `sonnet`, and `haiku`; full IDs pin a version, and alias resolution varies by provider.  The apps gateway may still resolve its `fable` alias to Fable 5, so the API ID and app alias must remain separate. | Fable 5.1 $10 input / $0.25 cached read / $50 output; Opus 5 $5 / $0.50 / $25; Sonnet 5 $2 / $0.20 / $10; Haiku 4.5 $1 / $0.10 / $5.  Batch input/output is 50%. | Do not rewrite the apps gateway's `fable` alias from the Anthropic API ID alone.  For Claude CLI, aliases are the safer moving picker entries when latest provider resolution is desired; use full IDs when pinning matters.  These rates apply Anthropic API, not Claude subscription CLI. |
| MiniMax API: `MiniMax-M3`, `MiniMax-M2.7`, `MiniMax-M2.7-highspeed` | Exact Open Platform IDs match BotFleet. | M3 <=512k: $0.30 input / $0.06 cached read / $1.20 output; >512k: $0.60 / $0.12 / $2.40.  M2.7 $0.30 / $0.06 / $1.20; M2.7-highspeed $0.60 / $0.06 / $2.40.  Cache writes for M2.7 rows are $0.375. | BotFleet uses the global/China Open Platform API and standard tier.  MiniMax Token Plan subscriptions are separate; do not mix plan quota and API prices.  M3 and M2.7 share the lowest listed standard <=512k input/output prices; quality and coding-task ranking were not benchmarked here. |
| DeepSeek ACP/static: `deepseek-v4-flash`, `deepseek-v4-pro` | Current canonical API ID is `deepseek-flash` (DeepSeek-V4.1-Flash).  `deepseek-v4-flash` remains a compatibility alias; `deepseek-v4-pro` is current. | Peak/off-peak: Flash cache hit $0.006/$0.003, cache miss $0.30/$0.15, output $1.20/$0.60; Pro cache hit $0.044/$0.022, cache miss $1.32/$0.66, output $3.96/$1.98. | BotFleet's `src/lib/deepseek-prices.ts` values are stale versus current official pricing.  Update display logic only after deciding peak/off-peak semantics; do not claim one flat rate.  Use Flash for cost-sensitive coding and Pro for harder work. |
| xAI direct API: `grok-4`, `grok-4-fast`, `grok-3-mini`; Grok ACP: `grok-4.6`, `grok-4.5` | Official current API docs expose `grok-4.6` and `grok-4.5`; no current official price was verified in this pass for the direct static rows `grok-4`, `grok-4-fast`, or `grok-3-mini`. | `grok-4.6` short $2 input / $0.50 cached / $6 output; long $4 / $1 / $12.  `grok-4.5` short $2 / $0.30 / $6; long $4 / $0.60 / $12. | `grok` direct driver uses `XAI_API_KEY`; `grokAgent` uses the subscription CLI and `~/.grok/auth.json`.  Do not infer API prices for CLI subscription rows. |
| Google API / Antigravity: BotFleet rows such as `gemini-3.8-flash-high|medium|low`, `gemini-3.7-flash-*`, `gemini-3.1-pro-*` | Google API canonical ID is `gemini-3.8-flash` with reasoning level as a request setting.  Antigravity's effort-suffixed names are managed CLI catalog IDs, not verified Google API IDs. | Google Gemini 3.8 Flash paid standard through 2026-12-31: $0.75 input / $0.075 cached / $3.75 output including thinking; scheduled 2027 rate is $1.50 / $0.15 / $7.50.  Batch/Flex is 50%. | Antigravity is a managed subscription/pay-as-you-go agent surface; no public per-token price was verified for its effort-suffixed IDs.  Keep Google API pricing separate from Antigravity quota. |
| Cursor ACP: `auto`, `composer-2.5`, `composer-2.5-fast`, provider rows | Cursor's own canonical `composer-2.5`; BotFleet's `composer-2.5-fast` is the fast pricing variant. | Composer 2.5 standard $0.50 input / $0.20 cached read / $2.50 output; fast $3 / $0.50 / $15. | Cursor subscription pools and on-demand usage govern billing; the model is available in Cursor only.  Use Composer 2.5 for Cursor-native agentic coding and keep its prices separate from provider APIs. |
| Kimi ACP/static: `kimi-code/k3`, `kimi-code/k3-256k`, `kimi-code/kimi-for-coding`, `kimi-code/kimi-for-coding-highspeed` | Kimi Code's stable third-party ID is `kimi-for-coding`; `k3` and `k3-256k` are membership model IDs.  The `kimi-code/` prefix is BotFleet/CLI namespacing.  API/third-party configs use raw IDs, and `k3[1m]` is CLI environment syntax only. | No public Kimi Code per-token price was verified.  Kimi Code is subscription quota; Moonshot Open Platform pay-as-you-go is a separate billing system and endpoint. | Keep Kimi Code subscription rows separate from Moonshot API prices.  Highspeed requires the appropriate membership tier and is the same model with faster service. |

Sources: [Anthropic model overview](https://platform.claude.com/docs/en/models/overview), [Anthropic pricing](https://platform.claude.com/docs/en/about-claude/pricing), [Anthropic model IDs](https://platform.claude.com/docs/en/about-claude/models/model-ids-and-versions), and [Claude Code model aliases](https://code.claude.com/docs/en/model-config); [MiniMax paygo pricing](https://platform.minimax.io/docs/guides/pricing-paygo), [MiniMax Token Plan](https://platform.minimax.io/docs/guides/pricing-token-plan); [DeepSeek pricing](https://api-docs.deepseek.com/quick_start/pricing), [DeepSeek updates](https://api-docs.deepseek.com/updates/); [xAI pricing](https://docs.x.ai/developers/pricing), [Grok 4.6](https://docs.x.ai/developers/models/grok-4.6); [Gemini API pricing](https://ai.google.dev/gemini-api/docs/pricing), [Gemini latest models](https://ai.google.dev/gemini-api/docs/latest-model); [Cursor Composer 2.5](https://prod.cursor.com/docs/models/cursor-composer-2-5); [Kimi Code FAQ](https://www.kimi.com/code/docs/en/kimi-code/faq.html), [Kimi Code membership guide](https://www.kimi.com/en/help/kimi-code/membership-guide), [Kimi Code model configuration](https://www.kimi.com/code/docs/en/kimi-code/models.html).

## Dominated Or Stale Rows To Flag

- `gpt-5.4` and `gpt-5.4-mini` were retired from Codex ChatGPT sign-in on 2026-08-31 according to the official Codex model guidance.  `o3` and `o3-mini` were present only in the pre-refresh BotFleet static fallback and have no current local app-server evidence here.
- `gpt-5.3-codex-spark` is not API-supported in the local cache and is a text-only CLI specialty.  The API's separately priced `gpt-5.3-codex` must not be presented as the same model.
- Anthropic API `claude-fable-5` is behind current `claude-fable-5-1`, while the apps gateway may intentionally retain an app-specific Fable 5 alias.
- `deepseek-v4-flash` is now a compatibility alias for `deepseek-flash`; BotFleet's flat DeepSeek price table is stale.
- Antigravity effort suffixes, Cursor subscription labels, and Kimi Code prefixes are transport-specific aliases.  A provider API price must not be inferred from them.

For a current picker, resolve dynamic catalogs at runtime and display transport beside each row: `Codex OAuth`, provider API/BYOK, or subscription CLI.  The shortlist can expose Terra/Sol/Luna/Astra for Codex; Sonnet/Opus/Haiku for Anthropic; M2.7/M3 for MiniMax with cost and context labels; DeepSeek Flash/Pro with peak/off-peak labels; and Composer 2.5 only on Cursor.
